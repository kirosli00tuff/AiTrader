# Python bridge + advisory services (llm_consensus, ml_factor, whale_signal).
#
# EXACT version pins (Task 9 security hardening): reproducible installs, no
# silent supply-chain drift. All services run offline with mock fallbacks.
# Bump deliberately after review; do not loosen back to >= ranges.
numpy==1.26.4
PyYAML==6.0.3
requests==2.32.5
# Encrypted-at-rest credential store (account_manager/credentials.py resolver).
cryptography==46.0.5

# Optional (advanced): PyTorch is the intended production DNN trainer per
# docs/DNN_ADVISORY_DESIGN.md. The shipped Stage-A model is NumPy-based so the demo
# and tests run without it. Uncomment to enable the optional torch trainer:
# torch==2.4.1

# IBKR LIVE only. ib_insync is the client for the local IB Gateway. It is
# imported lazily by execution/ibkr_adapter.py, so the bridge and all tests run
# offline without it. Install it ONLY when the operator intends to place live
# orders through a locally run IB Gateway. Live stays disabled behind the
# approval gate regardless. Exact pin (no supply-chain drift):
# ib_insync==0.9.86
