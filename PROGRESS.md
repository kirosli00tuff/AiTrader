# Project Progress

Status tracker for AiTrader. Read at the start of each session. Update at the end of each session.

## Current State

The C++ safety spine builds clean and runs the offline paper loop (`ctest` 7/7). Polymarket is fully removed (region). Alpaca paper is the primary online loop (`feed_mode: alpaca_paper`, paper + market-data only, no live path). IBKR is wired as the live-only venue via a locally run IB Gateway but stays DISABLED behind the approval gate. The real LLM council is merged (Opus 4.8, GPT-5.5, Gemini 3.1 Pro) with a Claude Haiku 4.5 base-check gate, prompt caching, and cost controls (budget, per-symbol cooldown, token cap, neutral skip). The native strategy layer (momentum + reversion + regime detector, closed-bar eval, native ATR exits) is in; the adaptive tuner learns from real closed-trade PnL (≥30-trade gate); the `dnn_advisory` factor has a real-data walk-forward training pipeline with gated promotion; Coinbase replaces Binance; SEC EDGAR is the sole active whale feed (free, delayed 13F). ClankApp was removed for a dead host. Whale Alert stays reserved as an optional crypto feed and Unusual Whales Pro as the reserved paid equities upgrade, all live-OFF by default. Alpaca is the sole paper venue holding paper credentials (APCA_API_KEY_ID/APCA_API_SECRET_KEY), IBKR is live-only, Coinbase sim-only. A live API health check (GET /health/integrations) verifies each integration with a real round trip, and scripts/test_full_system.sh runs the whole suite; security hardening (loopback bridge, credential masking, pre-commit secrets hook, pinned deps) landed. A separate `rl_advisory` PPO module (gym env, real-fill training gate, walk-forward eval, `/score/rl` bridge endpoint) is built but **shipped OFF** — it never touches the ensemble until an operator toggles `rl_enabled` past the `rl_min_real_fills` gate. Two council cost cuts (risk pre-check + equities market-hours skip) short-circuit doomed/after-hours setups before any provider spend. Council/whale live paths stay behind config/env flags and the bridge. Live trading disabled by default. The React and TypeScript GUI was rebuilt Alpaca-style (dark neutral, one gold accent) with a left sidebar (Paper, Live, Controls, Settings), Paper and Live each holding Overview/Stocks/Crypto subpages (category-filtered server-side), and a Controls page carrying the full GUI Plan surface (weight sliders by layer, per-layer + per-model toggles, champion/challenger + RL enable behind their gates, regime override, budget dial) on validated api_server control endpoints with Level 1 read-only, all additive to the untouched Dash UI. Next up: wire the engine/trainer to consume the controls.json operator overrides, and prove paper-loop stability.

## Stable and Working

- RiskGate: 14 hard checks, final authority on every order, tested
- Kill switch: latching, manual resume required, tested
- Live-trading gate: four independent blocks, live unreachable by design
- Config validation: throws on unsafe values at load
- Secret handling: encrypted store, env fallback, nothing committed
- Alpaca paper: real HTTP for market data and paper orders
- SQLite DAO: 14 tables, WAL mode, append-only audit log
- Dash UI: paper tab, live tab locked, advanced tab, accounts tab
- Real LLM council: 3 providers, Claude Haiku base-check gate, offline mock fallback, 29 tests
- Kill-request wiring: engine consumes the GUI/API halt-request control file each iteration and trips the same latching kill switch (archived to avoid re-trip, manual resume required), ctest-covered
- React GUI (rebuilt Alpaca-style): Paper/Live Overview/Stocks/Crypto subpages (server-side category filter) + a Controls page (weights, layer/model toggles, champion/challenger, RL enable gate, regime override, budget) on validated api_server endpoints, Level 1 read-only, bound 127.0.0.1, 36 backend + 7 frontend tests. Dash UI unchanged (fallback)

**Adaptive real-time layer (2026-07-16, ships DISABLED). The core vision framework is now fully built.** A Finnhub poller (once a minute, held names first, then watchlist, then general market news, reusing the discovery client so there is one rate limiter rather than two) feeds a FREE no-LLM materiality filter (keywords, sentiment magnitude, event type). The vast majority is dropped for nothing and still stored, so the cost claim stays checkable. Only an escalated event reaches the one paid stage: a single cheap Haiku read returning relevance, direction, severity, and a suggested action, on a 20 read/day budget (about $0.40/day) SEPARATE from and ADDITIVE to both the discovery and trading budgets, with a per-poll cap so one news storm cannot spend the day. THE ASYMMETRY IS THE POINT: a live event may make the engine more cautious directly (trim, exit, flag for review, applied through the same native exit path the engine already uses, never a bypass) and can NEVER make it more aggressive directly. An aggressive read becomes a watchlist REFERRAL, and Stage A, Stage B, the four levels, and the RiskGate all still have to agree before anything is bought. There is no flag for event-driven entry because there is no code path to enable, enforced three times independently in two languages (`adaptive/actions.py` DefensiveAction refuses to CONSTRUCT for a non-defensive action and the queue writer takes only that type; `discovery/watchlist.py` lands an adaptive add as `referred`, status derived from source; `core/adaptive_actions.hpp` DefensiveKind has three enumerators, none aggressive, parse is an allowlist, and the consumer never calls the entry path). Three flags, all default FALSE (`adaptive_news_feed_enabled` the master, `adaptive_watchlist_shaping_enabled`, `adaptive_react_defensive_enabled`), in a NEW `adaptive_realtime:` block kept distinct from the `adaptive:` learning tuner. Verified: with the flags off a 12000-step run is behaviorally identical to the pre-adaptive baseline (272 trades / 136 closed, BTC/USD + ETH/USD + SPY, zero adaptive rows, zero API calls, proven against a client that raises on any attribute access). GUI: an Adaptive page (event feed with dropped rows dimmed and labelled, interpretations, defensive actions beside what the engine actually did) plus three armed toggles in Controls. See LIVE_READINESS.md for the graduation criteria.

**Discovery engine + long-term sleeve (2026-07-16, ships DISABLED).** A curated universe (55 crypto refreshed daily to the active 50 by liquidity, 119 stable curated equities) is screened hourly cheap-to-expensive: Stage A free pre-screen (Finnhub quant data + pre-computed sentiment + native technicals, ZERO LLM tokens) to 12 finalists, Stage B Haiku gate to 5 survivors, Stage C the full four-level framework to a handful of verdicts. Hard per-stage ceilings plus a daily discovery council budget separate from and additive to the trading budget (worst case ~$62/month combined, under the $100 ceiling). Survivors join an event-sourced dynamic watchlist both sleeves draw from. Sleeves moved to 70/30 (30 is a CEILING, mechanism unchanged, hard cap now 35%). The research_satellite gains a long-term quality-and-catalyst-plus-council strategy producing a thesis with target and invalidation, reusing the existing satellite path. `discovery_enabled` and `long_term_sleeve_enabled` both default FALSE, verified: with the flags off a 12000-step run is byte-identical in behavior (272 trades / 136 closed, same 4 symbols, zero discovery activity). The real-time news-REACT layer is NOT built, deferred, and will ship gated (see LIVE_READINESS.md).

## In Progress

- GUI overhaul (React + TypeScript, additive). Three pages built (Settings and APIs, Paper, Live) served by a read-only FastAPI backend (api_server/) over the same SQLite DB, loopback only. Coinbase Pro dark theme, sidebar + status bar, WebSocket live stream. Dash UI retained as fallback. Backend 22 pytest, frontend 3 render tests + typecheck + build all green. The engine now consumes the GUI kill-request control file and trips the latching kill switch (2026-07-09), so the kill button actually halts the loop. The GUI Plan controls are now built (2026-07-09): the app was rebuilt Alpaca-style with Paper/Live Overview/Stocks/Crypto subpages and a Controls page (weight sliders by layer, per-layer + per-model toggles, champion/challenger + RL enable behind their gates, regime override, budget dial) on validated backend control endpoints, Level 1 read-only.
- None active. The "close open flags + RL advisory (shipped off) + council cost cuts" prompt is complete (2026-07-05): every follow-up flag from the 12-task prompt is cleared, `rl_advisory` (PPO, shipped off) is built, and the two council cost cuts (risk pre-check + market-hours) are in. See "Open Flags / Follow-ups" and RETURN.md.

## Not Started

- Live-approval workflow end to end (`try_enable_live` still never called by design)
- Real (disabled-by-default) live adapter for Coinbase (IBKR live adapter is now wired behind the gate, 2026-07-06)
- Training the RL advisory policy: the `rl_advisory` PPO module is built but shipped OFF and untrained (activates only past the `rl_min_real_fills` gate, default 500 real fills; no synthetic-data path). Supervised `dnn_advisory` is the only Layer-3 signal serving today.
- Engine/trainer consumption of the controls.json operator overrides is now COMPLETE (2026-07-13). The engine consumes the per-layer enable toggles, per-level source, the kill request, the feed/clock toggle, and now the model toggles, regime pins, and budget, each iteration. Promote/rollback of the dnn champion execute through the Python registry path at the audited endpoint (gated). RL enable stays gated behind `rl_min_real_fills` (set_rl refuses below the gate). Nothing operator-writable weakens a Level-1 limit.

## Next Up

**THE CORE VISION FRAMEWORK IS NOW FULLY BUILT (2026-07-16).** Every layer the
project set out to build exists in the codebase: the deterministic safety spine
and the RiskGate, the native strategy layer with a regime detector, the adaptive
tuner learning from real closed-trade PnL, the LLM council with its Haiku
base-check gate, the dnn_advisory and rl_advisory factors, whale signals, the
two-sleeve core-satellite split, the long-term research strategy, the discovery
funnel with its dynamic watchlist, and now the real-time adaptive layer with both
its observe-and-shape and react halves.

**No major capability remains to be invented.** What is left is a different kind
of work, and it is deliberately not more building:

1. **Validation.** Run the loop and prove it stays stable: no drift, no leaks, a
   sane tuner past its 30-trade gate, a DB growing cleanly. This has been the
   gate before new capability all along, and now nothing is queued behind it.
2. **Data-gated graduation.** Several layers are built but shipped off and cannot
   honestly be turned on until the evidence exists. Each has an explicit,
   non-negotiable gate: `rl_advisory` needs 500 real fills (no synthetic path);
   `dnn_advisory` still serves a synthetic-trained champion until enough real
   labelled samples exist; discovery, the long-term sleeve, and the three adaptive
   flags each need their prerequisites and an operator decision. The gates are the
   plan. Waiting for them is the work.
3. **The deliberate live decision.** `try_enable_live` is still never called, by
   design. Live trading remains off behind the four-block approval gate, and
   turning it on is a human decision made against evidence, not a milestone to
   reach.

The temptation from here is to keep adding layers. The honest next move is to run
what exists, let the data accumulate, and let each gate open on its own terms.
See LIVE_READINESS.md for the per-capability criteria.

## Known Issues and Caveats

- **Non-strategy exits no longer inflate the real-fill gates (2026-07-16, RESOLVED).** `trades` now carries an `origin` discriminator (`strategy` | `adaptive_react` | `rebalance`), and `count_closed_trades` counts STRATEGY fills only. That function gates the DNN real-data trainer AND the RL 500-fill activation (`rl_min_real_fills`, a CLAUDE.md hard rule); both build their features from `bars`, so it is purely a gate, and the question it must answer is "has the POLICY been exercised enough", not "have any fills occurred". An adaptive defensive exit and a sleeve rebalance trim are real fills but not policy decisions. Filtering makes both gates STRICTER (they open later, on fewer but meaningful fills), which is the safe direction for a gate whose job is to say "not yet". The rebalance half of this bug PREDATED the adaptive layer and is fixed by the same change. Caveat: a DB written before the migration has no column to filter on, so `count_closed_trades` falls back to the unfiltered count there rather than crashing; historical rows backfill to `strategy` and any pre-existing rebalance trims stay miscounted, because the information to tell them apart was never recorded.

- Advisory factor *scores* on the default (no-`--bridge`) path are deterministic C++ mocks; the real LLM/dnn/whale scores engage only with `--bridge` + the bridge server up. The learning signal the tuner consumes is real (closed-trade PnL), but the verdicts feeding it out-of-box are stand-ins.
- Shipped `dnn_advisory` champion is still synthetic-trained; `rl_advisory` is untrained (real-fill gate unmet) and shipped off. The real-data trainer now DOES train a challenger once enough history exists (verified against a synthetic-regime run: 47,900 samples, `challenger_recorded`).
- Native fills now flow offline via `feed_mode: synthetic_regimes` (or `replay`) + `clock_mode: simulated` — the flat random-walk still produces ~zero native trades by design. **Tuner-throttle residual CLEARED 2026-07-13:** `adaptive.rule_based_weight_floor` (default 0.35) floors both the tuner's raw rule_based weight and rule_based's normalized share of the gate verdict, so the native conviction is never diluted below the gate minimum. A 5000-step synthetic run now keeps generating native entries past 100 closed trades (was ~30). Remaining synthetic-mock cap: the RiskGate `required_model_agreement_count` gate (the momentum-biased mock advisory factors disagree with mean-reversion entries), a mock-data artifact a real council would not have, not the tuner.

## Open Flags / Follow-ups

Cleared 2026-07-05 (all verified this session): venv created and full `pytest tests/ -q` green (**124 passed**); `python -m ml_factor.train_real` run against the demo DB (refuses cleanly with `insufficient_real_data`, synthetic champion retained); real SEC EDGAR 13F fixture recorded (ClankApp left SYNTHETIC — host DNS-unreachable); residual doc-consistency sweep done (`docs/ARCHITECTURE.md`, `docs/BUILD_SPEC.md`, `docs/FOLLOWUP_CREDENTIALS.md`, `DNN_RL_DESIGN.md`→`DNN_ADVISORY_DESIGN.md`) + AUDIT honest-state refresh; bars OHLCV storage landed (was "no historical price data persisted").

**Paper-loop-stability flag CLEARED 2026-07-05 (feed work):** the offline loop now generates real native fills. Config-tunable strategy thresholds, a deterministic synthetic-regime feed, a historical replay mode, and a simulated clock landed; a verified synthetic run produced 31 closed native trades (momentum + reversion, ATR exits) and `train_real` trained a real-data challenger (47,900 samples). Replaced by the tuner-throttle residual noted in Known Issues above (native entries plateau after ~30 fills as the tuner de-weights `rule_based`).

- ~~**Six `layer.whale True -> False` events on 2026-07-17 (08:39Z to 08:44Z) are unexplained.**~~ **MECHANISM ESTABLISHED AND REPRODUCED 2026-07-21, closed in class by two earlier fixes.** It was never a phantom writer, which is why the 2026-07-18 search for a writer restoring True correctly found none: the FILE stayed False throughout. The cause is a READ failure. `api_server/controls.read_controls` falls back to `_defaults()` on any read error, and the layer default is True, so a click whose read could not open the file reports `old=True` while the disk says False, exactly the observed shape. Reproduced three ways this session against a scratch control dir: a torn read (empty file from a truncating writer mid-write) returns default True, an open failure (unreadable or fd-exhausted) returns default True, and a repeated-click loop with the reader failing each time reproduces `old=True new=False` with the disk staying False. This is the SAME phenomenon as the 2026-07-19 "engine reads discovery ON, funnel reads OFF" finding (closed by the keystore fd-leak fix), in a different key with a different default (discovery OFF, layers ON). Both candidate triggers were fixed: the torn read by the atomic-write fix (2026-07-17 ~07:23), the fd exhaustion by the keystore single-connection fix (2026-07-19). NOT recurred since (no `diagnostics/layer_unaudited_change*` file exists). RESIDUAL, unprovable rather than open: which trigger fired at 08:39Z on 2026-07-17. The events are 76 minutes after the atomic-write fix, which argues against a torn write unless a stale pre-fix backend was still serving, and fd evidence for that hour was never captured (the instrumentation postdates it). The capture that would catch a recurrence records the fd count, the process start time, and the file bytes, which is adequate to distinguish the triggers; it simply has not had to fire.
- ~~**`whale_position_scale_cap` (0.35) and `dnn_position_scale_cap` (0.5) are parsed and range-validated but no consumer enforces them.**~~ **RESOLVED 2026-07-18 by REMOVAL.** Enforcing them "as documented" was impossible without inventing a per-factor sizing mechanism, because no consumer ever defined what they clamp, and inventing sizing behavior in the money path is exactly what the resolution rule forbids. The keys are gone from config.hpp, config.cpp, and both yaml files, an older yaml still carrying them loads fine, and a test pins that they stay removed. The real controls, now stated where the caps were claimed: the ensemble weights (whale_signal_weight 0.10 shipped), the discovery advisory adjustment bound (+/- 0.10), and the enforced `default_position_scale_cap` on every sizing path.
- ~~**controls.json carries TWO bare `"whale"` keys and the C++ layer toggle works only by emission order.**~~ **CLEARED 2026-07-18.** The engine no longer reads a bare layer name. It reads `layer_whale_enabled`, `layer_council_enabled`, `layer_dnn_advisory_enabled`, and `layer_adaptive_enabled`, none of which any other key contains as a substring, so the reader no longer depends on emission order. The nested `layers` and `layer_sources` maps are unchanged (Python and the GUI read them by path). A test scrapes the needles out of the `core/*` readers and asserts each appears EXACTLY once in a written controls.json, which catches the whole class rather than this instance. The proposed fix in the original flag (order the writer's blocks and document it) was rejected: it would have left the hazard live and load-bearing on a comment.

Found 2026-07-18 by the abstention session. The two pipeline flags CLEARED 2026-07-18 by the pipeline session:

- ~~**train_real records challengers as registry metadata with NO servable artifact.**~~ **CLEARED 2026-07-18.** The trainer saves a signed artifact with its normalizer, promotion refuses a challenger without one before touching the registry, and a successful promote installs the artifact as the serving champion.
- ~~**The real trainer's feature set differs from the serving builder.**~~ **CLEARED 2026-07-18.** One canonical builder (`bars-v2`) shared by identity, signature verified at serve time, constant defaults removed.
- **The `min_directional_votes: 1` setting is an EVALUATION-PERIOD choice.** Revisit with outcome data once single-voter verdicts accumulate. 2 is the conservative setting.

Found 2026-07-18 by the FABLE diagnostic pass. ADDRESSED 2026-07-18 by the provenance session, residue noted:

- ~~**916 synthetic walk bars sit in the real bars table and will poison the next warm start.**~~ **QUARANTINED 2026-07-18.** 892 marked `source='synthetic'` by `scripts/quarantine_synthetic_bars_20260717.py` and both trades marked `bar_source='synthetic'`. The remaining 24 walk bars landed on aligned `:00` stamps and stay `unknown` deliberately: the next backfill upserts those exact timestamps with real data and `backfill` provenance, and `unknown` never trades and never reads as real meanwhile. The real-fill gates exclude the two contaminated fills.
- ~~**The bridge process failure class is DETECTABLE and now INSTRUMENTED. The root cause stays open.**~~ **ROOT CAUSE FOUND, MEASURED, AND FIXED 2026-07-19.** The 2026-07-18 instrumentation did its job on the next occurrence: bridge pid 587930 held 1023 of 1024 fds and 1018 of them were open handles to `.keystore/credentials.sqlite`, leaking at ~30 fds/hour with socket_count flat at 1. The credential resolver opened a NEW sqlite connection per lookup and `with conn:` never closes (transaction scope only); on Python 3.14 each connection is cycle garbage refcounting never frees. Fixed: one long-lived locked keystore connection per process (`account_manager/credentials.py`), every other unclosed SQLite open in the repo closed (`ui/db.py`, `api_server/store.py`, `ops/demo.py`), fd-measuring tests that failed pre-fix (+397 to +1000 fds per 1000 resolutions) and pass post-fix (+0), and a guard test pinning the `with sqlite3.connect(` idiom out of runtime code. The fd alarm now keys off the healthy baseline (auto min(256, half the soft limit), was 80 percent of the limit, which still read ok at 410 fds while the feed had already substituted), a NEW `fd_trend` capability check reads a rising fd FLOOR as degraded within about an hour of onset, and the bridge raises its soft fd limit to 65536 at startup as documented defence in depth. The watchdog now actually remediates: see the session entry.

Found 2026-07-18 by the discovery diagnostic session (OPUS pass). Reported, deliberately NOT fixed (that session was diagnosis only). Ordered by severity and dependency:

- **The discovery conviction floor sits above what the pipeline produces, and it blocks the whole layer.** `discovery/evaluate.py` passes `council_min_confidence` (0.60, the TRADING gate's threshold) as `conviction_floor`. Seven real council verdicts across two days measure 0.5265 to 0.5938. None cleared it. Every verdict is therefore `avoid`, `run.py` skips `avoid` when building the watchlist, and the watchlist has never gained a member. Nothing downstream of Stage C can work until this is decided. No dependencies.
- **The synthetic DNN champion is the deciding vote against every discovery candidate.** `dnn_bias` measured -0.31 to -0.34 on all 12 symbols ever evaluated and never once positive. ADA/USD had council confidence 0.6026, above the floor, and the advisory adjustment of -0.066 pushed it to 0.5366. An untrained advisory factor should not be able to veto. Settle the floor first or this only moves the failure.
- ~~**The discovery budget counts evaluator returns, not provider calls.**~~ **CLEARED 2026-07-18.** `evaluate_survivors` now charges one budget unit only when the evaluation CONTACTED a provider (`build_verdict` reports `provider_calls` from the council's scored `per_model`, empty on every short-circuit). A gate decline, risk pre-check, or market-hours skip costs zero and does not eat the per-pass ceiling. An evaluator that does not report is charged one, the conservative direction. Corrected accounting for 2026-07-17: passes 3 and 4 recorded 5 calls each with every candidate flat at conviction 0.0 and agreement 0, so the true provider spend that day was 2 of the recorded 12. Mutation-tested: reverting to count-on-return fails 3 tests.
- ~~**The watchdog's freshness probe cannot see a stale discovered symbol.**~~ **CLEARED 2026-07-18.** `feed_ok` now checks every tradeable symbol BY NAME: the profile-resolved static whitelist (`stack.whitelist()` now applies the active_quant overlay the engine applies) plus the active watchlist members when discovery is on. One stale symbol fails the check and is named in the result, the status line, and the notification. Equities are held to freshness only inside US regular hours (one cadence authority, `discovery.run.us_market_open`), crypto around the clock. The per-symbol provenance check is kept: fresh but non-real on the real path is unhealthy and named. Mutation-tested: any-fresh semantics (the old MAX(timestamp) behavior) fails 3 tests.
- ~~**A bare `mal_engine` writes warn events into the production database.**~~ **CLEARED 2026-07-18.** The `--help` fix is confirmed closed: all five `discovery_blocked` no-bridge warns are dated 2026-07-17 and predate it, zero since. The remaining stray path is closed structurally: without an explicit `--db`, `mal_engine` writes a scratch `mal_demo.db` (gitignored) and prints so, never `market_ai_lab.db`. Every real launcher (start script, GUI supervisor) passes `--db` explicitly, verified. Touching production now requires asking for it, pinned by subprocess tests both ways.
- ~~**UNEXPLAINED, do not fix until reproduced: "engine reads discovery ON but the Python funnel reads it OFF". ROOT CAUSE STILL OPEN, capture now AUTOMATED (2026-07-18).**~~ **EXPLAINED AND CLOSED 2026-07-19 by the keystore fd-leak root cause.** A bridge with no free fd cannot open controls.json. Every control-file reader swallows the read error and falls back to config, which ships discovery OFF, so the funnel (in the fd-exhausted bridge process) read OFF for 228 polls straight while the engine (its own process, healthy fd table) parsed the same file ON the whole time. That is exactly the documented unreadable-means-config direction doing its job in one process and not the other. The 2026-07-17 06:51:10 event remains attributed to the pre-ad586f7 flag-source bug; the 11:56:36 event that opened the 19-hour outage is this fd exhaustion. Closed by the keystore fix, the baseline-keyed fd alarm, the fd_trend check, and the watchdog now restarting a degraded bridge. The evidence capture stays wired: a future mismatch still records the control file bytes, pid, start time, and fd count.

Found 2026-07-17 while fixing other things, and deliberately NOT fixed, so they are recorded here rather than lost:

- ~~**`mal_engine --help` is not a help flag: it RUNS a 20-iteration demo against the real database.**~~ **CLEARED 2026-07-18** by the pipeline session: `--help` and `-h` print usage and exit before any config load, schema init, or DB open, subprocess-tested.
- ~~**`api_server.controls.real_fills()` contradicts its own docstring.**~~ **CLEARED 2026-07-18**: it now calls `ml_factor.real_dataset.count_closed_trades` directly, so the write gate and read gate are one counter by construction, and an unreadable DB reads 0 (gate shut). The read gate remains the authority.
- ~~**`adaptive/run.py::_db_path` resolves the database cwd-relative.**~~ **CLEARED 2026-07-18**, repo-anchored along with `ops/watchdog._db_path`, the `discovery/run.py` defaults, and the quarantine CLI default, pinned by a foreign-cwd test.

Found 2026-07-19 by the remediation-loop session, deliberately NOT fixed here (reported per the prompt):

- ~~**Discovery can surface symbols the execution venue cannot serve, and MANA/USD and RUNE/USD are live examples.**~~ **CLEARED 2026-07-20 by the invariant session.** Discovery now backfills BEFORE the watchlist add and refuses a candidate the tradeable predicate cannot confirm (journalled applied=0). The four live dead entries (MANA/USD, RUNE/USD, and ZEC/USD, APT/USD found by this session's probe) were pruned through the event-sourced path by `scripts/prune_unserviceable_20260720.py`, and the tradeable invariant contains any that ever slip in: symbol_unavailable, per-symbol, never a stack stop. Original flag text follows for the record. Discovery ranks the curated universe through Finnhub (Binance pairs), onboarding backfills through Alpaca, and Alpaca serves no data for those two pairs (probed live: 565 AAVE/USD bars, 0 MANA/USD, 0 RUNE/USD from one request). Both sit `active` on the watchlist with zero bars. The engine merges them, gets no Alpaca quotes, and walks them synthetically on the real path (engine `feed_substitution` events at 2026-07-20 06:03:44Z and 06:05:15Z name exactly these two). Consequence after this session's fix: once the stack stays up past the grace, their in-window synthetic bars read as a GENUINE substitution, one restart fires, the condition recurs, and the watchdog holds with the symbols named. No capital risk (the provenance gate refuses entries on non-real bars and they never warm on real data), but the hold notification will persist until an operator acts. The clean fixes are an operator watchlist removal through the event-sourced path (never a raw DELETE, the SOL/USD lesson) or a venue-capability check in discovery onboarding. Not done here: this session's scope was the watchdog, and deleting production watchlist rows overnight repeats the exact mistake the SOL/USD postmortem documented.

Still open (not defects — known limits):

- Advisory factor scores run through real Python services only with `--bridge`; the default path uses deterministic C++ mocks.
- Whale live-fetch parsers are verified against one recorded SEC 13F payload only; the other feeds' assumed shapes are still unverified against live responses (and live is off by default behind `WHALE_LIVE_ENABLED` / `SEC_EDGAR_ENABLED`).
- Live-approval workflow not wired end to end (`try_enable_live` never called). Safe, but incomplete.
- Real LLM providers VERIFIED live 2026-07-12 (RESOLVED): all four LLM paths plus Alpaca paper (data + order-auth) return working via scripts/verify_live_integrations.sh. The earlier OpenAI HTTP 400 and Gemini HTTP 404 (2026-07-11) were NOT auth failures. OpenAI: `gpt-5.5` is reachable, the request shape was wrong (needs `max_completion_tokens` not `max_tokens`, and only the default temperature) — fixed in the provider and health check. Gemini: `gemini-3.1-pro` does not exist for the key; the reachable id is `gemini-3.1-pro-preview` — updated in config, CLAUDE.md, and everywhere the string appeared. Gemini 3.1 Pro is thinking-only and truncated at the old 400-token cap, so `council_max_tokens` was raised to 2048 (a ceiling, not spend) so the council gets a parseable verdict. A non-fatal startup check now warns if a configured model is unreachable. `rl_advisory` untrained (real-fill gate unmet); `dnn_advisory` still shipping the synthetic champion until enough real labelled samples exist.
- Real-path mock substitution CLOSED by strict mode (2026-07-13): on feed_mode alpaca_paper the engine refuses to start when a layer set on-real has no reachable real service, printing what is missing, instead of silently using a mock. A per-level source toggle (off / on-mock / on-real) lets the operator drop any single layer to mock mid-run without stopping the loop. SEC EDGAR verified against live 13F AND live Form 4 (both HTTP 200, fixtures recorded), and the full-activation defaults (real council + SEC EDGAR + whale live) plus the one-command start (scripts/start_paper_trading.sh) landed. Warm-start ADDRESSED (2026-07-13): the native strategy now warms from a real-bar backfill before it evaluates (scripts/start_paper_trading.sh backfills + verifies warm first, the engine seeds indicators from the bars table and gates entries until warm), so a live run starts against warm indicators instead of a cold start. Feed mode and clock mode are now runtime-switchable from the GUI with an open-position safety rule that never orphans a position. Controls consumption CLOSED 2026-07-13: the engine now consumes model toggles, regime pins, and budget each iteration, and promote/rollback execute through the audited registry endpoint. Still open: a real COUNCIL verdict end to end also needs a fresh EMA20/100 crossover or Bollinger reentry to actually occur in the live window (warm indicators are necessary but the entry event still has to happen).

New flags from the feed-work session (2026-07-05, `369b6a6`):

- **`rule_based` now carries the native signal's conviction on ALL native runs, not just synthetic** — a real decision-path change (previously the gate's confidence/edge came only from advisory factors). It is what makes fills flow, but with a real LLM council + trained `dnn_advisory` the native setup then contributes to BOTH direction/sizing AND the gate's confidence/edge (mild double-counting). Confirm this is intended before any live-adjacent use. GATED 2026-07-10: the engine.native_conviction_feeds_gate config flag (default true, preserves this behavior) now controls it. Set it false to have the gate confidence and edge come from the advisory factors alone while the native setup still drives direction and sizing. Composition only (signal_engine::compose_gate_verdict), RiskGate untouched.
- **"≥30 closed trades per factor" (Task-5 wording) is not met for momentum:** the real tuner gate is ≥30 TOTAL closed (`learning/adapt_gate.hpp`), and EMA20/100 crossovers are rare (3 fired in the verified run). Total-closed is what the tuner uses; the per-factor phrasing is not satisfied.
- **Cut B (equities market-hours council skip) simulated-clock consistency FIXED 2026-07-13.** The skip now keys off the simulated timestamp under `clock_mode: simulated` (via `now_epoch`) and real wall-clock under `clock_mode: real`.
- **Replay council-cooldown timing FIXED 2026-07-13.** Cooldown spacing now keys off the true historical bar `ts` (`util::iso8601_to_epoch`), matching the per-day trade cap, instead of a synthetic sequential epoch.

## Session Log

### 2026-07-21 (Opus 4.8) — DIAGNOSTIC: the ATR band is symmetric but mean reversion is not, and it currently rejects everything on the low side

- **The design smell is symmetry.** The band rejects a cross-back setup whenever ATR is more than 1 SD from its 100-bar mean in either direction, but a LOW-side rejection (quiet tape) is defensibly skipping dead tape while a HIGH-side rejection refuses the volatility a snapback needs. Over stored history the band rejects 46 percent of cross-back setups (923 of 1,707 pass), and of the 784 rejections 58 percent are low-side and 42 percent high-side.
- **Right now it rejects everything low-side.** In the current calm market, ATR is below its mean on 7 of 11 symbols and ABOVE it on none, so every current band rejection is a quiet-tape rejection. That is why SOL/USD (z = -1.61) was one gate from an entry and blocked only by the band on 2026-07-21.
- **Outcomes unmeasurable**, same as the volume filter: the band decision and direction are not recorded per trade, and only 4 real-path native exits exist, so win rate / return / MAE split by rejection direction cannot be produced without recorded state or a backtest.
- **Width sweep:** 1.0 removes 46 percent of setups, 1.5 removes 24 percent, 2.0 removes 10 percent, unbounded is the baseline. Crypto and equities both pass 54 percent at 1.0, so a per-class split is not justified; the count asymmetry (crypto 1,469 vs equity 238) is a reachability/budget issue, not a band-width one.
- **Recommendation, applied to NOTHING:** keep 1.0 for now (no outcome data to change it on), but the most promising change is a ONE-SIDED band that keeps the low-tail dead-tape filter and stops refusing high-side volatility. The prerequisite is recording per-entry band state or running a backtest, the same prerequisite the volume filter needs.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, any threshold. Live trading stays off.

### 2026-07-21 (Opus 4.8) — DIAGNOSTIC: the volume filter is inert on live data, because after the fabrication fix the live path carries no volume

- **The filter cannot be measured on live data, because there is none.** Every post-fix live bar carries volume 0, which the corrected filter treats as unknown and passes, so the live kill rate is exactly 0 percent, down from the fabricated 32 to 84 percent. The filter is INERT on every live decision. Post-fix live real-volume bars: ~0 (2,754 absent vs 40 in the restart transition window).
- **The prior kill rate was noise.** The 2026-07-21 "32 to 84 percent" was computed on the fabricated uniform-random series, a coin flip. The filter never killed 32 to 84 percent of real setups; it decided live entries on a random number, and now decides nothing.
- **Only backfill has real volume, asymmetric by data availability.** Equity backfill is 100 percent present, crypto backfill only 35 percent (quiet crypto minutes report zero trades). On backfill the filter removes 28 percent of cross-back setups overall, but 49 to 73 percent on equities and 1 to 54 percent on crypto purely because zero-volume bars auto-pass. It is filtering data availability, not volatility.
- **Outcomes unmeasurable:** the filter decision is not recorded per trade and only 4 real-path native exits exist (the 242 closed fills are overwhelmingly offline synthetic). Attribution is impossible without a backtest, which a diagnostic will not run.
- **Cross-back is the selectivity that survives.** Already ON, it halves the raw oversold setups (3,012 to 1,728) with no volume input, so it works on the live path where volume is absent. Removing the volume filter would not leave the strategy unguarded.
- **Recommendation, applied to NOTHING:** keep the filter as is, do not change `vol_multiple` (no live data to tune against), and adopt Alpaca's latest-BAR endpoints (the deferred feed change from the fabrication fix) so live bars carry real volume. Only then can the filter be measured on the data it actually gates. It is a dormant guard starved of data, not a broken one.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, any threshold. Live trading stays off.

### 2026-07-21 (Opus 4.8) — Council cost estimate calibrated to measured spend: every ceiling tightens 29 percent

- **Reconfirmed and applied.** A full council round measures $0.05618 over 41 persisted rounds (up from the audit's 15), so `council_est_cost_per_call_usd` was set from 0.04 to 0.056 in both the council base and the active_quant overlay, plus the C++ and Python fallback defaults so no stale copy drifts. No budget, ceiling, or threshold was raised: the tightening is the point.
- **Effect:** every council spend ceiling now enforces against real spend. The $5/day ceiling permits 89 calls instead of 125, the $100/month 1,786 instead of 2,500, a uniform 29 percent tightening.
- **Consumers:** six sites read it (four C++ enforcement/display, two Python ceiling checks), all now resolving 0.056, and no independent hardcoded copy of 0.04 remains. Reported separately and left as found per the change scope: discovery Stage C and the research sleeve price the SAME council with their OWN estimates (`discovery_est_cost_per_call_usd` 0.04, understated the same way; `research_est_cost_per_call_usd` 0.08, now slightly over the measured value). Calibrating those is a flagged follow-up.
- **Projection comment corrected:** the worst-case combined monthly is now ~$87 (council + discovery) plus $14 research = ~$102, stated plainly as marginally OVER the $100 combined ceiling that then pauses both sleeves. Observed 72-hour production spend was $2.18, so the ceiling is a backstop.
- **Verified:** pytest 893 (from 892, +1 pin asserting the shipped estimate is 0.056 not 0.04), ctest 26/26 against the shipped config (23/26 with the operator's profile edit, the same three known failures, left as found). The C++ config test does not assert the estimate value, so the calibration passes cleanly under the shipped profile.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, any budget or ceiling. Live trading stays off.

### 2026-07-21 (Opus 4.8) — DIAGNOSTIC: the whale toggle events are a read failure, not a phantom writer, and the class is already fixed

- **The FILE never changed. The READ did.** The six 2026-07-17 `layer.whale True -> False` events read `old=True` after each write of False not because anything restored True, but because `api_server/controls.read_controls` falls back to `_defaults()` on any read error and the layer default is True. So a click whose read could not open the file reports old=True while the disk still says False. The 2026-07-18 search for a writer restoring True correctly found none, because the cause was never a writer.
- **Reproduced three ways** against a scratch control dir: a torn read (empty file) returns default True, an open failure (unreadable/fd-exhausted) returns default True, and a repeated-click loop with the reader failing reproduces `old=True new=False` with the disk staying False.
- **Same phenomenon as the 2026-07-19 discovery ON/OFF mismatch** (closed by the keystore fd-leak fix), in a different key with a different default. Both candidate triggers are already fixed: torn read by the atomic-write fix (07-17), fd exhaustion by the keystore fix (07-19). Not recurred since; no capture file exists.
- **Residual, unprovable not open:** which trigger fired at 08:39Z on 2026-07-17. Fd evidence for that hour was never captured (the instrumentation postdates it), and the events are 76 minutes after the atomic-write fix, so a torn write needs a stale pre-fix backend to explain it. The capture wired 2026-07-18 records fd count, start time, and file bytes, which would distinguish the triggers on any recurrence; it has not needed to fire. Open Flags entry updated accordingly.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, any control file (the reproduction used a scratch dir). Live trading stays off.

### 2026-07-21 (Opus 4.8) — DIAGNOSTIC: the fast tier cannot clear 0.65 by construction, because a benched dnn is averaged in as a confident zero

- **STRUCTURAL, and the defect is in the composition, not the floor.** On the fast tier `compose_gate_verdict` correctly excludes the un-run council (weight 0.57) from the confidence denominator, leaving rule_based (0.18), dnn_advisory (0.15), and whale (0.10). But dnn is BENCHED and returns confidence 0.0 (live-probed) while its 0.15 stays in the denominator, so the composed confidence is `(0.18*rb + 0.15*0 + 0.10*whale)/0.43`. With rule_based at its fast-tier cap (0.88) and live whale (0.518) that is 0.4888, and the recorded ETH fast block was 0.488. Even whale at an unreachable 1.0 gives 0.60. The ceiling is below 0.65 under every input.
- **The record agrees:** 27 of 27 fast-tier candidates blocked, max ever 0.535. The council tier is NOT structural (max blocked 0.644, and six entries fired including a council-tier momentum at strength 0.7 that cleared with a strong_buy council).
- **Not the floor, not the tuner.** `rule_based_weight_floor` (0.35) never triggers here (rb share is already 0.419), and triggering would make it worse. The tuner is not overriding (weight_overrides empty, param_history stale from 2026-07-02), so the composition is stable.
- **The contained fix, applied to NOTHING:** exclude a benched dnn from the confidence and edge denominator exactly as the un-run council already is (the 2026-07-15 `council_ran` mechanism). Projected: fast-tier confidence rises from 0.4888 to 0.7506 and clears, with no threshold change. The 0.65 floor is correct and stays; the bug is that a fast-tier entry's confidence is artificially low because a zero is averaged in.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, min_confidence_default, any behavior. Live trading stays off.

### 2026-07-21 (Opus 4.8) — DIAGNOSTIC: open positions are stranded across restarts, the engine never rehydrates exit state

- **DEFECT, not a held position.** The ETH/USD long opened 2026-07-17 at 2030.69 has no exit fired in six days because the engine cannot see it. Exit state lives only in the in-memory `open_positions_` map (`core/engine.hpp:427`), populated ONLY at entry (`core/engine.cpp:1189`, `:2754`). The constructor never reads the `positions` table, and the table has no stop or target column to read: its exits survive only in the `trade_entry` event (stop 1993.66, target 2086.23, factor momentum). A fresh process starts with an empty map, `handle_bar_close`'s `open_positions_.find(key)` misses on every closed bar, and the position is invisible: not evaluated, not exited, silent because there is no record to log about.
- **The stop is breached, not near.** ETH price is 1881.8 against a native stop of 1993.66, 5.6 percent past it, and the momentum time-stop (24 bars, 2 hours) is overdue by roughly seventy times its window. The RSI-2 indicator exit does not apply (that is reversion-only; this is momentum). Any one of the stop, target, or time-stop would have closed it days ago if it were being managed.
- **The exit path RUNS.** `on_closed_bar` (`:617`) calls `handle_bar_close` (`:659`) on every real closed bar; the `find` at `:806` misses because the map is empty after a restart. So "runs and declines" and "does not run" are indistinguishable here, and the truth is the first: it runs and sees nothing.
- **Five positions stranded by the same cause:** ETH/USD, SPY (2026-07-14), BTC-USD (legacy dash form the feed cannot even re-quote), and PRES-2028-YES and FED-CUT-Q3, both prediction-market names that predate the Polymarket removal and whose venue no longer exists, so they are permanently unmanageable phantom risk.
- **Contained fix, described and applied to NOTHING:** persist stop/target/time_stop/factor/bars_held on the position, then rehydrate `open_positions_` at construction from every `qty != 0` row so the first `handle_bar_close` after a restart manages it. This is a capital-management defect that must not exist before live: a stranded position with a breached stop is an uncapped loss.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, any behavior. Live trading stays off.

### 2026-07-21 (Opus 4.8) — The live volume fabrication is removed: the real path reports absence instead of inventing a number

- **It reached the bars table, which is the part the previous note missed.** `AlpacaFeed::poll` drew `ms.volume` from a uniform [1000, 10000] per tick, `BarAggregator` SUMMED those draws, and `Engine::on_closed_bar` PERSISTED the result as a `real_feed` row. So every live bar the engine has ever closed carries an invented volume: 3,465 rows, $1.6 trillion of fictional dollar volume, BTC/USD averaging 55,906 against 0.0056 for its own backfill bars, and the same distribution on BTC/USD, SPY, and AAPL. The full nine-step consumer trace with file and line is in RETURN.md.
- **On the decision path it was a coin flip.** Applying the RSI-2 gate's own comparison to every stored live bar: 1,591 passes against 1,644 failures over 3,235 comparisons, 49.2 percent, exactly what a uniform draw compared against the mean of twenty draws of the same distribution produces. NOT establishable from the record, and said so rather than estimated: how many of those rejections were bars where the trend filter, cross-back, and ATR band had all already passed, because a filter rejection writes no event.
- **What Alpaca actually exposes, probed live.** The latest-TRADE endpoints the live path uses return a single trade SIZE (`s: 200` for SPY, `s: 0.0029` for BTC/USD), not a bar aggregate, and the bridge forwards price only. Summing per-poll trade sizes would double count across polls and miss everything between them, so no honest bar volume exists on this path. The latest-BAR endpoints DO carry a real `v` (SPY `v: 1196`) and are the honest source, deferred as a feed change rather than folded into a correctness fix. The backfill path already stores real per-bar volume, which is why the historical analysis was sound while the live series was not.
- **The fix: absent is reported as absent.** `ms.volume = 0.0` means no volume reported, and both volume filters test `volume > 0` first so an unmeasured bar does not gate rather than reading as below-average. A genuine zero-volume bar is treated identically and correctly. NO threshold changed: `vol_multiple` 1.0 and `vol_lookback` 20 are untouched, as are MockFeed and the offline modes.
- **Guards, both mutation-killed.** A lexical scan of the `AlpacaFeed::poll` body that reads CODE LINES ONLY (the first version was satisfied by my own comment quoting the generator it replaced, which is the kind of guard that proves nothing), a `BarAggregator` assertion that volume-less ticks close a volume-less bar, and a strategy assertion holding both directions (below-average still gates, absent does not). Restoring the fabrication fails the lexical half; reverting the filter fails the strategy half.
- **Verified:** pytest 892 unchanged, ctest 26/26 against the shipped config (23/26 with the operator's profile edit, the same three known failures), and the offline synthetic runs behaviorally IDENTICAL to the pre-fix baseline in both profiles (active_quant 6/2/35, swing 108/204/1222).
- **Found on the same lines and deliberately not fixed:** `ms.spread` and `ms.order_book_imbalance` are also uniform draws on the real path. **Residue reported, not migrated:** the 3,465 contaminated rows no longer reach the decision path but still feed `discovery/universe.py`'s dollar-volume ranking, where 38.9 percent of recent crypto input is fabricated.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, any threshold. Live trading stays off.

### 2026-07-21 (Opus 4.8) — Run record and one correction to the entries below

- **The 08:01Z stack ran 6 hours 27 minutes** (760 iterations, 2 trades, 5 entry candidates) and stopped cleanly at 14:28:55Z on a graceful `POST /engine/stop`. **WHO SENT IT IS NOT ESTABLISHED.** No `watchdog_restart`, no `engine_supervisor` event, no `.run/watchdog_state.json`, and no kill trip. The watchdog is the only automated caller of that endpoint, so it is the likely initiator, but nothing in the record proves it and it is recorded here as unexplained rather than attributed. The stack was restarted afterwards and is running.
- **Correction to the universe entry below:** it says the stack was left running "with eleven verified symbols". On restart the universe resolves to TEN (8 verified core + 2 verified periphery). AAVE/USD left the watchlist between the two runs, which is the 48-hour staleness prune doing its job, not a regression. The core is unchanged at 8 of 8 serviceable.

### 2026-07-21 (Opus 4.8) — AUDIT: real spend is $2.18 per 72 hours, and every spend ceiling is 40 percent looser than it reads

- **Measured 72-hour spend: about $2.18 of production LLM cost** (38 discovery Stage C rounds plus 1 trading council round plus 39 gate calls), and about $0.78 more from the 2026-07-20 prompt-measurement harness, which is diagnostic rather than production. Adaptive interpretation spent NOTHING (the table is empty, the free filter dropped everything) and the research satellite spent NOTHING across 13 passes (every thesis reads "screened out: no catalyst" at conviction 0.0, so the free Finnhub screen refused before any council call).
- **Cost per full council round measured at $0.0560** (three providers plus the gate), from the persisted prompts and per-provider records. Opus is 71 percent of the bill on 33 percent of the calls, entirely because of its $75/1M output rate.
- **THE CALIBRATION DEFECT: `council_est_cost_per_call_usd` is 0.04 while the measured cost is 0.056, so every ceiling is 40 percent loose.** The $5/day ceiling permits 125 calls that really cost $7.00; the $100/month ceiling permits 2,500 calls that really cost $140. The config comment projecting "~$62/month worst case" is $87 at measured prices. Raising the estimate to the measured value TIGHTENS every ceiling and can only reduce spend. Not applied, this was an audit.
- **The gate-disabled rounds were never a misconfiguration.** `council_eval` ids 1-10 carry `reason: gate disabled` because they ARE the 2026-07-20 measurement harness: their timestamps (05:46Z, 06:00Z), symbols, and prompt versions match the re-runs recorded in RETURN.md exactly, and that harness disabled the gate deliberately so the comparison was between prompts. Ids 11-15 carry real Haiku reasons. The gate is ENABLED now at config, controls.json, and the resolved read. Skipping it saves nothing ($0.0003 a call) and costs the screening, at 187 times the gate's price per candidate it would have declined.
- **Projections:** $21.8/month at observed volume (22 percent of the ceiling), $87.4/month at the configured council caps, $101.8/month including the research budget, which is marginally over the $100 combined ceiling that then pauses both sleeves.
- **Waste found, in descending cost.** (1) Discovery EVALUATES BEFORE IT VERIFIES SERVICEABILITY: `funnel.run_pass` is line 253 of `discovery/run.py` and the backfill-plus-predicate check is line 287, so a full round can still be spent on a symbol the venue cannot serve (ZEC/USD and APT/USD each were). (2) Crypto consumes the entire shared 12-call daily budget before the equity session opens, every day: 07-21, 07-19, 07-18 and 07-17 all read crypto 12 / equity 0, and lifetime is 58 crypto calls against 2 equity. One asset class is structurally never evaluated by Stage C. (3) Seventeen of the twenty most recent passes paid for Stage B (12 Haiku calls each, about $0.06 total) to produce five survivors the exhausted budget could not evaluate. (4) Gemini errored on 14 of 15 recorded rounds, probably unbilled but costing latency and a third of the intended diversity; it has since recovered.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, any budget, any ceiling, any config. Live trading stays off.

### 2026-07-21 (Opus 4.8) — DIAGNOSTIC: the strategy layer DOES fire, the RiskGate confidence floor is where candidates die, and the live volume series is fabricated

- **The premise is falsified by the event log.** Between 13:10Z and 13:55Z the engine produced FIVE native entry candidates (three RSI-2 reversion on SOL/USD, UNI/USD, ETH/USD; two momentum on SPY and UNI/USD), executed one, and wrote a `council_skip` plus a `risk_block` for each of the other four. UNI/USD momentum entered at 3.700000 (strength 0.7, regime trending) and exited on target at 13:25:26Z for +0.5526 against a `real_feed` bar. The 07:00Z to 08:00Z window the prompt observed was about one hour, and the measured base rate predicts well under one candidate per hour, so zero was the expected reading of a window too short to say anything.
- **The bottleneck is not the strategy, it is the fast-tier confidence.** All four blocks read `confidence below min_confidence_default` at 0.509 (UNI), 0.488 (ETH), 0.427 (SOL), and 0.299 (SPY) against the 0.65 floor. Every one cleared the 0.02 edge floor and three of four cleared the agreement requirement. All four took the fast tier, so no council ran and nothing was spent.
- **The strategy is SELECTIVE, not unsatisfiable.** Over every stored real-provenance 5-min bar: RSI-2 reaches its entry threshold 213 to 1,169 times per symbol, the minimum RSI-2 observed is 0.00 on six of ten symbols, and the FULL RSI-2 conjunction fires 7 to 97 times per symbol (crypto about 32 days, equities about 50 sessions). With momentum the universe would produce roughly 12 signals per day. Right now SOL/USD is one gate from an entry (blocked only by the ATR band) and LDO/USD is armed at RSI-2 0.0.
- **Where the setups go:** of the 260 BTC/USD bars where the trend filter and cross-back both held, the ATR band kills 48 percent and the volume filter kills 80 percent, leaving 26. Across the universe the band kills 38 to 62 percent and volume kills 32 to 84 percent.
- **THE DEFECT THAT MATTERS: the live volume series is fabricated and it feeds a real entry gate.** `market_data/market_data.cpp:186` sets `ms.volume = 1000.0 + 9000.0 * next_uniform()` on every AlpacaFeed tick. Measured on BTC/USD: backfill bars average volume 0.0056, `real_feed` bars average 55,906, and the live figure is statistically identical across BTC/USD, SPY, and AAPL, which is a generator, not a market. The 2026-07-20 session recorded the fabrication and cut it out of the council prompt; the strategy's volume gate still consumes it. Consequences: the historical reachability counts (computed on real backfill volume) do not transfer to the live path, and on the live path the volume gate is noise rather than a filter.
- **Recommended, applied to nothing:** stop fabricating live volume first (a correctness fix that decides whether any tuning number means anything), then reconsider the volume filter against a cross-back trigger, then the 1.0 SD ATR band, then the equity RSI-2 entry of 5, and investigate the fast-tier confidence composition before touching any threshold. The 0.65 floor is a Level-1 value and must not be lowered.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, any threshold, any config, any behavior. Live trading stays off.

### 2026-07-21 (Opus 4.8) — Full system verification: 15 of 17 sections pass, the startup source label stops lying, and the strategy layer is measured as the real problem

- **`scripts/test_full_system.sh`: 15 PASS, 2 FAIL, 0 SKIP.** Zero skips means no optional path was hidden by a missing key: council live keys, Alpaca paper, whale, and Finnhub all ran for real. Both failures share one root cause and it is not a code defect, it is the operator's uncommitted `strategy.profile: active_quant` edit to the shipped config, proven by stashing that single line and watching 26/26 pass.
- **THE FINDING THAT MATTERS: the strategy layer is not producing signals under active_quant.** Two of the failing assertions are behavioral, not shipped-default assertions. `tuner_floor` ("keeps generating native entries past 100 closed trades") and `market_hours_entry` ("crafted equity entry executed IN US hours") both fail because no entry fires. Measured directly on the identical deterministic synthetic feed, 5,000 iterations: swing 108 trades, active_quant 6. Live agrees, 0 trades and 0 council calls across 11 warm symbols since the restart. The synthetic tape bounds reachability on that tape only, so the live-data version of the question is the next session's.
- **Two harness defects fixed, both about the suite lying rather than the system failing.** `sec_whale` asserted the PRESENCE of `whale_position_scale_cap: 0.35`, a key deliberately removed 2026-07-18 and whose ABSENCE a pytest guard already pins, so that section had been failing for three days on a contradiction between two guards. And `sec_build` (zero warnings) passed only on an already-built tree: a clean compile hit a pre-existing `-Wmissing-field-initializers` in `core/engine.cpp` and FAILED. The initializer is now field-by-field, behavior-identical, and a clean build emits zero warnings.
- **The startup source label stops lying.** The banner printed "source: mock" while the engine wrote `source=alpaca` into its own `continuous_start` event, because the "alpaca_paper forces alpaca" rule existed in two places and only the Engine applied it. Both now call one pure `market_data::resolve_source`, pinned by four assertions.
- **Claims against tests: 315 covered, 13 gaps CONFIRMED, 82 candidates UNVERIFIED.** The adversarial verification pass ran out of session after 15 of 97 verifiers, so the rest are candidates and are recorded as such rather than as findings. The confirmed gaps concentrate in the safety spine (the four-block live gate is never driven, eight of the RiskGate's 18 refusal sites have no test, the loss-triggered kill trip is untested end to end) and in active_quant strategy features (the crypto 2x ATR stop, the RSI-2 crypto/equity threshold split, the ATR volatility band). One shape explains nearly all of them: the pure predicates are well tested and the engine wiring that consumes them is not.
- **Also recorded:** all 9 configured integrations answer a real round trip, and Gemini has RECOVERED from the account quota exhaustion that made it unmeasurable through the 2026-07-20 sessions.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, promotion criteria, the RL fill gate, `min_directional_votes`. The operator's config edit was left as found and is not in the commit. Live trading stays off.

### 2026-07-21 (Opus 4.8) — The universe becomes a verified core plus a discovered periphery, resolved once, and the core is verified at startup instead of assumed

- **The premise was half wrong, and the probe says so.** SOL/USD's 8,884 bars were NOT fabricated walk bars. A live re-fetch matched the stored closes EXACTLY on 7,363 of 7,364 overlapping 5-min bars and 360 of 361 daily bars (the single mismatch on each is the last bar, partial when it was first fetched). They are real Alpaca history from before the provenance column existed, migrated to `unknown` by the 2026-07-18 migration that refuses to guess. Nothing was quarantined, because nothing was fabricated. The predicate refusing SOL was the invariant working: unprovable is not real.
- **Alpaca serves all eight core symbols.** Live per-symbol probe into a throwaway DB: BTC/USD 9,004 bars, ETH/USD 8,644, SOL/USD 8,834, SPY 4,087, QQQ 4,090, AAPL 4,060, MSFT 4,090, NVDA 4,090. Nothing was removed from the core. The problem was never serviceability, it was COVERAGE.
- **Two hardcoded four-name literals, in two languages, were the whole defect.** `market_data/alpaca_source.py` defaulted its backfill to `_WHITELIST_CRYPTO`/`_WHITELIST_EQUITY` (BTC, ETH, SPY, QQQ), and `core/engine.cpp` built `all_instruments_` from a matching four-instrument vector. Both were written when the swing whitelist was exactly those four. active_quant widened the core to eight and neither literal heard about it, so SOL/USD, AAPL, MSFT, and NVDA were declared, never backfilled, AND never polled. Both now derive from the declared core.
- **THE resolution point is `market_data/universe.py`.** Verified core union verified periphery, both held to `symbol_is_tradeable`. It replaces four independent constructions: `stack.whitelist` (config only), `controls.whitelist` (config only and with NO active_quant overlay, so it reported four symbols while the engine traded eight), `watchdog.tradeable_symbols` (config plus watchlist), and `operator.symbol_diagnostics` (config plus watchlist, ungated). The C++ half is `Engine::universe_report` over the same predicate against the same table.
- **The core is verified, not assumed.** `stack.verify_core` attempts the backfill for every core symbol and then asks the predicate, the same judgment discovery runs (`universe.judge_serviceable`, now one function both call). A backfill that cannot run verifies NOTHING and condemns nothing, so a missing credential can never read as an empty universe. The start script and the GUI supervisor both run it.
- **The warm check consults the predicate.** A symbol is warm only when it is TRADEABLE and holds enough bars. Counting bars alone is what let SOL/USD read WARM on unprovable data while the entry path had already ruled it out.
- **An empty or nearly empty universe is a LOUD condition** (`MIN_TRADEABLE_UNIVERSE = 2`, mirrored as `Engine::kMinTradeableUniverse`): a critical `universe_resolved` event, a starred startup-block line, a GUI banner, and one watchdog notification per hold window. It never remediates: a restart cannot make a venue serve a symbol, so it is treated like a kill trip, said out loud and never acted on.
- **Verified on the restarted stack:** every core symbol SERVICEABLE, `universe_resolved` reporting "11 of 11 declared", all eleven symbols closing `real_feed` bars at 08:00:09Z and warm (AAPL, MSFT, NVDA and SOL/USD for the first time), the periphery (AAVE/USD, LDO/USD, UNI/USD) joining correctly, zero `symbol_unavailable`, zero `feed_substitution`. The degraded path was rendered against an empty scratch DB: 0 tradeable, all eight named unserviceable, critical event, Trades=0.
- **Suites:** pytest 892 (from 866, +26 in tests/test_universe_resolution.py), ctest 26/26 against the shipped config, vitest 129 (from 127), tsc clean, production build green. Three mutations killed with file-copy rollback: the warm check reverted to counting bars, a consumer rebuilding the core-plus-periphery union, and the hardcoded instrument literal restored.
- **Caveat, pre-existing and NOT introduced here:** with the operator's uncommitted `strategy.profile: active_quant` edit in `config/default_config.yaml`, three C++ tests fail (`config`, `tuner_floor`, `market_hours_entry`) because they assert SHIPPED defaults while reading the file the operator edited. They pass 26/26 against the shipped config. This is the "an operator edits the shipped default because there is no runtime lever" pattern CONTEXT.md already names. The profile edit was left exactly as found: reverting it would silently change the running profile.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, promotion criteria, the RL fill gate, `min_directional_votes`. Live trading stays off. The stack is left RUNNING on the real path with eleven verified symbols.

### 2026-07-20 (Fable 5) — One council run per evaluation: the per-slot amplification is gone, spend cut threefold, verdict equivalent

- **Confirmed from code and a live instrumented evaluation:** each of the three llm slots used to trigger its OWN full council round through /score/llm, 9 provider calls and 3 gate calls per council-tier evaluation, each slot a separately sampled composite of the same council. Present since the initial commit. Whether it ever fired on the live trading path is unknown from the record (no per-provider persistence existed until today, and council-tier entries were rare to never). Discovery Stage C was always a single round per survivor, now pinned by test.
- **The round is hoisted:** new private `Engine::fetch_council_verdict`, the only /score/llm call site, runs once before the factor loop, and every llm slot carries the composed verdict. A failed round leaves the slots on their mocks as before. Per-provider transparency preserved by construction: the abstention-rule composition happens inside the one round and persists per provider in council_eval.
- **Live before and after on the same state:** old shape composites spanned conviction 0.600 to 0.630, all the same verdict, pure sampling noise. New shape composed 0.616 inside that band, directional 2 with 1 abstention, correct under the abstention rule. Equivalent in character, a third of the calls.
- **Budget audit:** the engine counted ONE call per evaluation while spending up to three rounds, so spend ceilings were enforced against a third of true trading-path spend. One counted call now equals one round, and no budget value changed. Per-slot mapping of raw provider verdicts into the ensemble slots was deliberately NOT done: it would change what feeds the agreement and confidence composition upstream of the RiskGate.
- pytest 866 (from 861), ctest 26/26 after rebuild, mutation (per-slot fetch reintroduced) killed.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, budgets, weights, min_directional_votes. Live trading stays off.

### 2026-07-20 (Fable 5) — Council token cost cut where it caches, evidence untouched, verdicts verified unchanged

- **The units legend moved into the cached system prefix and the per-call user message halved** (1,038-1,093 to 557-569 chars, 285 Anthropic tokens measured). Close precision trimmed to 5 significant digits. Anchors, threshold disclosure, and abstention framing byte-level intact, pinned by test. PROMPT_VERSION evidence-v2.1.
- **Caching, provider-proven from usage fields:** Anthropic ACTIVE (probe 1 cache_creation 1121, probe 2 cache_read 1121, input 285 per call, about 35 percent off Opus input per subsequent call inside a pass window). OpenAI inactive (prompt 1019 GPT tokens, below its 1,024 automatic minimum, padding would cost more than it saves). Gemini unverifiable (HTTP 429 account quota persists). Gate prefix far below Haiku's minimum, negligible.
- **Nothing load-bearing was cut.** Kept with reasons recorded: the closes list (it caught the frozen tape), position line, timestamps, day high/low, the Gemini 2,048 output cap. No field was demonstrably ignored in the 11 recorded rationales.
- **The acceptance test was the verdicts, and they did not regress:** same six symbols re-run under v2.1, Opus 4 of 5 directional (0 of 38 on the old prompt), composed band 0.556-0.60 with real agreement, every flat reasoned. Differences from the v2 run track the market moving between runs. Nothing reverted, no threshold changed.
- Cost at config prices: about $0.057 per full council round, about $21/month at the configured 12-calls-per-day worst case, less with cache hits, far less at observed volume.
- pytest 861 (from 858). NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, thresholds, any C++ code. Live trading stays off.

### 2026-07-20 (Fable 5) — The council gets real evidence, an anchored scale, a real long-term mode, and a replay record. Opus went directional for the first time.

- **The diagnostic's recommendations are implemented and measured.** New allowlist renderer (`llm_consensus/evidence.py`): a field with no real measurement is OMITTED, never zeroed, and the fabricated fields (random order_book_imbalance, hash-constant catalyst_score) never reach a model. Real evidence added from the shared DB, every line with units: last 12 real-provenance closes, returns over 12/48/288 bars, volume only from backfill bars (live bars aggregate fabricated tick volume, reported), the engine's regime read (label, ADX, realized vol, active factor), and position state. Prompt grew from ~235-270 to ~870-1,000 tokens per provider call.
- **The confidence scale is anchored (0.50 coin flip through 1.00), the acting threshold is disclosed with an anti-gaming instruction, and flat is framed as a deliberate, recorded abstention.** Reasoning comes BEFORE the verdict in the schema. The long-term mode now actually reaches the prompt and asks a different question (multi-week thesis with target_view, horizon_weeks, invalidation, fed by real fundamentals and catalysts), with model-stated levels recorded as reasoning, never executed.
- **Every scored council round now persists per provider** (direction, conviction, abstention, rationale, extras) with the full state and the exact prompts, in new Python-owned `council_eval` tables, keyed for replay under changed templates. The A/B harness exists.
- **Measured live (2026-07-21T05:46Z, 6 symbols): claude-opus-4-8 went directional 3 of 5 after holding 38 of 38 historically, citing the new evidence. Two composed verdicts hit the 0.60 floor from the council alone with agreement 2, never seen before. The two flats carry written reasons (frozen last-hour closes). Gemini unmeasurable: HTTP 429 account quota, error-flat, not hold.** No threshold changed.
- pytest 858 (from 836). Two mutations killed (padded zeros, mode split). The tradeable invariant guard caught and correctly forced the provenance query into market_data.tradeable.
- Open flags: Gemini quota exhausted at the account level (remeasure when it returns), whale endpoint consumes the hash-constant catalyst as market_bias fallback (found, not fixed, out of scope).
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, promotion criteria, the RL fill gate, min_directional_votes, any C++ code. Live trading stays off.

### 2026-07-20 (Fable 5) — DIAGNOSTIC: the council prompts and the context provided, findings only, nothing changed

- **The question: does the prompt itself produce the hedging pattern (directional verdicts 0.49 to 0.59, everything at or above 0.60 flat)? Answer: yes, plausibly, on four evidenced mechanisms.** Full verbatim prompts, context inventory, token counts, assessment, and recommendations are in RETURN.md. No code, config, prompt, or threshold changed. Three free read-only Finnhub calls, zero LLM calls.
- **One system prompt serves everything.** All three providers, the trading mode, the discovery mode, and the long-term research mode share one SYSTEM_PROMPT and one seven-field user message. `mode` and `horizon` are set by callers and DROPPED by build_user_prompt: the research prompt is byte-identical to the short-term prompt, proven live. The long-term question is never actually asked.
- **The council receives seven numbers and two of them are fabricated or padded.** Discovery: order_book_imbalance always 0.0 (no order book on the free tier), catalyst_score 0.0 for every crypto candidate, both rendered as measurements. Trading path: imbalance is uniform random in [-1,1] every tick even on the real feed, catalyst is a per-symbol hash constant (MockCatalystProvider is the only provider wired). No history, no volume, no regime, no indicators, no scale definitions, no threshold disclosure. Whole prompt about 235 to 270 tokens.
- **The zeros-read-as-flat failure was already measured and fixed at Stage B, never at Stage C.** discovery/gate.py's own docstring records the measurement (12 of 12 rejected, a +14 percent synthetic move called "flat, rangebound") and renders only fields that exist. The council still uses the padding renderer.
- **Per-provider asymmetry on identical prompts, all 38 labelled reads:** gpt-5.5 directional 27 of 38, claude-opus-4-8 hold 38 of 38, gemini-3.1-pro-preview hold 36 of 38. "Directional and unconvinced" is GPT at 0.49 to 0.63, "confident and flat" is Opus and Gemini. The conviction band held stable across three days and fifteen-plus symbols while markets varied, pointing at the input, not the markets. Post-abstention-fix, the council alone has still never cleared 0.60: both historical non-avoid verdicts crossed only on the whale's bounded +0.05.
- **Recommendations recorded, not applied, ordered by expected impact:** render only fields that exist (extend the Stage-B fix), add real evidence (bars, regime, native signal), anchor the confidence scale and disclose the floor with flat framed as legitimate abstention, fix field semantics and state the mode (return_5 carries the DAY move), evidence-before-verdict field order, persist the Stage-C state and per-provider rationale (prerequisite for any replay A/B, today only aggregates persist).
- **Also read from code, flagged for a future session, out of scope here:** one council-tier trading evaluation calls /score/llm once per llm slot and each call runs the full council, nine provider calls and three gate calls per evaluation, each slot carrying a separately sampled composite of the same council.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, prompts, thresholds, behavior of any kind. Live trading stays off.

### 2026-07-20 (Fable 5) — The operator experience: live symbol-grouped reasoning, council decision records, funnel narratives, promoted diagnostics

- **The interface is now built around the engine's written reasoning, not price charts.** New front door `/` (Operator) answers, in order: what is it doing right now (live activity grouped by symbol), how is it performing (equity, day change, win rate, drawdown), what is it about to do (positions with the engine's own logged ATR stop and target, watchlist), is it healthy (engine, bridge, kill switch, unavailable symbols). Blocks are first-class: each refusal renders with the numbers it was judged on against their floors.
- **Live activity, grouped by symbol, never dropping an event.** New `GET /activity?since_id=` (events with ids and parsed payloads, ascending) and an id-anchored `events_delta` on the existing WebSocket every 1.5s (was a 15-event snapshot that silently missed bursts). The client accumulates by id and repairs any reconnect gap over REST. Collapsed rows summarize ("blocked 12x on confidence below min_confidence_default", "trade entry (momentum) at 09:14"); expanding shows the full stream. Stack-level events live in a System row. Pinned by a backend WS test that writes an event between ticks and asserts delivery, and frontend grouping tests at 600-event volume.
- **Council verdicts as decision records.** New `GET /council/decisions`: each council-tier evaluation event joined (by shared timestamp) with the per-provider model_outputs recorded at the same moment, plus the floors and the DNN bench state. The card shows each provider's direction, conviction, edge, and weight, marks abstentions (hold at zero confidence), composes directional-vs-abstained against the floor, and states exactly which check a failed verdict failed and by how much ("confidence 0.31 vs floor 0.65, short 0.34 · agreement 1 of 2 required"). The DNN's benched state is labeled inline wherever it contributes zero. Known limit, stated: per-provider rationale TEXT is not persisted for the trading council (mock path writes none), so the record shows the numbers; discovery candidates keep their written rationale on the Discovery page.
- **Funnel passes read as narratives.** The Discovery page now opens each pass with one sentence ("Started with 50, screened to 12 for free, gate passed 5, council evaluated 2 at 2 paid calls") above the existing stage bars, drops-with-reasons, cost, and budget lines.
- **Diagnostics promoted from the terminal.** New `GET /diagnostics/symbols` (tradeable/unavailable via THE predicate, newest-bar provenance, last real bar, warm/cold from the engine's own warm_state events) and `GET /diagnostics/watchdog` (the watchdog state file, including a live HOLD, plus the feed-story events). The page shows bridge fd count, alarm threshold, and degraded checks, and gives every condition one plain line of copy: symbol_unavailable is contained and never stops the stack, feed_substitution is the emergency and does.
- **Controls clarified, not widened.** Each layer toggle states in one line what turning it off does; a weight change previews its normalized effect before the confirm posts to the validated endpoint (pinned: no POST until confirm); promotion, rollback, and RL enable stay on their existing gated surfaces; Level 1 stays read-only. No new write path anywhere: the six new endpoints are GET-only (POST returns 405, pinned).
- **Also new:** `GET /bars/{symbol}` (recent bars plus session change, feeds the quiet inline sparklines) and `GET /positions/exits` (open positions joined with the trade_entry payload's stop/target, the engine's own numbers, never recomputed).
- **Suites:** pytest 836 (from 829: +7 tests/test_api_operator.py, including GET-only, loopback bind, no key values, and the WS never-drop contract), vitest 127 (from 116: +10 operator component tests, +1 controls endpoint/preview test), tsc clean, production build green.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, promotion criteria, the RL fill gate, trading behavior of any kind. Live trading stays off.

### 2026-07-20 (Fable 5) — Untradeable-without-real-bars becomes ONE system-wide invariant: no fabrication on the real path, symbol_unavailable split from feed_substitution, watchdog stop authority scoped, discovery verifies serviceability

- **The live failure, closed at the class level.** Six symbols received real bars on time, but MANA/USD and RUNE/USD (Alpaca serves neither, ever) got fabricated synthetic walk bars at the same timestamps, the stack-level substitution condition read them as a live substitution after the grace, and the watchdog stopped the whole stack. Two unserviceable symbols killed a run where six were trading correctly. The fix is ONE invariant, not per-site patches: on the real path a symbol with no real bar history is NOT TRADEABLE. It is not evaluated, no bar is ever fabricated for it, and it never contributes to a stack-level alarm.
- **THE predicate, one per language, same definition, drift-pinned.** `symbol_is_tradeable` = at least one bar with source real_feed or backfill, any timeframe. C++: `Engine::symbol_is_tradeable` over new `Storage::has_real_bars` (cache flipped by the first live real tick, refreshed at onboarding). Python: `market_data/tradeable.py` (the watchdog's old private `_REAL_SOURCES` copy is deleted). Consumers, all calling the predicate: the engine entry path, `check_feed_substitution`, per-poll availability events, discovery onboarding, watchdog `feed_ok`/`check_health`. Guard tests pin the C++ and Python source sets equal, pin every consumer lexically, and refuse ad-hoc `source IN` provenance queries in any other runtime module.
- **The real path never fabricates.** `AlpacaFeed::poll`'s walk fallback is REMOVED, both shapes (per-symbol missing quote and bridge-down whole-poll). No quote means no tick, logged once per symbol, once per dead bridge. Every tick the feed emits is a real venue quote. MockFeed and the synthetic/replay offline modes are unchanged by design. Proven live at startup: "bridge unavailable; yielding NO ticks" while the bridge booted, zero synthetic bars written.
- **Two conditions that never share an alarm.** `symbol_unavailable` (never served: contained, per-symbol, once-per-transition engine event, prune-worthy, never remediation) is now distinct from `feed_substitution` (served before, non-real now: the emergency, remediation unchanged). The watchdog classifies against the predicate BEFORE any freshness or provenance judgment, so an unavailable symbol cannot reach `non_real_symbols` by construction; `onboarding_incomplete` is renamed/subsumed by `unavailable_symbols` / `feed_symbol_unavailable`.
- **Stop authority scoped: `any_tradeable_serving`.** If any tradeable symbol is receiving real bars on time (fresh AND real), the feed is not broken: staleness elsewhere is named and contained (`stale contained` in the status line), and no number of unserviceable symbols warrants a stack stop. A dead feed (tradeable symbols stale, nothing serving) still remediates, and a live substitution OUTRANKS the serving predicate, so the emergency path is not weakened. Mutations: dropping the serving scope fails 3 tests; letting serving veto substitution fails 4.
- **Discovery verifies serviceability before onboarding.** THE CAUSE MANA/RUNE PASSED: `run_once` added Stage-C survivors to the watchlist BEFORE backfilling, and `onboard()`'s `no_bars` result was computed and never acted on, so a zero-bar symbol stayed active. Now the backfill runs FIRST and a candidate is added only if the tradeable predicate confirms real bars landed; a refusal is journalled (`watchlist_event` applied=0) and logged. A backfill that cannot run (no credentials, the offline test env) verifies nothing and adds as before. Live probe this session: ZEC/USD and APT/USD (added by the 2026-07-20 06:40 pass, pre-verification) are ALSO unserved (0 bars each while UNI/USD got 753 in the same request).
- **Pruned through the journal, marked per the quarantine precedent.** `scripts/prune_unserviceable_20260720.py` removed MANA/USD, RUNE/USD, ZEC/USD, APT/USD via `apply_event` (source prune, probe evidence in the reason, soft delete, never a raw DELETE). Their fabricated bars (3 MANA + 3 RUNE) already carry `source='synthetic'` from the write site; the script sweeps any unknown stragglers (found 0) and refuses to touch a symbol that has real bars. Active watchlist after: AAVE/USD, LDO/USD, UNI/USD.
- **Both directions proven on the real stack.** Direction 1 live: MANA/USD re-added through the journal, stack started, the feed logged "no data available for MANA/USD; yielding no tick" once, `symbol_unavailable` fired once, ZERO MANA bars were written, all seven serviceable symbols closed real_feed bars and warmed, and the real `check_health` read feed ok with `feed_substitution` false and seven `serving_symbols`. MANA removed again after the proof. Direction 2 hermetic (the 07-19 precedent; production is not poisoned deliberately): a symbol WITH real history going non-real in-window past the grace still triggers the designed stop-then-start, even with another symbol serving.
- **Also fixed:** the keyless council mock rationale leaked the credential env var NAME into thesis output ("MOCK (no OPENAI_API_KEY)"), caught by `test_research_satellite` in this keystore-locked environment; it now names the provider label ("no OpenAI key").
- **Suites:** pytest 829 (from 806: +21 test_tradeable_invariant.py, +1 funnel refusal, +1 per-symbol broken-feed, fixtures updated to the new contract), ctest 26/26 (+ test_feed_no_fabrication, the fabrication mutation killer). Mutations killed with file-copy rollback: walk fallback restored (C++ test + lexical guard both fail), serving scope dropped (3 tests), substitution vetoed by serving (4 tests).
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, promotion criteria, the RL fill gate, `min_directional_votes`. Live trading stays off. The stack is left RUNNING on the real path with the three serviceable watchlist members.

### 2026-07-19 (Fable 5) — The watchdog stops killing fresh starts: substitution scoped to current data, a startup grace, onboarding-incomplete separated from stale, and remediation that cannot loop

- **The 2026-07-20 remediation loop is closed.** The remediation added the day before fired on stale historical data: the newest bars for LDO/QQQ/SPY were synthetic rows left over from the 2026-07-19 outage, the watchdog read a synthetic newest bar as a live feed substitution, and it POSTed /engine/stop seconds after every clean start, before one live bar could arrive. Every start died the same way. Aggravators: MANA/USD and RUNE/USD sat on the watchlist with zero bars ever and counted as stale, and the substitution check considered the newest row of ANY age.
- **Substitution is judged only on bars inside a recency window.** New `watchdog.substitution_recency_window_seconds` (default 900, equal to the staleness threshold). A non-real bar older than the window lands in `out_of_window_non_real`, logged as historical evidence, never a substitution. No bar inside the window is a freshness question and the two are never conflated. Mutation: removing the window fails 3 tests.
- **A startup grace period protects a stack that has not yet fetched live data.** New `watchdog.startup_grace_seconds` (default 900: three 5-minute bar intervals, one full live bar plus margin, and equal to the staleness horizon). Engine age comes from /proc via the engine pid in the lock (`evidence.process_start_epoch`), falling back to the lock ts, and an unknowable age reads as PAST the grace. Inside it, feed conditions are observed and logged (`grace_observed`), never remediated. Engine, bridge, and backend failures are never grace-suppressed: a degraded bridge still gets its single restart at any age. Mutation: removing the grace fails 2 tests.
- **Zero bars ever is onboarding-incomplete, not stale.** A never-backfilled symbol lands in `onboarding_incomplete` / `feed_onboarding_incomplete`, is named in the status line, and never by itself marks the stack unhealthy. THE CAUSE OF THE MANA/RUNE ZERO-BAR STATE, proven by a live probe: discovery ranks through Finnhub (Binance pairs), onboarding backfills through Alpaca, and Alpaca serves NO data for MANA/USD or RUNE/USD. The same request returned 565 AAVE/USD bars and 0 for both. AAVE and LDO backfilled because Alpaca carries them. The funnel can surface names the execution venue cannot serve (open flag below).
- **Remediation cannot loop.** The loop guard persists the last restart in `.run/watchdog_state.json` across cycles and watchdog restarts. The same condition recurring within `remediation_hold_window_seconds` (1800) of a restart escalates to notify-and-hold: the stack is left exactly as it is, one loud notification, renotified once per window, released on the first healthy cycle with a recovery note. Failed restart attempts count, so a refusing supervisor cannot loop either. `max_restarts_per_hour` (3) is now WIRED as the cross-condition cap (it was parsed and used nowhere) and catches A/B/A/B alternation. A kill trip still outranks everything: notify only, never restart, never auto-resume.
- **Both directions proven against the observed failure.** Reproduced hermetically: newest LDO/QQQ/SPY rows synthetic and hours old, MANA/USD and RUNE/USD on the watchlist with zero bars, engine 30s old, overnight. The watchdog reads substitution FALSE, logs LDO out-of-window, reports MANA/RUNE as onboarding-incomplete, marks LDO stale (the honest freshness question), and returns `grace_observed` with zero supervisor posts. The other direction: a synthetic bar 60s old on the real path past the grace reads substitution TRUE and triggers the designed stop-then-start exactly as before.
- **Suites:** pytest 806 (from 790: +16 in test_watchdog_recency.py, 2 per-symbol tests retargeted to the new zero-bar contract), ctest 25/25. Mutations killed: recency window removed (3 tests), grace removed (2 tests). Test hermeticity: MAL_RUN_DIR joins the conftest isolation set so loop-guard state never lands in the repo or leaks between tests.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, promotion criteria, the RL fill gate, `min_directional_votes`. Live trading stays off.

### 2026-07-19 (Fable 5) — The keystore handle leak is fixed at the source, the watchdog actually remediates, and the fd alarm keys off the baseline

- **THE ROOT CAUSE OF THE BRIDGE OUTAGES IS FIXED.** The credential resolver opened a new sqlite connection to `.keystore/credentials.sqlite` on EVERY lookup and closed none of them: `with conn:` is a transaction scope, not a close, and on Python 3.14 each connection lands in cycle garbage that refcounting never frees, so a mostly idle bridge ran out of fds instead of running out of garbage. Live evidence from pid 587930: 1023 of 1024 fds, 1018 of them keystore handles, ~30 fds/hour, socket count flat at 1. A process with no free fd cannot open a new file or socket, which is the 19-hour (07-17) and 20-hour (07-19) silent feed substitutions AND the engine-reads-ON funnel-reads-OFF condition (the funnel could not open controls.json and fell back to config, which ships discovery OFF).
- **The fix is one long-lived keystore connection per process, serialized under a lock.** `_store()` keeps its call shape but now yields THE shared connection (`check_same_thread=False`, all use inside the store lock, so the bridge's request threads are safe), reconnects when the store path is repointed (test hermeticity), and drops a broken handle on sqlite error so the next call self-heals. Chosen over per-call open-and-close per the prompt's preference and because it also removes the churn; chosen over a value cache because a key saved in the GUI must be visible to the bridge without a restart.
- **Every other SQLite open audited, three more leak sites fixed.** Leaking: `ui/db.py` (4 sites, the long-running Dash app) and `api_server/store.py` (2 sites, the long-running backend the GUI polls), plus one-shot `ops/demo.py` (2 sites), all now `contextlib.closing`. Already closing correctly (verified, unchanged): watchdog, maintenance, backup, weeklog, discovery run/universe/store paths, adaptive run, rl_advisory, ml_factor, market_data backfill, api_server controls/stack, quarantine script.
- **Proven by measurement, both directions.** New `tests/test_keystore_fd_leak.py` resolves a credential 1000 times and asserts the process fd count does not grow. Against the pre-fix code: +397 (get_credential), +1000 (get_credential_source). After: +0. The same harness covers resolve_env, the missing-credential path, writes, 8-thread concurrent resolution, backend query/append loops, and the Dash query loop. A lexical guard pins `with sqlite3.connect(` out of runtime code. Mutation: reintroducing the per-call connection fails 8 tests.
- **The watchdog detected 60 times and remediated zero. Now it remediates.** Three compounding defects: `attempt_restart` only knew how to START a down stack; `stack.self_heal` refused while the sick bridge still answered HTTP 200 ("a healthy stack is already running"); and the supervisor's refusal ("start refused: already running") echoed `state: running`, which the old success predicate read as a completed restart, so every degraded cycle notified "Restarted" while nothing was ever stopped. Now a running-but-sick stack (degraded bridge OR feed substitution) is STOPPED first through the supervisor's graceful /engine/stop (the GUI Stop path, never the kill-request file), then started, and success requires the supervisor to ACCEPT the start (`ok: true`). An unreachable backend leaves the running stack up, deliberately: stopping an engine we cannot restart would turn a degraded stack into a dead one. A kill trip still outranks everything: notify only, never restart, never auto-resume. Mutations: the state-echo predicate fails 1 test, removing stop-first fails 6.
- **The fd alarm fires on the way INTO trouble, not at the cliff.** At 07:57Z the feed had already substituted at fd_count 410 while the 80-percent-of-limit alarm (819) read ok, so percent-of-limit was the wrong axis. Auto threshold is now min(256, half the soft limit): several times any honest burst, keyed to the healthy baseline of a few dozen fds, and it does not rise when the limit does. A new `fd_trend` capability check compares the fd-count FLOOR between the halves of a sliding hour window: a leak raises the floor, honest load only raises the ceiling, so sustained growth of 12 (the measured 30/hour leak shows ~15 there) reads degraded within about an hour of onset, and the watchdog then restarts the bridge with the evidence capture intact.
- **The soft fd limit rises to 65536 at bridge startup, documented as defence in depth and nothing more.** It buys an unknown future leak time under the telemetry, never disguises one: the alarm keys off the baseline, not the limit, so a leak reads degraded at 256 fds whether the ceiling is 1024 or 65536. Never lowers an already-higher limit, bounded by the hard limit.
- **Suites:** pytest 790 (from 759: +13 test_keystore_fd_leak.py, +9 test_watchdog_remediation.py, +9 fd trend/threshold/rlimit in test_evidence_capture.py), ctest 25/25. Mutations killed: leak reintroduction (8 tests), state-echo success predicate (1 test), stop-first removal (6 tests).
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, promotion criteria, the RL fill gate, `min_directional_votes`. Live trading stays off.

### 2026-07-18 (Fable 5) — Open defects closed: the budget charges provider calls, the watchdog sees each symbol, strays cannot touch production, and the two unexplained failures now document themselves

- **The discovery budget charges provider CONTACT, never evaluator returns.** `build_verdict` reports `provider_calls` (the council's scored per_model count, 0 on every short-circuit), and `evaluate_survivors` charges one unit only when it is positive. A gate decline, risk pre-check, or market-hours skip costs nothing and does not eat the per-pass ceiling. An unreporting evaluator is charged one, so an unknown evaluator can never spend unbounded. The persisted `council_calls` now carries the honest number. Corrected 2026-07-17 accounting: the recorded 12 calls were really 2 (passes 3 and 4 spent 5 phantom units each, every candidate flat at conviction 0.0).
- **The watchdog freshness probe is per-symbol and names the offender.** `feed_ok` checks every symbol the engine actually trades: the profile-resolved whitelist (`stack.whitelist()` now applies the active_quant overlay, fixing a latent warm-report mismatch too) plus active watchlist members when discovery is on. One stale or bar-less symbol fails health BY NAME in the payload, the status line, and the ntfy message. Equities count only inside US regular hours (the one cadence authority, `discovery.run.us_market_open`). The provenance rule survives per symbol: fresh but non-real on the real path is unhealthy and named.
- **Watchlist polling was already wired, and is now pinned live.** The engine merges active watchlist members into the whitelist and the feed at construction and add-only each iteration (`onboard_discovered_symbols` since 2026-07-16). A subprocess test seeds a watchlist row, runs the built engine offline, and asserts the `discovery_onboard` event AND closed bars for the member. SOL/USD went stale for a process reason, not a code one: it was a hand-seeded verification row whose engine exited before polling, the seeding session then deleted the row with a raw SQL DELETE (bypassing `apply_event`, hence no journal entry), and every later engine correctly found an empty watchlist. It was also never in the running swing whitelist (SOL sits only in the active_quant overlay, which no launcher selects).
- **A stray invocation cannot pollute the production diagnostic log.** The `--help` fix is confirmed: all five bare-engine `discovery_blocked` warns predate it, none since. Structurally: `mal_engine` without `--db` now writes a scratch `mal_demo.db` and says so, never `market_ai_lab.db`. Every real launcher passes `--db` explicitly.
- **The bridge carries fd telemetry.** `/health` reports `fd_count` with an `fd_headroom` capability check that reads degraded at `bridge.fd_warn_threshold` (auto 80 percent of the RLIMIT_NOFILE soft limit), and a daemon thread logs the count every `bridge.fd_log_interval_seconds`. On a degraded or substitution state the watchdog reads the bridge's fd AND socket counts externally via /proc (works even when the bridge cannot open a file, which is the suspected state), writes an evidence record BEFORE the restart destroys the state, and puts both numbers in the notification.
- **The two unexplained failures self-document on recurrence (root causes deliberately NOT fixed).** New `ops/evidence.py`: rate-limited JSON records to `diagnostics/` holding the control file bytes as read (verbatim, with sha256, size, mtime, mode), the reading process pid, START TIME from /proc (the fact that settles the stale-process hypothesis), argv, and fd count, each field gathered independently so fd exhaustion voids nothing. Wired at both detection points: `set_layer` captures when the on-disk value contradicts the last audited write (the layer.whale class), and the bridge captures when the engine says `engine_reads_enabled` on a discovery request while the funnel reads the flag OFF (the mismatch class, fd count included).
- **The unenforced scale caps are REMOVED, not enforced.** `whale_position_scale_cap` and `dnn_position_scale_cap` had no consumer and no defined clamp semantics, so "enforce as documented" would have meant inventing a per-factor sizing mechanism in the money path, a behavior change by definition. Removed from config.hpp, config.cpp, and both yaml files (older yamls still load), every "0.35 cap" doc claim rewritten to name the real controls (ensemble weights, the discovery +/- 0.10 adjustment bound, and the enforced `default_position_scale_cap`), and a test pins the keys stay removed.
- **Suites:** pytest 759 (from 718, +41 new across four test files), ctest 25/25, vitest 116, tsc clean. Mutations killed: budget count-on-return (3 tests fail), freshness any-fresh (3 tests fail).
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, promotion criteria, the RL fill gate, `min_directional_votes`. Live trading stays off.

### 2026-07-18 (Fable 5) — One DNN pipeline: train and serve share the builder, artifacts carry their signature and normalizer, and the graduation path is real

- **Train and serve are the same function by identity.** `real_dataset._features_at` is THE canonical builder (`features.features_at` is the same object, pinned by test). The 10-feature state builder with its constant defaults is deleted. Serving scores the newest real-bar window (`serve_window`, warm-up-guarded, proven-synthetic bars excluded on BOTH paths), so the features a model trains on are exactly the features it is served. Canonical set `bars-v2`: ret_1, ret_5, atr_norm, rsi, regime, time_of_day (computed from the bar timestamp, not the old constant 0.5). vol_z left the set because the engine synthesizes bar volume even live, the exact train-real serve-fake mismatch this work removes.
- **Artifacts carry their signature and fitted normalizer, and serving refuses without them.** The npz stores feature_names, feature_set_version, norm_mean, norm_std. `forward` standardizes with the persisted stats, `signature_matches` refuses a missing signature OR missing normalizer, and `score_state` fails closed (flagged zero payload, clear reason) on any mismatch. The unsigned synthetic bootstrap self-heals narrowly (dnn-0.x only, regenerable by seed); any other unsigned artifact is refused, never overwritten.
- **The metadata-only challenger is impossible.** `train_real_challenger` trains a real `DnnModel` through the shared `_fit` loop and saves `challenger-<id>.npz` with signature and normalizer, recorded as `artifact_path`. `request_promote` refuses a challenger without a loadable signed artifact BEFORE touching the registry (pinned: the registry is untouched on refusal) and installs the artifact as champion.npz on success, which is exactly what bench_state's artifact-match rule needs to unbench. A promote whose install fails says the factor stays benched. Promotion criteria and the RL fill gate untouched. The DNN stays benched throughout.
- **Every silently defaulted serving feature is gone.** recent_winrate (the proven -0.33 driver), streak, drawdown, imbalance, catalyst, spread_rel: removed. A symbol without enough real bars is UNAVAILABLE with a reason and zero contribution, consistent with the bench, never scored on constants. Full audit table in RETURN.md.
- **The known footguns are closed.** `mal_engine --help` and `-h` print usage and exit before any config, schema, or DB touch (subprocess-tested). `api_server.controls.real_fills` now calls the canonical `count_closed_trades`, so the RL write gate equals the read gate by construction and an unreadable DB reads 0 (gate stays shut). `adaptive/run.py::_db_path`, `ops/watchdog._db_path`, `discovery/run.py` defaults, and the quarantine CLI default are repo-anchored, pinned by a foreign-cwd test.
- **Suites:** pytest 718 (from 705), ctest 25/25, vitest 116, typecheck clean. Mutants killed: signature check bypassed (2 tests fail), promotion artifact refusal bypassed (1 test fails).
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, promotion criteria, the RL fill gate. Live trading stays off.

### 2026-07-18 (Fable 5) — Holds abstain, the synthetic DNN is benched, and discovery produces its first watchlist member ever

- **The ensemble no longer averages holds into a directional read.** Bias, conviction, and edge are computed over DIRECTIONAL voters only. A hold is scored, stays raw in per_model, and is reported as an abstention. Zero directional voters reads flat at conviction 0.0 and is always avoid, even for confident holds. Agreement counts directional voters only, which also kills the artifact where one seller plus two holds logged agreement 3. New `council.min_directional_votes` (shipped 1, DELIBERATELY PERMISSIVE for this evaluation period, 2 is the conservative revisit) gates non-avoid verdicts, and the 0.60 floor still applies to the directional conviction.
- **The uniformly negative DNN is diagnosed, mechanism proven.** Training draws features from N(0,1). Serving feeds production-scale values (returns ~0.03, constants 0.5). The model therefore evaluates one near-constant out-of-distribution point, and decomposition shows the hardcoded default `recent_winrate=0.5` ALONE moves the read from +0.03 to -0.33. At training scale the model spans -0.79 to +0.82, so it is responsive, not sign-flipped. Two more defects found in the graduation path: train_real records challengers as registry metadata with NO servable artifact, and trains on a 6-feature set while serving builds 10 different features.
- **The synthetic champion is benched, RL-style.** `bench_state` requires the registry champion to be provenance real-data AND match the serving artifact id, else bias/confidence/edge aliases are zero while raw dnn_* outputs stay visible. Visible as "BENCHED pending real training" in the bridge /status, the engine startup block, and the GUI readiness detail, distinct from operator-disabled and from unreachable. The artifact-match rule also guards the metadata-only-promotion gap. Promotion criteria untouched.
- **First productive discovery pass in the system's history.** Live verification (pass 13, real key, real bridge, 2 remaining budget calls): TIA/USD all-hold read 3 abstentions at conviction 0.00 (avoid, correct), and **LDO/USD: one directional seller at 0.56, two abstentions, whale confirmation +0.05, dnn exactly 0.00, conviction 0.61, verdict SELL, added to the watchlist and onboarded with 8,546 five-minute and 152 daily bars, every one `source='backfill'`**. Against the 12 historical verdicts: the all-hold cases now read 0.0 instead of manufacturing 0.53 to 0.69, and the single-voter cases carry the voter's own conviction instead of a diluted 0.55.
- **Suites:** pytest 705 (from 691, +14 new, 3 old fakes updated to the new contract), ctest 25/25, both mutants killed (abstention removed: 3 tests fail, bench gate inverted: 4 tests fail). Bridge capability health verified live: fresh_file ok, fresh_socket ok, market_quote ok.
- NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, Level 1 values, promotion criteria. Live trading stays off. Details and the DNN findings in RETURN.md.

### 2026-07-18 (Fable 5) — Bar provenance lands: the real path refuses non-real bars, the substitution is detectable, the bridge proves capability, the contamination is quarantined

- **Every bar now says where its prices came from.** `bars.source` (`real_feed` | `backfill` | `synthetic` | `replay` | `unknown`) is set explicitly on every write path: the AlpacaFeed tags each tick per symbol (one missing quote walks ONE symbol, and that symbol's bar goes synthetic), MockFeed tags synthetic, the bar modes pass their provenance at the compiler-forced `on_closed_bar` call sites, and the Python backfill writes `backfill`. One synthetic tick contaminates the whole aggregated bar. Empty or junk lands as `unknown`, never real. Existing rows migrate to `unknown` rather than a guess, tolerant on both the C++ and Python sides.
- **The real path refuses entries on non-real bars.** `core/provenance.hpp` is a pure exhaustively-tested predicate: on `alpaca_paper` only `real_feed` and `backfill` bars may open a position. The gate sits ahead of the warm gate and the whole signal path, logs `provenance_block` once per transition, and can only refuse. Exits are never gated, so a position is never trapped, and both the exit event and `trades.bar_source` record what the fill executed against. RiskGate logic, thresholds, and the live gate untouched.
- **The substitution itself is now a critical event within one poll interval.** On the real path a non-real tick fires `feed_substitution` (critical, once per transition, naming symbols and provenance), recovery fires `feed_restored`. The GUI run-state banner shows a red warning strip while active. The watchdog notifies through the existing ntfy path. The 19-hour blind spot becomes minutes.
- **The bridge proves capability, not liveness.** `/health` now exercises a fresh file read (the exact controls.json read that died), a fresh loopback socket (pooled connections survived the outage, so a pool proves nothing), and a cached real-quote probe (keyless reads `skipped`, not degraded, so the offline loop stays clean). Status `ok` or `degraded` on HTTP 200, so degraded stays distinct from unreachable.
- **The watchdog acts on degraded and cannot be fooled by advancing synthetic bars.** A degraded bridge is a failure. The feed check reads the newest bar's provenance: fresh but synthetic on the real path is UNHEALTHY (`feed=NON-REAL (synthetic)` in the notification), exactly the state the old MAX(timestamp) check waved through for 19 hours. Restart policy unchanged, kill trips never auto-resumed.
- **The contamination is quarantined by provenance, not deletion.** `scripts/quarantine_synthetic_bars_20260717.py` (idempotent, triple-guarded by window + symbol + engine-written timestamp shape) marked **892 bars** `synthetic` and the **2 trades** (BTC bought 74,335, exited 81,650, both walk prices) `bar_source='synthetic'`. The remaining 24 walk bars in the window landed on aligned `:00` stamps and stay `unknown` deliberately: the next backfill upserts those exact timestamps with real prices and `backfill` provenance, self-healing them, and `unknown` never trades and never reads as real meanwhile. The canonical real-fill counter (`count_closed_trades`) excludes proven-synthetic fills: 240 vs 241 unfiltered, the contaminated exit excluded. WEEKLOG flags synthetic-feed fills with a warning line.
- **Verified live end to end:** a scratch synthetic-regimes engine run wrote 160 bars, every one `source='synthetic'` through the real write path. Suites: ctest 25/25 (new `test_provenance`), pytest 691 (from 675, +16), vitest 116, typecheck clean. Mutation-tested five ways, all killed: gate-always-allows (6 checks fail), upsert empty-defaults-real (2 fail), counter-ignores-bar_source, feed_ok-ignores-provenance, health-never-degrades.
- Full detail, safest-choice notes, and the contamination report in RETURN.md.

### 2026-07-18 (Fable 5) — DIAGNOSTIC second pass, no behavior changes. Corrected timeline, and a 19-hour silent bridge outage found under the restart question

- **The funnel completes, replicated.** Pass 12 ran end to end in 113s against the real key and a fresh real bridge: 50 to 12 finalists free, 7 Haiku gate calls with 2 real rejections, 5 council calls with all three providers answering, 5 of 5 `avoid`, 0 watchlist adds. Same shape as the OPUS pass 11.
- **The verdict-rule finding is sharpened, not just confirmed.** Two of today's verdicts (LDO 0.6937, INJ 0.6358) CLEARED the 0.60 floor and were still avoid because all three providers held, so direction was flat. Across all 12 real verdicts ever: every directional one sits 0.4929 to 0.5938, every one at or above 0.60 is flat. The averaging makes direction and conviction anti-correlated by construction, and the synthetic DNN has read negative 17 of 17 times. The rule has never been satisfiable, not merely a floor set too high.
- **Timezone correction withdraws the OPUS stale-process hypothesis.** Commits log at -0700. e348d28 landed 06:19:28Z on 07-17. All five budget-spending passes (05:57 to 06:10Z) PREDATE it: they were the fix session verifying its own in-progress working tree at 23:00 local, and the Stage-B drop reasons record each repair step. The UTC budget date made a 23:00-local verification burn the next UTC day's allowance. The count-on-return bug (funnel.py:430) stands.
- **The real killer was invisible: the bridge process failed internally at 11:50 to 11:56Z on 07-17 and stayed sick for 19 hours.** Proven from the record: live market data died between 11:50:02Z (last flat real SPY bar, 750.87) and 11:55:06Z (first walk bar), leaving 916 synthetic 5-min bars in the real bars table through 06:55Z; the funnel's flag read went OFF at 11:56:36Z and never recovered (zero skips, zero passes after, fresh budget unused) while the engine's parse of the same file never flipped (zero discovery_toggle events ever); pass 10 lost 15 of 50 quotes minutes before. Pooled council connections kept working mid-outage (research theses at 16:55Z and 01:00Z). Fresh-socket and fresh-file paths died together: consistent with fd-class exhaustion, NOT proven, process gone.
- **Consequences proven:** zero equity discovery passes have ever run (07-17 RTH fell inside the outage), the run's only two trades executed against walk prices (BTC bought at 74,335 vs ~63k real), and the watchdog reported the feed fresh throughout because walk bars always advance.
- **Restarts confirmed benign:** all 13 in one 21:36 to 01:48 local build evening, then 22h04m continuous, clean stop, down since 06:55:51Z with a stale lock.
- Full report, corrections to the OPUS report, and fix order in RETURN.md (FABLE entry).

### 2026-07-18 (Opus 4.8) — DIAGNOSTIC ONLY, no behavior changes. The discovery funnel completes. Its output gate is set above what the pipeline can produce

- **A full pass ran end to end in isolation and returned `ok` in 95 seconds.** Real Finnhub key, real bridge, real council, all three providers answering. Universe 50, Stage A 50 to 12 finalists at zero LLM tokens, Stage B 5 of 5 finalists through the Haiku gate, Stage C 5 council calls, 5 verdicts. **All five verdicts were `avoid`.** `run.py` skips `avoid` when building the watchlist, so the pass added nothing and onboarded nothing. From the outside that is indistinguishable from a dead layer. The funnel is not broken.
- **The floor is the finding.** `build_verdict` requires conviction at or above `council_min_confidence` (0.60). Seven real council verdicts now exist across two days: 0.5265, 0.5366, 0.5537, 0.5726, 0.5773, 0.5786, 0.5938. None has ever cleared 0.60. The shortfall is consistent at 0.006 to 0.074.
- **Two compounding causes, both measured.** The ensemble averages non-directional holds into a directional read, so one convinced model plus two holds lands near the provider mean of about 0.55. And the synthetic DNN champion returned -0.31 to -0.34 on all 12 symbols ever evaluated and never once positive. ADA/USD is the proof: council confidence 0.6026, ABOVE the floor, pushed to 0.5366 by an advisory adjustment of -0.066. An untrained advisory factor is currently the deciding vote against the layer it advises.
- **The budget burn is a counting bug, and 83 percent of the spend bought nothing.** `funnel.py:430` increments `calls` when the evaluator RETURNS, not when a provider is called, so every short-circuit inside `consensus()` costs a full budget unit for zero provider spend. On 07-17 passes 3 and 4 spent 10 of the 12 daily calls returning confidence 0.0, agreement 0, and an empty `per_model`, which is the `_flat_consensus` signature and means no provider was contacted. The whole allowance went in 3 minutes 35 seconds across three operator-forced passes.
- **SOL/USD never received one live bar.** It has 8519 bars and ZERO with a non-zero seconds field, while BTC/USD has 427. The backfill writes aligned timestamps and the engine writes at odd seconds, so every SOL/USD bar came from the backfill. Its watchlist row was hand-seeded with the reason "onboarding path verification" and the onboarding ran inside a short-lived non-continuous engine that exited before polling. The C++ onboarding plumbing is correct. It has simply never been reachable, because no verdict has ever been non-`avoid`.
- **Two of the four reported failure modes are not the production engine.** Five `discovery_blocked: engine has no bridge` warnings coincide to the second with short non-continuous engine starts. Every real launcher passes `--bridge`. Consistent with the already-logged footgun that `mal_engine --help` runs a 20-iteration demo against the real database.
- **The restart premise inverts.** All 13 startups fall inside one 4h12m operator debugging window, and the engine then ran **continuously for 22 of 24 hours** (2648 iterations, clean stop). Hourly passes did fire during the churn. They returned `budget_exhausted` because the budget was already gone. The real problem is that the engine has been DOWN since 2026-07-18T06:55:51Z with a stale lock file, which is why 07-18 has no pass at all.
- **Not asserted.** The flag-mismatch mode ("engine reads discovery ON but the Python funnel reads it OFF", 06:51:10 and 11:56:36) is not reproducible and not explained. A torn control-file read fits the symptom and was not demonstrated. Which code the 06:06 and 06:08 processes ran is a hypothesis (pre-`e348d28`, process gone, start time unrecoverable), not a finding.
- Full evidence, per-stage numbers, and the recommended fix order are in RETURN.md.

### 2026-07-18 (Opus 4.8) — The duplicate whale key was real, the reported symptom was not, and the collision could never have caused it

- **The prompt's diagnosis was backwards, and the direction is the whole point.** The report was that the duplicate keys collide and drive the whale layer to False. Probing the REAL reader against the REAL file says `whale=1`, ON. Reading `json_get_bool` explains why it must: on a collision the search lands on the source STRING, a string parses as neither `true` nor `false`, and the function returns its DEFAULT, which is `true`. **A collision can only make a layer stick ON, never off.** So the hazard is real and the reported symptom cannot be it. Fixed the hazard, did not invent a mechanism to match the report.
- **What the collision actually costs, measured both ways.** `layers` emitted first, whale off: reads `whale=0`, correct. `layer_sources` emitted first, whale off: reads `whale=1`, the operator's off silently discarded. The toggle worked only because of the order two dicts happen to appear in `_defaults()`. A `sort_keys=True`, an alphabetical tidy, or any new block carrying a layer name would have turned the whale, council, and dnn_advisory OFF switches into no-ops, with nothing in the logs to say so. An advisory spender the GUI cannot switch off is the bad half of that trade.
- **The fix is distinct flat keys, not a documented ordering.** The engine now reads `layer_<name>_enabled`, which no other key contains as a substring. The nested `layers` and `layer_sources` maps are untouched, because Python and the GUI read them by PATH, where keying both maps by layer name is correct and unambiguous. The Open Flag had proposed ordering the writer's blocks and documenting it; that would have left the hazard live and load-bearing on a comment.
- **`adaptive` was safe only by luck** and was renamed with the other three. It has no source axis today, so it had no twin. Adding one later would have reintroduced the collision silently.
- **The audit found exactly three instances and no others.** Counting every needle the `core/*` readers flat-search against the live file: `council`, `dnn_advisory`, and `whale` each appeared twice; all 17 other keys appeared once. Now the guard is a test rather than a one-time audit: it scrapes the needles out of the readers and asserts each appears EXACTLY once in a written file. Exactly once, not at most once, because a key the engine reads but the writer never emits falls back to the same default and fails identically from the other side.
- **Mutation-tested both halves.** Reverting the C++ reader to the bare `"whale"` needle: the reordered-file case and the independence case FAIL, while the layers-first case still passes, which is precisely the invisibility that let this ship. Reverting the writer's emission: the uniqueness guard fails with `{layer_*_enabled: 0}`.
- **The six `layer.whale True -> False` events remain unexplained, and I said so rather than closing them.** They are Python-formatted, so they came from `set_layer`, and each read `old=True` after the previous click wrote False, meaning something restored True unaudited between clicks. Ruled out with evidence: every one of the 17 write sites is audited; `set_layer` and all eight other setters persist correctly in isolation; `seed_feed_clock` preserves layers; only `controls.py` writes the key and the C++ side never writes at all; the events postdate the atomic-write fix by 76 minutes; and there is one control file on the box, mode 0644, untouched since 08:48Z. The most likely remaining explanation is a backend process still running pre-fix code, since Python does not hot-reload, but I could not reproduce it and am not asserting it. Logged as an open flag.
- Live verification: engine banner reads `L4 whale on-real [controls.json]`, 12 iterations, ZERO `layer.whale` events, 48 `whale_signal` rows, ensemble weight 0.1007 against the 0.35 cap. Round-tripped the GUI toggle off and back on and watched the engine follow both ways. Ran against a scratch DB so the operator's DB stayed clean. pytest 675 (from 671), vitest 116, ctest 24/24, typecheck clean. NOT touched: RiskGate, the live-trading gate, the adaptive invariant, any Level-1 value. Live OFF.

### 2026-07-17 (Opus 4.8) — Review of the Whale Alert commit: I fixed half a flag bug and shipped two surfaces that contradicted each other

- An xhigh review of `a8c5aed` found 10 defects. All 10 fixed. The worst was self-inflicted: I corrected the env-vs-config flag source for Whale Alert and left the identical bug in `_check_sec_edgar` three functions away, so the Health row said SEC EDGAR was off while the Ops panel I added in the SAME commit said it was on. Verified live before and after. The fix generalizes rather than adding a second special case: `store.whale_flag()` is now the one resolution (env > controls.json > config), shared by both health checks, the Ops panel, and `stack.whale_env()`, which hands the bridge its env. Deriving the bridge's env from config alone would have let the GUI enable a feed the fetching process never heard about.
- **The activity count was wrong on screen.** `ts >= datetime('now','-1 day')` compares an ISO timestamp ("2026-07-16T01:00:00Z") against SQLite's space-separated `datetime()` output ("2026-07-16 08:27:22"), and 'T' (0x54) sorts above ' ' (0x20), so every row on the cutoff's calendar DATE counted regardless of time: a 32h-old signal reported as "last 24h" and the window was really 48h. Now `strftime('%Y-%m-%dT%H:%M:%SZ',...)`, which compares like for like and stays index-friendly. It was the only `datetime('now')` SQL comparison in the codebase.
- **Giving the operator a lever instead of blaming the edit.** They had set `whale_alert_enabled: true` in the SHIPPED config, because config was the ONLY way to turn the feed on. Reverting that alone would have killed their working feed, so the runtime lever came first: a `whale_feeds` control block (seeded from config, overridden by controls.json), their intent migrated into it, and THEN the shipped default restored to opt-in. Verified after: config ships false, their feed still reports working.
- **The block is named `whale_feeds` and not `whale` for a reason.** The C++ layer reader flat-searches for a bare `"whale"` key, so a top-level `"whale"` object would shadow the whale LAYER toggle and make it read ON regardless. Found the pre-existing version of the same hazard while checking: controls.json already has two bare `"whale"` keys and the layer toggle survives only by emission order. Logged in Open Flags.
- **My #1 fix silently broke test hermeticity, and a test caught it.** Routing SEC EDGAR through config meant the shipped `sec_edgar_enabled: true` made the keyless check fire, so any test calling GET /health/integrations started making REAL requests to efts.sec.gov. The `env` fixture's own comment promised "no real network or socket call is made" and it had been true only because deleting the env var used to mean off. The fixture now SETS the flags off, and conftest pins them for the suite.
- **Two of my tests could not have caught their own bugs.** The frontend "never renders the key" test asserted against a mock with no key in it: unfailable. Made real, it fails, because HealthPage renders the reason verbatim, which is correct: the guard is the backend classifying its failures so a key never reaches the reason. So the assertion moved to the layer that enforces it. And my "both sources side by side" test did not cover the health row, so the SEC EDGAR mutant passed until I wrote a test that compares the two surfaces directly.
- One finding WITHDRAWN after measuring it, not fixed: nothing.  All 10 stand. pytest 671 (from 666), vitest 116, ctest 24/24. Every fix mutation-tested. NOT touched: RiskGate, the live-trading gate, the adaptive invariant, or any Level-1 value. Live OFF.

### 2026-07-17 (Opus 4.8) — Whale Alert reported "off" while it was on and working: the flag-source mismatch again, one process further out

- Goal. Surface Whale Alert in the Health view and the Ops section so the operator can verify it works and see its state.
- The premise was stale, and the check was lying. `_check_whale_alert` already existed and was already in `_CHECKS`, and the Health row already rendered (the view maps whatever the backend returns) and the aggregate already counted it (the math is integration-agnostic). So Task 1's frontend half was already done. What was broken is that the check reported **`not_configured: whale_alert_enabled is off`** while config said ON, the key resolved, and the feed worked.
- **The cause is the flag-source mismatch, one process further out than last time.** The check read the `WHALE_ALERT_ENABLED` env var. The whale library takes env opt-ins correctly, and `stack.whale_env()` DERIVES them from config and spawns the BRIDGE with them. But the health check runs in the API BACKEND, which nobody exports that env to. So config was the intent, the env was the transport, and the backend read the transport it never received. `whale_alert_enabled()` now resolves an explicit env override, else config, the way everything else does.
- **THE KEY WORKS.** One real call: `working`, `one tx query ok`, 244.5ms, with the key absent from the row. The operator turned `whale_alert_enabled` on in config (still uncommitted) and the feed has been live and reporting itself off ever since.
- The same three gaps the Finnhub check had, fixed the same way. `_get` raises on a non-2xx, so the old `status == 200 else (FAILING, f"HTTP {status}")` was **dead code on every failure**: the exception escaped to the generic handler, which stringifies it into the reason the GUI renders, and the Whale Alert key rides in the URL as a query param. Now: a 429 retries with the WHALE ADAPTER'S OWN policy (2 retries, exponential from a 1s base, 5s cap, honors Retry-After) rather than a second copy, since the developer plan is 10 calls a minute and the check shares that budget with a running whale pass. Failures classify to fixed phrases (`bad key (HTTP 401)`, `rate limited`, `network unreachable (<type>)`).
- Ops (Task 2). A read-only `GET /whale/feeds` and a panel showing BOTH whale sources side by side: SEC EDGAR (equities, free, keyless, delayed) and Whale Alert (crypto, keyed, trial). A disabled feed reads as "off by choice"; a feed that is ON but unkeyed is flagged amber, because it cannot work. **The activity count is labelled honestly rather than as asked.** The prompt asked for "last successful fetch time and recent whale-signal activity count", and `whale_activity` (raw per-fetch rows) is EMPTY BY DESIGN: the engine asks the bridge for a scored signal and never persists the activity behind it. So the panel reports whale FACTOR signals (2113 total, real data) and says in the payload and on screen that it counts signals not fetches and is not attributed to a source, because the whale layer combines both feeds into one 0.35-capped factor. Calling it "last successful Whale Alert fetch" would have been a fabrication.
- **Killed a defect class instead of fixing its third instance.** `test_shipped_config_has_the_long_term_sleeve_off` went red because the operator enabled the long-term sleeve: it asserted a SHIPPED default through the runtime path (`cfg_path=None`), which layers the operator's controls.json over config by design. That is the THIRD time this shipped (test_discovery_funnel, test_discovery_whale, now test_long_term_sleeve), and the first two I fixed by hand without grepping for the rest. `tests/conftest.py` now points `MAL_CONTROL_DIR` at an empty temp dir for the whole suite, the same way it already isolates the credential keystore, so no test can read the host's live toggles and the runtime path and the shipped path agree. A first attempt at a source-grepping guard flagged three FALSE POSITIVES (tests that use the runtime path correctly, while isolating the control dir), which is what a brittle static heuristic buys. The fixture is the real fix.
- Tests + verify. pytest 666 passed (from 653, +13). vitest 116 (from 105, +11, new `whale-feeds.test.tsx`). ctest 24/24. Typecheck clean, build green. Mutation-tested: reverting the whale flag to the env-only read fails its test, and removing the control-dir isolation fails its test. Two of my own test bugs caught by the suite: I asserted a flat `[1.0, 1.0]` backoff copied from the Finnhub test when the whale adapter's policy is exponential (`[1.0, 2.0]`), and I mocked `integrationsHealth` when the page calls `api.integrations()`. NOT touched: RiskGate, the live-trading gate, the adaptive limit-weakening invariant, or any Level-1 value. Bind stays loopback. Live OFF.

### 2026-07-17 (Opus 4.8) — Reviewed the precedence fix and found it shipped with three defects, two of which reintroduced the bug it removed

- Why this entry exists. The precedence work below (`ad586f7`) reads as a clean result. It was not. An extra-high-recall review of that commit found 14 defects, and the three most severe were self-inflicted regressions IN the fix. Fixed in `a52c69e` and `64c5a73`. The original entry stands as written, with this correction above it: a progress log that quietly edits its own past is worth less than one that shows the correction, and the useful fact here is that the fix needed fixing.
- **Two of the three reintroduced the exact silent-fallback bug the commit removed**, through different doors. Every reader of controls.json falls back to config on any read failure, which is the right posture and makes every one of these SILENT.
  1. **The atomic write narrowed controls.json to 0600.** `tempfile.mkstemp` creates owner-only; the replaced `open(path, "w")` gave umask-default 0664. A reader on another uid (service account, container, a desktop launcher) would not fail loudly, it would fall back to config and act on the shipped defaults. The operator's live file had already been narrowed by the bug and was rewritten back to 644 through the fixed writer, state intact. Now an explicit `_CONTROLS_MODE = 0o644` via `os.fchmod` before the rename.
  2. **A strict `isinstance(v, bool)` rejected the 1/0 the C++ reader accepts.** `core/bridge_client.cpp json_get_bool` tests `json[*p] == '1'`. So a hand-edited `{"discovery_enabled": 1}` read ON in the engine and OFF in the funnel: the reported mismatch, verbatim, through a hand-edit instead of a torn read. A shared `as_bool` now matches the C++ reader, with the bool check first because `isinstance(True, int)` is True. Strings and floats are still refused: a malformed boolean must never be read as an intent to start a spender.
  3. **An unguarded cross-package import broke `score_rl`'s never-raises contract.** The lazy `from llm_consensus import control_file` in `rl_enabled` propagated ImportError through `score_rl`, which promises "none of which ever raise". The bridge's /score/rl would have 500'd instead of returning a labelled neutral.
- Eleven more, all fixed: a missing `fsync` (the write was atomic against readers but not durable against a crash, so a power loss could still leave a zero-length file and revert every toggle); an fd leak whose first cleanup attempt introduced a DOUBLE CLOSE (os.fdopen takes ownership, so closing the raw fd in the except could close a number another thread had reused); a cwd-relative DB path in the RL gate, the same class the commit fixes elsewhere; an uncached COUNT(*) on the per-bar advisory path once RL activates; `rl_ensemble_factor_names` and `score_rl` disagreeing about whether an under-gated RL participates; dead imports left by the extraction; an inert monkeypatch; temp files leaking on SIGKILL; em dashes; a test coupled to a config value.
- **One finding withdrawn after measurement.** "rl_advisory drags the council stack" was raised on module count. Measured: 50ms, no heavy deps, and python_bridge/server.py is the only production importer and already loads llm_consensus first, so the cost is provably zero. Moving the module would have been churn. A finding that does not survive measurement should be reported as unfounded, not fixed to look thorough.
- **Two of my own justifying comments did not survive the review either.** "Runs ONLY when rl_enabled is true, which today it is not" excused a per-score COUNT(*) for exactly the state the feature exists to reach. And the write I called atomic was atomic against readers but not against a crash.
- Every fix is mutation-tested: reverting any one of them fails its test. pytest 653 (from 640, +13). ctest 24/24. NOT touched: RiskGate, the live-trading gate, the adaptive limit-weakening invariant, or any Level-1 value. Live OFF.

### 2026-07-17 (Opus 4.8) — The flag-source mismatch was a TORN READ, not a missing override: the GUI's own writes were flipping every toggle to its default

> CORRECTION (see the entry above): this commit shipped with three defects, two of which reintroduced the silent-fallback bug it removed. Fixed in `a52c69e` and `64c5a73`. Read both entries together.

- Symptom, from the operator's live events. `discovery_blocked: engine reads discovery ON but the Python funnel reads it OFF`, on both asset classes in the same second, at 06:51:10. The reported diagnosis was that the funnel reads the static config while the engine reads controls.json.
- The premise was half right, and the real cause was worse. `discovery/settings.py` already layered controls.json over config, and had since it was built. The timeline shows why that matters: until 06:46 the SAME funnel was logging `discovery_skip: not due (last pass 36m ago, interval 60m)`, which only happens when it reads the flag as ON. So the funnel was reading ON, then suddenly read OFF. A missing override cannot do that. Something intermittent could.
- **ROOT CAUSE 1, a TORN READ, and it is the live bug.** `api_server/controls._write_controls` wrote with `open(path, "w")`, which TRUNCATES and then writes. Every reader of controls.json swallows a read error and falls back to config, so a read landing inside that window did not fail loudly, it silently reported the SHIPPED default, which for discovery is off. **Measured on the old writer: 88 percent of reads (2634/3000) returned discovery OFF while the file on disk said ON.** That is the mismatch, exactly: the GUI writes the file, the funnel reads a half-written file at the wrong microsecond, falls back to config, and reports OFF while the engine (reading at a different moment) reports ON. It explains both asset classes flipping in the same second, since the engine asks about them back to back. It was never discovery-specific: it silently reset EVERY runtime toggle to its default, on both sides, at random, and hit hardest exactly when the operator was toggling. Fixed with an atomic write (temp file in the same directory, then `os.replace`). Verified: 0 of 3000 reads wrong, no temp files left behind. Mutation-tested: restoring the truncating write fails the new test.
- **ROOT CAUSE 2, a CWD-RELATIVE PATH.** config ships `system.control_dir` as the relative `.control`, and three separate copies of the resolution (api_server/controls.py, discovery/settings.py, adaptive/settings.py) each resolved it against their OWN process's working directory. The engine, the bridge, and the API backend are three processes, agreeing only by all happening to launch from the repo root. Reproduced: the same call returns discovery ON from the repo root and OFF from /tmp. A relative control_dir now anchors to the REPO ROOT.
- The audit found three more instances of the class, where the GUI writes controls.json and a Python component read config only.
  - **`gate_enabled`**: `api_server.set_model("gate", ...)` writes it, validated and audited, and `llm_consensus` read `llm.gate_enabled` from config, so the council ran the Haiku base-check no matter what the operator chose. A cost control, ignored.
  - **`research_satellite_enabled`**: config `sleeves.research_satellite_enabled` vs control `sleeves.research_satellite`. The key NAMES differ, which is why it is mapped explicitly rather than block-overlaid.
  - **`rl_enabled`**: fixed, but only alongside a real safety change, below.
- **The RL hard rule is now structural, not write-path-only.** CLAUDE.md: "RL activates only past the `rl_min_real_fills` gate". That gate lived ONLY in `api_server.set_rl`, which refuses to WRITE an enable below it. `score_rl` gated on the flag alone. So the rule held only for operators who went through the GUI, and a hand-edited file of either kind could activate RL under-gated. Honoring controls.json for `rl_enabled` without fixing that would have widened the bypass, so the gate is now enforced at the READ (`rl_advisory.service.rl_gate_unmet`, counted with the canonical strategy-fills-only counter, failing CLOSED when the count cannot be read). Verified live: a hand-edited `rl_enabled: true` is honored as a request and `score_rl` returns `source: "gated"`, bias 0, confidence 0, out of the ensemble, at 240/500 fills. This makes the gate STRICTER, never looser.
- One rule, one implementation. New `llm_consensus/control_file.py` is the single Python reader (`overlay`, `flag`, `control_block`, `control_dir`), replacing three drifted copies. CONTEXT.md now opens with the rule so future flags follow it.
- **VERIFIED LIVE.** With discovery ON: the funnel answers `{"enabled": true, "due": true, "reason": "last pass 70m ago"}`, and the engine logs `discovery_pass_start (crypto)` plus `discovery_skip (equity, outside US regular trading hours)` with NO flag-mismatch block. With discovery toggled OFF through the real endpoint: the funnel answers `{"enabled": false, "reason": "discovery.discovery_enabled is false"}` and a FORCED pass refuses with `{"status": "disabled"}`. Both halves honor the toggle in both directions. The operator's ON state was restored afterwards.
- Tests + verify. pytest 640 passed (from 619, +21, new `test_control_precedence.py` plus RL gate tests). ctest 24/24. Both root causes mutation-tested. NOT touched: RiskGate, the live-trading gate, the adaptive limit-weakening invariant, or any Level-1 value. Bind stays loopback. Live OFF.

### 2026-07-16 (Opus 4.8) — The research_satellite sleeve toggle existed and did nothing: the prerequisite it feeds was unsatisfiable from the GUI

- Symptom, as reported. The long-term strategy's prerequisite panel demands the research_satellite sleeve be enabled first, and there is no GUI toggle for the sleeve, so it cannot be enabled from the interface.
- The premise was stale, and the truth was worse. The toggle EXISTS (`SleevesPanel.tsx`, `POST /controls/sleeve`, `controls.set_sleeve`, validated and audited), and the operator had already used it: `.control/controls.json` reads `sleeves.research_satellite: true`. It changed nothing, for two independent reasons, and the codebase admitted both.
  1. **The prerequisite ANDed the toggle with the raw CONFIG value.** `longterm_prerequisites` required `sleeve_on AND cfg_on`, and config ships the sleeve off with no endpoint that writes config. So the check was UNSATISFIABLE from the interface no matter what the operator did, and its detail line told them to go hand-edit `sleeves.research_satellite_enabled` in a YAML file. An AND also inverts what a runtime toggle means: it lets the control file only ever turn a sleeve OFF, never on.
  2. **The engine read config, not the control file.** `cfg_.sleeves.research_satellite_enabled` gated both consumers (the maintenance gate in `on_closed_bar`, and `sleeves::satellite_has_room`), so the toggle never reached the sleeve. `api_server/controls.py` said so outright ("The engine reads sleeve enable from config at startup ... engine consumption is a documented follow-up") and the GUI panel told the operator to their face that "the toggle here records intent". That follow-up was never done. It is the same cosmetic-control defect `core/discovery_controls.hpp` and `core/adaptive_controls.hpp` were each written to fix.
- Two more found on the way.
  3. **The sleeves control block was not seeded from config**, unlike every other block. It hardcoded `research_satellite: False` regardless of config, so an operator who enabled the sleeve in config still read off, and the two sources of truth disagreed with no way to tell which won.
  4. **The panel rendered every sleeve percentage 100x too small, with a meaningless sign.** `pct()` is the signed PnL formatter for already-percent values, and the panel fed it FRACTIONS, so the 30 percent target rendered as "+0.30%" and the 35 percent hard cap as "+0.35%". Caught by a new test asserting the target reads as "30%". Added `sharePct()` for shares; `pct()` is untouched, so StatusBar's PnL rendering is unchanged.
- Fixes. New `core/sleeve_controls.hpp` reads the sleeve enable from controls.json seeded from config (off when the file is missing, empty, or malformed: a broken file must never allocate capital to a sleeve nobody turned on). `Engine::consume_sleeves` runs each iteration on both loop paths and REFRESHES `cfg_.sleeves.research_satellite_enabled` in place, because that one field is what both consumers already read, so one write makes the toggle real everywhere without a second source of truth. Seeded at construction too, so the first bar honors it. Every change logs `sleeve_toggle` with the target and the cap. `longterm_prerequisites` now reads the RESOLVED state. The sleeve toggle became an `ArmedToggle`: it allocates capital, so it arms and states the 30 percent target, the 35 percent cap, and the drift-band rebalancing before it fires, under the heading "This allocates capital" rather than the shared "This starts spending" (a confirm that cries wolf trains an operator to skip it). `ArmedToggle`/`PrereqList` moved into the shared `controls.tsx` rather than becoming a third copy.
- **Enable order confirmed coherent (TASK 2).** The sleeve enables independently and has NO prerequisite of its own: verified with both the Finnhub key and the bridge unreachable, the sleeve enable still succeeds, while the long-term strategy correctly refuses. The four-level framework and the bridge are checked at the STRATEGY toggle, which is what needs them. Gating the sleeve on them would make the order circular: the panel says enable the sleeve first, then would refuse to do it without a bridge the sleeve never uses.
- **VERIFIED LIVE.** Against the operator's real state (config off, control file on), the engine banner now reads `satellite ON [controls.json]` where it read `OFF (opt-in)`. A mid-run flip reaches the engine in BOTH directions on a running continuous loop: `Sleeve research_satellite: off -> on (target 30% of equity, hard cap 35%, never exceeded)` then `on -> off (no new satellite positions; open ones exit on their own terms)`. Run on a scratch DB and scratch control dir, so the operator's live state was never touched. Mutation-tested both fixes: reverting the engine to config-only fails the new ctest, and restoring the AND fails two backend tests.
- Tests + verify. pytest 619 passed (from 610, +9). vitest 105 (from 96, +9, new `sleeve-toggle.test.tsx`). ctest 24/24 (from 23, +1, new `sleeve_controls`). Typecheck clean, production build green. NOT touched: RiskGate, the live-trading gate, the adaptive limit-weakening invariant, or any Level-1 value. The hard cap is asserted identical whether the sleeve is on or off: the toggle allocates within the cap, it never widens it. Bind stays loopback. Live OFF.

### 2026-07-16 (Opus 4.8) — The discovery layer was never connected: five breaks, two more underneath, now running live

- Symptom. The operator toggled `discovery_enabled` true, restarted the engine, and nothing happened: no funnel pass, no Finnhub call, no candidate, no new symbol bars. Confirmed at the source before changing anything: the ONLY discovery row the database has ever held is `control_change: discovery.discovery_enabled: False -> True` at 04:36:54Z, the toggle itself. The engine then ran 94 iterations and never once looked at discovery. Zero `discovery_pass` rows, zero watchlist rows, `last_pass_ts` empty.
- The trace found FIVE independent breaks, and no one of them was "the bug". Each alone was fatal, so fixing any one would have changed nothing observable.
  1. **No scheduler.** `discovery.run.run_due` had exactly one non-test caller, `ops/maintenance.maybe_run_discovery`, whose only caller was maintenance's own `__main__` CLI. Nothing ran that CLI: no crontab, and the watchdog, supervisor, and start script never call it. The funnel was correct, tested, and unreachable. Its own docstring said "run it from the existing maintenance scheduling"; that scheduling was never created.
  2. **The engine read the wrong flag.** `core/engine.cpp` gated on `cfg_.discovery.discovery_enabled` (config, ships false) while the GUI writes `.control/controls.json`. Every other runtime control reads controls.json each iteration. Discovery alone never did, so the toggle was cosmetic on the C++ side. `core/adaptive_controls.hpp` already names this defect class and cites discovery as its origin; discovery never got the same fix.
  3. **The watchlist was read once**, at construction, so a symbol surfaced mid-run was unusable until a restart.
  4. **The feed never polled a discovered symbol.** `all_instruments_` is hardcoded to four names and is NOT built from the whitelist, so a merged symbol closed no bars and could never warm or trade. Named, and nothing more.
  5. **Nothing backfilled its bars**, so even a polled symbol started cold.
- Then two more, underneath, that only became visible once the funnel finally ran. Both are one bug: a `state` dict built without the keys the prompt reads. `build_user_prompt` renders exactly symbol/venue/price/ret_5/imbalance/catalyst/volatility and defaults anything missing to 0.0, so the model cannot tell "measured, and zero" from "never fetched".
  - **Stage B** passed the pre-screen's SCORE COMPONENTS, which share one key name with that list (volatility) and no others. Every finalist reached the gate with zero price and zero return, and the gate rejected 12 of 12 on every pass. Proven structural, not a market read: it called a synthetic +14% move with a 14% intraday range a "flat, rangebound setup".
  - **Stage C** passed only symbol and price, and `consensus()` runs the trading gate internally, which skipped every survivor on the same absent order book. So consensus returned a flat verdict WITHOUT CALLING A PROVIDER: the pass recorded 5 council calls that never happened and 5 avoid verdicts nobody had reasoned about.
- Fixes. The engine reads `discovery_enabled` from controls.json each iteration (`core/discovery_controls.hpp`, off when the file is unreadable: this one starts a spender), asks the bridge whether a pass is due, and runs due passes OFF the loop thread (a pass takes tens of seconds; the kill switch is checked at the top of every iteration, so the loop must never wait on one). Cadence keeps ONE authority in `discovery/run.py due()`, asked over `/discovery/due`, so the equities US-hours rule is not written twice in two languages. Onboarding is ADD-ONLY: refresh every iteration, never withdraw, so a pass still cannot move a symbol under an open position, which is the only thing the once-per-run read was actually protecting. `Feed::add_instrument` extends the polled universe without a rebuild. Discovery got its OWN Stage-B gate (`discovery/gate.py`), prompted for the data it actually has, leaving the trading gate untouched: that gate guards real orders and its blast radius is live trading.
- **Finnhub answers an unknown symbol with ZEROS, not an error.** `/quote?symbol=BTC/USD` returns HTTP 200 and `{"c":0,...}`. So all 50 crypto names silently dropped out of the pre-screen and every pass reported `no_data`, looking unlucky rather than broken. Crypto must be asked as EXCHANGE:PAIR; all 50 curated names resolve on BINANCE (verified live).
- **VERIFIED LIVE (crypto, equities closed).** The full funnel now runs end to end: universe 50 -> 12 finalists (Stage A, free, real Finnhub quotes) -> 5 survivors (Stage B) -> real council. Three providers returned real verdicts (gpt-5.5 buy 0.58, opus hold 0.60, gemini hold 0.80 -> consensus buy 0.633), with real DNN advisory (-0.34). NEAR/USD drew a unanimous council sell (bias -0.26, agreement 3) -> conviction 0.577, just under the 0.60 floor -> avoid. **No candidate surfaced, and that is the system working, not failing:** crypto is selling off 3-5%, the native strategies are long-only, and the council read it bearish. The daily budget ceiling also proved itself, cutting a pass to 2 evaluations with 2 calls left of 12. Onboarding verified separately end to end: a watchlist row -> 8,519 real 5-min bars backfilled -> `discovery_onboard: SOL/USD: 300 bar(s) seeded, indicators WARM (tradeable)`, feed extended, warm report agreeing. That row was REMOVED after: the council never approved it, so it must not stay on the traded universe. Both cadence rules fired correctly in the log (`not due (last pass 1m ago, interval 60m)`, `not due (outside US regular trading hours)`), and a bridgeless engine logged `discovery_blocked` loudly instead of returning silently.
- **The suite was already RED before this session started.** Three tests asserted SHIPPED DEFAULTS while reading the operator's live control file (`cfg_path=None` layers controls.json over config by design), so they went red the moment a real operator enabled discovery, reporting a regression that had not happened. Verified pre-existing by stashing every source change. Fixed to read the shipped file, or to pin the flag they claim to test.
- Tests + verify. pytest 603 passed (from 590, +13). ctest 23/23 (from 22, +1, new `discovery_engine`). Build clean. NOT touched: RiskGate, the live-trading gate, the adaptive limit-weakening invariant, or any Level-1 value. Bind stays loopback. Live OFF.

### 2026-07-16 (Opus 4.8) — Finnhub in the live API health check: 429 retry, classified reasons, and the key verified working

- Goal. Add Finnhub to `GET /health/integrations` and the Health view so the operator can confirm the Finnhub key actually works.
- Found it mostly built, and said so rather than rebuilding it. `_check_finnhub` already existed and was already in `_CHECKS` (committed in 9a28212, the Settings-key session). The Health view maps whatever the backend returns, so the Finnhub row already rendered, and `StatusBar`'s aggregate math is integration-agnostic, so a configured-but-failing Finnhub already went amber while a not-configured one already did not count. Tasks 1 and 2 were therefore satisfied except for the parts below. Rewriting working code to match the shape of the request would have been churn.
- Gap 1, a 429 read as a hard failure. The prompt asked for the existing retry-with-backoff and there was none: `_get` raises on a non-2xx, so a rate limit surfaced as failing. The check now retries with the discovery client's OWN policy (`finnhub_source.retry_after_seconds`, bounded, honors Retry-After) rather than a second copy of it. This matters because the check shares the 60/min free tier with a running discovery pass, which is exactly when a 429 is most likely and least meaningful.
- Gap 2, reasons were not classified. A 401 surfaced as `HTTPError: HTTP Error 401: Unauthorized` from the generic handler. The operator needs `bad key` and `rate limited` and `network` to be different, because they call for different actions: re-paste the key, wait, or check the link. Reasons are now fixed phrases.
- Gap 3, the token rides in the URL. Finnhub is the only check that authenticates by query param, and the generic handler stringifies an escaping exception into the `reason` the endpoint returns and the GUI renders. The check now classifies its own failures and lets nothing raw escape. Stated honestly: this is defense in depth, NOT a fix for a demonstrated leak. No realistic exception (HTTPError, URLError) carries the URL in its message today. The guard makes "never returns the key" a property of the code instead of a property of which exception happened to fire, and a test pins it by raising an exception that CONTAINS the key.
- Fixed a test that pinned an unreachable branch. `test_finnhub_health_never_returns_the_key` stubbed `_get` to RETURN 401, but the real transport RAISES on a 401, so it asserted a reason production could never produce. It now raises a realistic HTTPError whose URL carries the token, which is a stronger test of the same property.
- THE KEY WORKS. One real minimal call against the operator's real key returned working, `one quote ok`, 107.8ms, with the key absent from the row. This supersedes the 1947-character key flagged last session, which resolved (so it passed the prerequisite check) while failing every real call. That gap between "a value resolves" and "the API accepts it" is the whole reason this check exists. `parse_quote` is now also verified against a LIVE response (keys c, d, dp, h, l, o, pc, t, parsing to a sane price); the other Finnhub parsers stay fixture-only and unverified.
- Tests + verify. Python pytest 590 passed (up from 585, +5). Frontend vitest 96 (up from 91, +5, a new `health-finnhub.test.tsx` covering the row in each state). Typecheck clean, build green. Mutation-tested both new guarantees: removing the 429 retry fails the retry test, and letting the exception escape fails the leak test with the token visible in the JSON body. NOT touched: RiskGate, live-gate, adaptive invariant, or any Level-1 value. Bind stays loopback. Live OFF.


### 2026-07-16 (Opus 4.8) — GUI controls to enable discovery and the long-term sleeve

- Goal. Discovery and the long-term sleeve shipped disabled behind config flags. Add GUI toggles and tunables through the same validated control-endpoint pattern the existing layer toggles use, so the operator can turn them on from the interface.
- Architecture. The flags were CONFIG-only, so a GUI toggle would have been cosmetic. Added a `discovery` block to controls.json seeded from config, so the shipped default is still disabled and the operator's toggle overrides it at runtime. That is the same precedence feed_mode and clock_mode already use. `discovery/settings.py` now layers controls.json over config, read fresh per call (never cached: the funnel runner is a separate process from the GUI, so a cached value would keep running after the operator turned it off). An explicit cfg_path (the tests) ignores the control file, so a local controls.json cannot leak into a test.
- Task 1, enable toggles. `set_discovery` / `set_long_term` write through the existing validated channel and audit old to new. Each toggle ARMS before it fires, and the confirm states plainly what it starts with real numbers (discovery: hourly passes over the 55 crypto and 119 equity universe, gate calls on 12 finalists, up to 5 council calls, within a 12 call/day budget separate from and additive to the trading budget, at most about $0.48/day; long-term: quality-and-catalyst screening, four levels in long-horizon mode, positions within the 35 percent hard cap it can never exceed, RiskGate still judging every order). Disable is immediate: turning a spender off must never need a ceremony, and is never blocked by a broken dependency.
- Task 2, tunables. Discovery budget, per-stage counts, cadence, and the whale surfacing weight, all through the validated endpoint. Every value is clamped server-side into DISCOVERY_BOUNDS, the narrowing rule is re-applied (survivors <= finalists, council <= survivors) so the GUI cannot build a funnel the config validator would refuse, and the response reports what was clamped rather than silently showing a different number than the operator typed. The API publishes its own bounds so the GUI renders the limits it is actually clamped to instead of keeping a second copy that could drift. An unknown field is REFUSED (extra="forbid"), so a typo cannot look like it worked.
- Task 3, prerequisites. Enabling is refused when a prerequisite is missing, and the refusal says what is missing and where to fix it. Discovery needs a Finnhub key that RESOLVES and the bridge up. The long-term sleeve additionally needs research_satellite enabled, since the strategy otherwise has nowhere to put a position (the config validator already refuses that combination, so the GUI refuses it too).
- Task 4, visibility. Controls shows the toggles, the tunables, last pass per asset class, and budget used against its ceiling. The top strip already carried discovery state; the run-state banner gained discovery and long-term dots plus the budget count. Off reads calm and explains itself, on shows it working.
- Tests + verify. Python pytest 476 passed (up from 463, +13). Frontend vitest 69 (up from 52, +17). C++ ctest 20/20. Typecheck clean, build green. VERIFIED end to end: config says off, controls.json turns it on, `settings.discovery_enabled()` returns True, untouched keys fall back to config, and no control file at all means disabled. NOT touched: RiskGate, live-gate, adaptive invariant, or any Level-1 value (asserted: /risk level1 is byte-identical after every discovery write, and the control file carries no risk key). Bind stays loopback. Live OFF.


### 2026-07-16 (Opus 4.8) — add the Finnhub API key field to Settings

- Goal. The discovery build reads FINNHUB_API_KEY from the keystore, but Settings had no field to enter it. Add one alongside the existing keys using the same masked-credential pattern.
- Root cause, narrower than expected. The backend was already complete from the discovery build: `finnhub_key` was in the credential registry, in `_REQUIRED_FIELDS`, and already returned by `list_status()`, so `GET /credentials` was serving it. The bug was frontend-only: `SettingsPage.tsx` maps credentials into a HARDCODED category allowlist (`CATEGORIES`), and no category claimed the `finnhub` group, so the page silently dropped a credential the API was returning.
- Task 1, the field. Added a **Discovery data** category claiming the `finnhub` group. The existing `CredField` component is reused unchanged, so Finnhub gets the exact same pattern as every other key: masked `type=password` input, dots when set, save through the same validated endpoint and the same encrypted Fernet keystore, never logged, never rendered in plaintext. It already resolved through the shared resolver, so saving it makes the discovery client and the health check see it.
- Root-cause fix, not just the symptom. A hardcoded allowlist means ANY future credential is invisible until someone remembers to list it, which is exactly how this shipped. Added an **Other credentials** catch-all: groups no category claims now render through the same single path instead of vanishing. Implemented by appending a category rather than a second render block, so there is one rendering path and an uncategorized key still saves normally.
- Health check. Finnhub was absent from `api_server/health.py` entirely, so "the health check sees it" was not true. Added `_check_finnhub`, following the Whale Alert pattern: no key reports `not_configured` (a missing optional key is not a fault, and it makes no network call at all, keeping the suite offline), a resolved key makes one minimal real quote call, and a bad key fails honestly by HTTP status without echoing the token.
- Tests + verify. Python pytest 463 passed (up from 457, +6). Frontend vitest 52 (up from 43, +9). C++ ctest 20/20. Typecheck clean, build green. VERIFIED end to end against the real code, not mocks: saved through the encrypted keystore, resolves by credential name AND by env name, masked to dots with `source: in-app`, plaintext absent from the status payload, seen by both `discovery.finnhub_source.resolve_key()` and `health._key()`, and encrypted at rest (the plaintext is not present in the keystore file). NOT touched: RiskGate, live-gate, adaptive invariant, or any Level-1 value. Bind stays loopback. Live OFF.


### 2026-07-16 (Opus 4.8) — whale activity as a candidate-surfacing signal in discovery Stage A, Level 4 evaluation intact

- Goal. Whale data does TWO jobs: it surfaces candidates in the free Stage-A pre-screen, and it still informs the Stage-C verdict as the Level-4 advisory factor. Same data, two questions, deliberate by design. Everything stays behind the existing discovery flags, default off. No trading behavior changed.
- Task 1, surfacing. New `discovery/whale_surfacer.py`. A strong whale signal (institutional accumulation, insider buying, a large crypto exchange outflow) raises an instrument's Stage-A rank, so a name whales moved into can reach the finalist set when price and volume alone would not have surfaced it. Scores evidence times conviction: activity score AND |bias| must both be present, since loud-but-directionless is noise and confident-on-nothing is not a read. Accumulation at full weight, distribution at half (both native families are long-biased in paper, though a sharp exit is still information). Delayed-only 13F evidence down-weighted 0.6, matching the Level-4 posture. Uses the existing sources: no new feed, no new credential, zero LLM cost.
- Task 2, Level 4 unchanged. The Stage-C whale advisory layer is untouched, still capped at 0.35, still informing the verdict and sizing on survivors. Tests pin both that it still moves conviction AND that it stays bounded and cannot flip a verdict. Documented in CONTEXT.md and the README that the two roles are deliberate.
- Task 3, weighting + transparency. `discovery.stage_a_whale_weight` (default 0.15) is operator-tunable without touching code. The five fixed components sum to 1.0 and whale adds on top before normalization, so at 0.15 whale is one sixth of the total: level with sentiment and native, below momentum and volatility. Weight 0.0 is a normalization no-op that restores the exact pre-whale ranking, pinned by a test that recomputes the old formula by hand. Whale-surfaced candidates are TAGGED, and the tag is a COUNTERFACTUAL: Stage A ranks the universe twice (with and without whale) and tags only names that would NOT have made the cut otherwise, so the tag means something.
- Task 4, GUI. The discovery funnel view shows a whale-surfaced count and states the two-jobs design so the tag does not read as a duplicate. The watchlist shows a whale tag and carries the attribution in the reason ("surfaced by whale accumulation"). Read-only, no new write path.
- Cost bounds. "Already-fetched" means the existing sources, not a DB cache: whale_activity is written only by ops/demo.py and is empty in practice. So the surfacer fetches through the existing adapter chain with a hard 6h TTL cache (13F lags ~45 days, Form 4 ~2 days, so an hourly re-fetch buys nothing) and a 60-per-pass fetch budget bounding load on SEC EDGAR. Past the budget a symbol scores 0 and ranks on technicals as before. Any failure degrades to 0, never a wrong number.
- Tests + verify. Python pytest 457 passed (up from 422, +35 across tests/test_discovery_whale.py). Frontend vitest 43 (up from 39). C++ ctest 20/20. Typecheck clean, build green. VERIFIED end to end with mocked whale data: a name ranked third of three on technicals was surfaced by whale accumulation, displaced the second, was tagged with its reason, persisted, and carried the attribution to the watchlist, while a technically-found name was correctly NOT tagged. Also verified the other direction: whale at the default weight CANNOT rescue a dead-flat name from behind ten trending names, which is the "does not dominate" requirement working. NOT touched: RiskGate, live-gate, adaptive invariant, the 0.35 whale sizing cap. Live OFF.


### 2026-07-16 (Opus 4.8) — discovery funnel, watchlist, and long-term sleeve GUI views, read-only

- Goal. Add GUI views so the operator can see the discovery funnel, the dynamic watchlist, and the long-term sleeve the discovery build added. Read-only, times in the operator's local timezone, consistent with the existing timezone work. No trading behavior changed.
- Task 1, backend. Five read-only endpoints on the loopback api_server, all GET, all reading through the existing `mode=ro` connection: `/discovery/latest` (most recent pass per asset class with per-stage counts plus every drop with its stage and reason), `/discovery/candidates` (Stage-C survivors with their four-level verdicts and advisory sizing), `/watchlist` (the active list with why each instrument is on it and its sleeve target, plus recent adds and prunes), `/longterm/positions` (open research_satellite positions joined to their persisted theses), and `/discovery/state` (the summary the top strip and sleeve panel read). New `store.discovery_latest/discovery_candidates/watchlist/watchlist_events/longterm_positions` and `controls.discovery_state/discovery_enabled/longterm_state/discovery_used_today`. No POST exists on any discovery path, so there is no new write path at all.
- Task 2, funnel view. The latest pass per asset class drawn as a funnel: universe, Stage A finalists, Stage B survivors, Stage C evaluated, bar width proportional to count so the narrowing is visible without reading a number, and each bar labelled with what that stage spends (0 tokens, gate calls, council calls). Bars run cool to warm top to bottom so the colour IS the cost. Cost this pass shows against the separate discovery budget, and every drop is grouped by stage with its reason.
- Task 3, watchlist view. Each instrument with why it is on the list, added date, last-confirmed date (the staleness clock), sleeve target rendered as a plain word, score, and status, against the list cap. A recent adds and prunes feed makes the list visibly alive. A REFUSED event from the reserved `adaptive_react` source renders as REFUSED rather than being hidden.
- Task 4, long-term view. Distinct from the quant core: each position renders its full thesis readable, not truncated into a cell. Direction, conviction, target, horizon, invalidation condition, entry date, current PnL, and where the position sits against its thesis (`on thesis` / `target reached` / `invalidated`, computed from stored numbers only, never deciding an exit). A thesis predating the long-term strategy carries no levels, and the view says so rather than inventing a target. Plus a research feed of recent theses.
- Task 5, dashboard integration. Discovery state (on/off, watchlist size, last pass) in the top strip, polled slowly since a pass runs hourly at most. The sleeve panel gained a discovery line (state, watchlist vs cap, last pass, discovery budget used today, and that both sleeves draw from the watchlist) plus a note that the satellite target is a ceiling, not a floor. Three sidebar links, grouped after Controls because they are read surfaces.
- Task 6, tests. Frontend vitest 39 passed (up from 17): each view in BOTH the disabled and populated states, the funnel counts and per-stage cost labels, drop reasons by stage, the refused react event, the full thesis, the NULL-levels case, and timezone rendering (02:00Z renders 7:00 PM PDT). Backend 100 passed in test_api_server.py: funnel narrowing, drops with stage and reason, watchlist reasons, the full thesis, the three distinct long-term booleans, an absent-tables DB rendering empty rather than 500, a DB-hash check proving the views write nothing, a route scan proving no discovery path exposes a non-GET method, a canary proving no key value reaches a response, and that a view cannot change the live posture. Typecheck clean, production build green (310 kB). No network in any test.
- Verify. Python pytest 422 passed (up from 405), ctest 20/20, vitest 39, build green. Bind still loopback, store still `mode=ro`. One real bug found and fixed by the tests: `discovery_latest` ordered by `id DESC`, assuming insertion order equals chronological order; a deliberately inverted seed caught it, and the query now orders by `ts DESC, id DESC`. NOT touched: RiskGate, live-gate, adaptive invariant, engine, config. Live OFF.

### 2026-07-16 (Opus 4.8) — discovery funnel with Finnhub, dynamic watchlist, 70/30 sleeves, long-term research strategy, all disabled by default

- Goal. Build the discovery engine and the long-term sleeve strategy: a curated universe screened hourly by cheap signals into a small candidate set, the gate and the four-level framework vetting the finalists, verdicts feeding two sleeves at 70/30, and the 30 percent sleeve running a long-term quality-and-catalyst-plus-council strategy. Everything ships DISABLED behind flags so the running paper config is unchanged. The real-time news-react adaptive layer is explicitly OUT OF SCOPE and deferred.
- Task 1, Finnhub. New `discovery/finnhub_source.py`: quotes, company news, pre-computed news sentiment, basic fundamentals, analyst ratings, earnings calendar. The key resolves keystore-first (`finnhub_key` added to the credential registry, env `FINNHUB_API_KEY`), never hardcoded, never logged. 60-calls/min sliding-window rate limiter, and the same bounded 429 backoff the whale adapter uses (2 retries, 1s base, 5s cap, honor Retry-After). Per-endpoint TTL cache (quote 30s, fundamentals/ratings 6h). Pure parsers kept separate from transport. FIXTURES ARE SYNTHETIC: no key resolves here, and the host IS up (an unauthenticated probe returned HTTP 401 "Please use an API key"), so the blocker is a missing credential, not a dead host. Logged in `tests/fixtures/finnhub_SYNTHETIC.json` and CONTEXT.md.
- Task 2, universe. Config-driven in the new `discovery:` block: 55 curated crypto refreshed daily to the active 50 by liquidity and dollar volume from the `bars` table, and 119 stable curated equities (large caps plus liquid ETFs). Documented as the OUTER EDGE of the funnel, liquid names only. Absent volume evidence keeps config order rather than reading as zero liquidity.
- Task 3, funnel. New `discovery/funnel.py`. Stage A free pre-screen (price, volume, volatility, momentum, gap, Finnhub sentiment, native technical) over the whole universe to 12 finalists, ZERO LLM tokens. Stage B Haiku gate on finalists only to 5 survivors. Stage C four-level evaluation (council + DNN advisory + whale, reusing the existing layers) on survivors only, producing buy/sell/avoid with sizing and a rationale. Hard ceilings per stage plus a daily discovery council budget (12/day) SEPARATE from and ADDITIVE to the trading budget. Config validation enforces that the funnel narrows: survivors <= finalists, council calls <= survivors. Every stage's counts and every drop with its stage and reason persist. Cadence: crypto hourly 24/7, equities at the US open and hourly through US hours only.
- Task 4, watchlist. New `discovery/watchlist.py`, event-sourced. Discovery adds Stage-C survivors, prune removes on staleness (48h) or a broken thesis, capped at 40 on score. Every mutation goes through one `apply_event` with an explicit source, journalled to `watchlist_event`. `adaptive_react` is RESERVED and REFUSED (`source_not_enabled`), the seam the deferred react layer uses without a rewrite. Both sleeves draw from the list.
- Task 5, 70/30. `sleeves.quant_core_target_pct` 0.70 / `research_satellite_target_pct` 0.30, up from 80/20. The mechanism is UNCHANGED: the hard cap is still (target + band), now 35 percent, `satellite_has_room` still refuses mechanically, and drift-band rebalancing still trims through the RiskGate-approved exit path. 30 percent is a CEILING, not a floor. The sleeve still ships OFF, so no capital moves today.
- Task 6, long-term strategy. New `research_satellite/long_term.py`: a free quality screen (ROE, margin, growth, sane P/E) AND a catalyst (earnings inside 21d, a strong sentiment shift, or an analyst upgrade), BOTH required. Survivors get the full four levels in long-horizon mode, producing direction, conviction, target, horizon, and an invalidation condition, persisted with the position (`research_thesis` gains nullable target / invalidation_price / invalidation / entry_price via the existing additive-migration path). SAFEST DESIGN: `research_satellite/research.py` DISPATCHES on the flag, so the existing bridge `/research/thesis` endpoint and the existing tested C++ satellite path (conviction threshold, hard cap, RiskGate, no time stop) consume it unchanged. A thesis may only TIGHTEN a stop, never widen it, and the levels derive deterministically from the 52-week range so a model cannot hallucinate a level that widens risk.
- Task 7, disabled by default. `discovery_enabled` and `long_term_sleeve_enabled` both false. The startup block prints discovery state, universe sizes, funnel ceilings, the split, each flag, and that the react layer is not built. VERIFIED with a counterfactual: flags off, a 12000-step synthetic run produced 272 trades / 136 closed on the same 4-symbol whitelist as before, with zero discovery passes, zero watchlist rows, zero watchlist reads. Flag on, a seeded watchlist correctly merged 2 symbols into the traded universe (`whitelist_size 6`, logged `discovery_watchlist`).
- Task 8, out of scope. The news-interpretation-and-react layer is NOT built. Documented in CONTEXT.md and a new LIVE_READINESS.md section: discovery uses Finnhub PRE-COMPUTED sentiment as a cheap number only, no live LLM news interpretation, no autonomous event-driven entry or exit, every entry routes through the full funnel and the RiskGate, never a raw headline.
- Tests + verify. C++ ctest 20/20. Python pytest 405 passed (up from 289, +116 new across `test_finnhub_source.py`, `test_discovery_funnel.py`, `test_discovery_watchlist.py`, `test_long_term_sleeve.py`). Mocked Finnhub and mocked council throughout, no network. A Python/C++ default-parity test reads `config/config.hpp` and asserts `discovery/settings.py` mirrors it, so the two declarations of one contract cannot drift. NOT touched: RiskGate, live-gate, adaptive invariant. Live OFF, RL gated.

### 2026-07-15 (Opus 4.8) — add daily week-review digest and week-end summary, reporting layer only

- Goal. A week-review report file WEEKLOG.md at the repo root, appended daily by an automated job, that distills the day's trading evidence from the database into a structured readable digest. At week end the operator runs a summarize command and hands the one file to a reviewer for calibration analysis. Raw data stays in the database, unchanged. No trading behavior changed.
- Daily digest. New ops/weeklog.py, read-only over the SQLite DB (opened mode=ro), appends one dated section per run summarizing the prior 24 hours, timestamps shown in both UTC and America/Vancouver. Each section covers trades (by sleeve and symbol, entries and exits, win rate, gross and net PnL after fees, average hold via FIFO entry/exit pairing, best and worst with their entry reasons), blocks and near-misses (risk_block by reason plus a near-miss table of confidence blocks within 0.10 of the floor with symbol, confidence, agreement, tier, council_ran, the calibration evidence), council and cost (calls vs budget, gate skips by reason, per-provider verdicts and errors, est spend day and week), sleeves (allocation vs the 20 percent satellite cap, rebalance events, per-sleeve PnL, research theses with conviction and outcome), sessions (crypto trades and PnL tagged Asia/London/NY by UTC window), health (engine starts/stops, watchdog restarts, kill-switch changes, DNN challenger attempts, RL fills vs the 500 gate), and anomalies (empty payloads, unparseable verdicts, feed staleness, repeated provider failures).
- Week-end summary. python -m ops.weeklog --summarize appends a week-summary aggregating the 7-day window: totals, the full near-miss table, the pre-registered success-criteria checklist from CONTEXT.md marked met / not met / review from the data, and open calibration questions the numbers raise.
- Wiring. ops/maintenance.py runs the daily digest alongside the backup job (append_weeklog in its daily __main__), failure-isolated so a digest error never breaks maintenance. WEEKLOG.md is committed with a header explaining its purpose plus the first daily section generated from the current database.
- Safest-choice notes. The current DB has empty-payload risk_block events from before the confidence-logging fix; the digest surfaces those as an anomaly and shows near-misses only from blocks that carry real numbers (one real ETH/USD near-miss in the last 24h). Qualitative success criteria (uptime, research quality, discipline) are marked "review" with the supporting numbers rather than a false met/not-met, since they are not a numeric bar. A disabled satellite (no sleeve snapshots) reports "satellite off", not a false drift.
- Tests + verify. Python pytest 289 passed (6 new weeklog tests on a seeded DB: correct counts and PnL, near-miss band inclusion only, criteria marked correctly against thresholds, append without clobbering prior sections, no key or credential in the file). The --summarize command verified end to end against the real DB. Read-only over the database, no network. NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, any trading behavior. Live OFF, RL gated 240/500.

### 2026-07-15 (Opus 4.8) — display GUI timestamps in the operator local timezone, storage stays UTC

- Goal. The React GUI now shows every timestamp in the operator's local timezone (default America/Vancouver, PST or PDT by date) instead of raw UTC. Display-only: storage, the engine, logs, and the events table stay ISO-8601 UTC.
- Shared utility. Added web/src/api/tz.ts, a small display-timezone store (default America/Vancouver, persisted in localStorage, a useSyncExternalStore hook, DST handled by the IANA zone name not a fixed offset). The existing shared formatters web/src/api/format.ts (clockTs, shortTs) now render in that zone with a short label like "7:45 PM PDT", reading the zone from the store so no component hardcodes it. Every wall-clock display in the app already flows through these two formatters (activity feed, trade tables and detail, skip feed, positions), so routing them covers the app.
- Reactivity + selector. Layout subscribes to the zone so a change re-renders every page live. Settings gained a display-timezone selector (defaults to America/Vancouver, persists the choice, writes no operational value, shows a live "now" preview).
- Safest-choice notes. HealthPage has no checked_at field (only latency_ms, a duration, not a wall-clock) and EquityChart uses an index-based x-axis with no absolute time labels, so neither renders a timezone-relevant timestamp. There was no existing localStorage settings store, so this display-only preference persists in localStorage, which never reaches the engine, YAML, or the DB.
- Tests + verify. Frontend vitest 17 passed (6 new: a known UTC converts to Vancouver PDT in summer and PST in winter, shortTs carries the date and zone label, the default zone is America/Vancouver, a subscribed component renders the converted time and re-renders when the zone changes to UTC, an invalid zone is ignored). Typecheck clean (tsc --noEmit), production build green (tsc -b + vite build, 74 modules). The diff touches only the frontend (web/) and this doc. No backend, engine, or DB change. NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant, timestamp STORAGE (stays UTC). Live OFF, RL gated 0/500.

### 2026-07-15 (Opus 4.8) — gate all equity entries on US market hours, fast tier included, exits exempt, crypto unaffected

- The bug (from live logs). At 23:40 UTC, outside US regular hours, the market-hours rule skipped the council for QQQ but a fast-tier native momentum entry executed anyway. The market-hours gate only gated the council call, not the native entry, so after-hours equity fills slipped through. After-hours paper fills are thin-market artifacts that corrupt validation data.
- The fix. Added one pure helper util::equity_entry_blocked_by_market_hours(enabled, category, now) next to us_equity_market_open, so the entry decision lives in one named function reused by the engine and the tests. Wired it into core/engine.cpp on_closed_bar in the ENTRY path, right after the venue-capability gate and before any sizing, RiskGate, or council work: outside US regular hours an equity takes NO new entry (fast tier and council tier both, the gate sits before the tier decision), logs one market_hours_entry event, and returns. It keys off the simulated bar time under clock_mode simulated and wall-clock otherwise.
- Exits exempt, crypto 24/7. The exit path runs earlier in on_closed_bar and never consults the gate, so an open equity position always closes and is never trapped. The gate is equity-only, so crypto keeps entering 24/7. Removed the old Cut B council-only market-hours skip (the entry now returns before it), so there is no council-skip-then-executed-trade, and no per-iteration spam (the gate fires only when a native entry signal exists).
- Tests + verify. C++ ctest 20/20 (new market_hours_entry: a pure-helper suite for the block/allow/crypto-any-hour/flag-off/simulated-time cases, a crafted replay that opens SPY in-hours @130.70 then executes its stop-loss exit OFF-hours @117.04, and a synthetic run that shows equity entries drop from 107 with 81 off-hours (gate off) to 27 with 0 off-hours (gate on) while crypto is unchanged and 191 clean market_hours_entry skips log). Python pytest 283 passed (C++-only change). Updated test_tuner_floor to run 12000 steps (was 5000) so its no-plateau assertion still holds now that off-hours equity fills are correctly removed. NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant. Live OFF, RL gated 0/500.

### 2026-07-15 (Opus 4.8) — scaffold global-session equity rotation, gated on venue capability, disabled pending IBKR global access

- Regional session model. New config-driven model (config/regional_session.hpp, pure header) with three regions: NY, London, Asia, each with an exchange id, a tz label, session hours as minutes since UTC midnight, a venue_available flag, and a per-region equity whitelist placeholder. Parsed from a new global_sessions config block. Structured as a venue mapping, so adding IBKR global routing later is config plus an adapter mapping, not an engine rewrite.
- Venue-capability gate (THE safety rule). The engine only evaluates and trades an equity region a connected venue can reach. In on_closed_bar, for an equity, region_for_equity maps the symbol to a region and venue_available_for checks capability; if no capable venue, the entry is refused before any adapter and logged venue_unavailable_for_region. Today only NY (Alpaca US equities) is reachable, so only US equities trade during US hours, exactly as now. London and Asia are venue_unavailable. The rule holds whether or not rotation is enabled. Crypto is never gated, it trades 24/7 (the gate is equity-only).
- Rotation flag, disabled. global_equity_rotation_enabled default false. No live rotation behavior beyond the disabled scaffold and the venue gate. The config and docs state that enabling requires IBKR global access, per-region whitelists, and the flag on, and it stays off until the operator is deliberately live on IBKR.
- Startup and status. The startup block prints the current global session, the rotation state (DISABLED scaffold), and which equity region is tradeable now (NY tradeable via Alpaca, London and Asia venue-unavailable), so it is always clear only US equities trade and the machinery is dormant.
- IBKR readiness. execution/ibkr_adapter.py documents IBKR as the venue that unlocks rotation and what is needed. New LIVE_READINESS.md records the same, the safety rule, and that the validation week stays US-equities-plus-crypto unchanged.
- Tests + verify. C++ ctest 19/19 (new test_regional_session: rotation disabled default, only NY tradeable, an injected non-US equity is refused by the venue predicate, simulated-time session detection at 02:00/09:00/14:00 UTC, crypto in no equity whitelist). Python pytest 283 passed (unchanged). End-to-end proof: a bounded synthetic_regimes run with SPY mapped into the Asia region logged venue_unavailable_for_region 77 times for SPY and SPY never traded, while crypto (BTC/USD) traded 20 times unaffected. Under the default config SPY maps to NY and trades. NOT touched: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant. Live OFF, RL gated 0/500.

### 2026-07-15 (Opus 4.8) — wire Whale Alert crypto whale adapter for a one-time trial evaluation, opt-in and capped at 0.35

- Adapter. WhaleAlertAdapter (whale_signal/adapters.py) now follows the SEC EDGAR pattern: a pure _parse method over the uniform Whale Alert transactions schema (hash, blockchain, symbol, amount, amount_usd, from/to owner + owner_type, timestamp), so fixtures test parsing with no network. The key resolves keystore-first via WHALE_ALERT_API_KEY, never hardcoded, never logged. The developer plan 10 req/min limit is handled with bounded retry-and-backoff on 429 (honoring Retry-After), then a clean degrade to the deterministic mock.
- Scoring. Parsed transactions feed the SAME whale scoring path SEC EDGAR uses. The transparent heuristic reads owner_type: a transfer TO an exchange is an inflow (selling pressure), FROM an exchange is an outflow (accumulation), size-bucketed by amount_usd. The whale factor now has two active sources, SEC EDGAR for equities and Whale Alert for crypto, combined under the unchanged 0.35 advisory cap.
- Config and gating. New flag whale.whale_alert_enabled, default false, so the system runs unchanged. When true AND the key resolves, WhaleAlertAdapter joins default_adapters for crypto. Enabled without a key, or disabled, leaves the chain SEC-only (no crypto mock injected), so nothing changes silently. The flag exports to the bridge as WHALE_ALERT_ENABLED (stack.whale_env). The bridge /status and the C++ startup block print the Whale Alert feed state alongside SEC EDGAR.
- Health. GET /health/integrations gained a real Whale Alert check: when enabled and keyed it makes one minimal call reporting working or the failure reason with latency, off or unkeyed reports not_configured, the key is never logged.
- Fixture. Recorded a REAL Whale Alert response from one live 200 OK call this session (20 transactions), trimmed to 6 (tests/fixtures/whale_alert_transactions_sample.json) covering exchange inflow (BTC/ETH to Binance), outflow (USDT from Binance), and unknown transfers. No SYNTHETIC fallback was needed.
- Tests + verify. Python pytest 283 passed (up from 274, 9 new: parse the real schema, the inflow/outflow heuristic reads owner_type, size buckets read amount_usd, a 429 retries then degrades cleanly with an injected fake requests, the 0.35 cap holds and both sources combine, an absent key reports not live without raising and is excluded from the chain, off by default excludes it, a non-crypto symbol is ignored). No real network in the suite. C++ ctest 18/18 (startup line change only). NOT touched: RiskGate, live-trading gate, adaptive invariant. Live OFF, RL gated 0/500.

### 2026-07-15 (Opus 4.8) — log real confidence numbers on every block, fix the fast-tier native confidence miscompute

- Verdict: DEFAULT-LOW / miscompute for crypto, GENUINE-LOW for the equity setups seen. The fast-tier native confidence was NOT genuinely low for BTC/ETH. It was structurally miscomputed. A fast-tier entry deliberately skips the council, so the three LLM slots hold only neutral in-process mocks (~0.5). The gate confidence blended those un-consulted mocks in, and with the native rule_based capped at its floor share (0.35), a genuine native conviction (0.7+) got dragged below the RiskGate min_confidence floor (0.65). Every fast-tier native entry was structurally blocked with "confidence below min_confidence_default", and the block event logged an empty payload so the real number was invisible.
- Fix. compose_gate_verdict gained a council_ran flag. When the council did NOT run (fast tier, spend-ceiling or market-hours skip, all providers off), the gate confidence/edge are recomposed from the factors that actually produced a signal (native rule_based + real advisory dnn/whale), excluding the three neutral council mocks. Bias, verdict, and agreement stay from the full set, so the fix only stops the un-consulted mocks from dragging confidence down. It never eases agreement and never touches the RiskGate or its thresholds. The council tier (council_ran true) is unchanged, equal to the full-ensemble combine. The 0.65 floor was NOT changed.
- Diagnosable blocks. Every risk_block event now carries real numbers in payload_json: confidence, min_confidence, edge, min_edge, agreement, required_agreement, tier (fast/council), council_ran, factor, symbol, notional, reason, layer. This covers the native path, the legacy bootstrap-sim path, and the research-satellite entry. No block logs an empty payload.
- Verification. Bounded synthetic_regimes run under active_quant (6000 iters, no crash): 6 native fast-tier trades executed at confidence 0.662 to 0.678 (a sufficient-confidence native signal now passes the fast-tier check), 0 empty-payload blocks. Traced fast-tier confidence: BTC/USD 0.7192, ETH/USD 0.7128, both now ABOVE the 0.65 floor (the confidence block is resolved for crypto). Those two now block only on agreement (1 real factor vs 2 required, a separate and legitimate RiskGate check on a lone reversion signal with no advisory agreement, left untouched). SPY 0.6448/0.6466 and QQQ 0.6216 are genuinely just below the floor (the equity advisory read was weak), correctly still blocked on confidence, now with real numbers. Unit test test_fast_tier_confidence codifies the controlled before/after: the full blend at 0.645 is below the floor (would block) while the council-skipped recompute at 0.73 clears it, a genuinely weak advisory read still blocks (no forced trades), and council_ran true equals the full combine.
- Tests + verify. C++ ctest 18/18 (new test_fast_tier_confidence). Python pytest 274 passed (unchanged, no Python change). NOT touched: RiskGate logic, live-trading gate, adaptive limit-weakening invariant. No key value logged, bridge stays loopback. Live OFF, RL gated 0/500.

### 2026-07-15 (Opus 4.8) — add watchdog + notifications, nightly backups, growth safeguards, scheduled DNN challenger training, week config + success criteria

- Crash watchdog. ops/watchdog.py runs as a separate process (launched by the start script, stopped by the teardown trap). Every few minutes it checks engine/bridge/backend health + crypto bar staleness, attempts ONE clean restart via the supervisor on a failure, and notifies via ntfy.sh either way (topic from config, component status only, never a key or position). It never touches the kill-request file, and a kill-switch trip is notified but never auto-resumed (manual resume stays required).
- Nightly backups. ops/backup.py takes a consistent sqlite online-backup snapshot into a gitignored backups/ dir with dated names + retention (default 14), restore-verified by counting trades rows. backups/ and logs/ are gitignored.
- Growth safeguards. ops/maintenance.py prunes informational events older than a window, never deleting trades, positions, bars, or audit-relevant events, and estimates events/day for the week-size projection. The week projects to a few MB of events, bounded by the prune.
- Scheduled DNN challenger. ops/maintenance.maybe_train_challenger runs the real-data challenger daily, refusing cleanly below the 200-sample minimum. Promotion stays gated + MANUAL: the existing GUI registry surface (champion vs challenger with validation metrics + a can_promote flag gated on meets_promotion_criteria) shows a waiting challenger for the operator's deliberate promote. Auto-promote stays off.
- Week config. stack.materialize_week_config writes a COMPLETE week config (both sleeves 80/20, active_quant, all advisory layers on-real, feed alpaca_paper, clock real, live OFF, RL OFF) by layering the week overrides over default_config, which stays conservative. CLI: python -m api_server.stack week-config. The C++ startup block renders it correctly (verified).
- Success criteria. CONTEXT.md gained a pre-registered Success Criteria section (min closed fills, drawdown within Level-1, sleeve split holds within band unattended, combined spend at/under $100/month, uptime target with watchdog restarts counted, research judged on thesis quality with the option to stay capped or be cut, mid-week changes limited to trading-blocking defects).
- Tests + verify. Python pytest 274 passed (up from 262, 12 new ops tests: watchdog detects a dead engine + stale feed, attempts one restart, notifies both outcomes, never auto-resumes a kill trip, never touches the kill-request file; backup restorable + retention prunes; prune protects audit + tables; challenger refuses/reports without auto-promote). C++ ctest 17/17 unchanged (no C++ change this session). NOT touched: RiskGate, live-trading gate, adaptive invariant. Live OFF, RL gated 0/500.

### 2026-07-15 (Opus 4.8) — add core-satellite hybrid: quant core + LLM-research satellite, hard satellite cap, drift-band rebalancing, sleeve GUI

- Two sleeves. quant_core (the systematic RSI-2 + momentum stack) and research_satellite (LLM deep-research, fewer/larger/longer-held positions). Config split 80/20, drift band 5 percent. research_satellite ships OFF by default (operator opt-in), so the default quant loop is untouched.
- Hard cap enforced in code. core/sleeves.hpp (pure, unit-tested): satellite_has_room refuses any satellite entry that would push the sleeve past target + band, a research conviction can never override it. satellite_cap_value = (target + band) * equity. Verified: an over-conviction position asking for 100k against a 25k cap is refused.
- Rules-based rebalancing. decide_rebalance trims the OVERWEIGHT sleeve back to target when it drifts past the band, on a drift trigger and a scheduled cadence, through the normal RiskGate-approved exit path (never a bypass), logged with before/after allocations.
- Sleeve tagging + accounting. positions.sleeve and trades.sleeve tag every position (default quant_core, tolerant migration). Per-sleeve snapshots persist to a new sleeve_history table. The engine computes per-sleeve allocation from open positions and enforces the cap.
- Research decision path. research_satellite/research.py runs the council (gate-screened for cost) and returns a structured thesis (direction, conviction, horizon, rationale), persisted to research_thesis. The bridge exposes /research/thesis. The engine, when the satellite is enabled and the bridge is up, opens a satellite position only above the conviction threshold, under the hard cap, and past the RiskGate.
- Cost control. A separate research_daily_budget plus a hard combined monthly spend ceiling (counts quant council + research) pauses both sleeves when reached, sized so a month stays near or under 100 dollars.
- GUI. Backend read endpoints (/sleeves, /sleeves/history, /research/theses) + validated control writes (/controls/sleeve toggle, /controls/rebalance). A React SleevesPanel shows the split vs target, the band, a rebalance-due flag, per-sleeve toggles, a rebalance-now button, and the research thesis feed.
- Tests + verify. C++ ctest 17/17 (new test_sleeves: hard cap holds against over-conviction, drift rebalancing trims correctly, config validates). Python pytest 262 passed (up from 250, 12 new: research thesis + budget/ceiling + sleeve API endpoints, no key, loopback). Frontend typecheck + build + 11 render tests. Bounded offline run with the satellite enabled: startup shows both sleeves at 80/20 with the 25 percent hard cap, the loop runs without a crash, per-sleeve snapshots persist. RiskGate, the live-trading gate, and the adaptive limit-weakening invariant untouched. Live OFF, RL gated 0/500.
- Follow-up (documented): the engine's live research-satellite entry needs the bridge + provider keys (not run unattended to avoid API spend); the manual rebalance-now control-file consumption in the engine mirrors the kill-request pattern and is a documented follow-up (the automatic drift/scheduled rebalance is wired).
- Next: the queued week-run prep (watchdog, backups, DNN challenger schedule, week config).

### 2026-07-15 (Opus 4.8) — add evidence-backed RSI-2 + momentum quant stack, regime-driven blend, active_quant profile, two-tier cost-bounded execution

- New native factors. Added a Connors RSI-2 mean-reversion factor (signal_engine/strategy): long only, only above the 200-period trend MA (dips bought inside an uptrend), RSI-2 below a config entry threshold (crypto 10, equity 5), an optional cross-back confirmation, an ATR volatility band (ATR within atr_band_std of its atr_mean_period mean), and a volume filter. It exits on the RSI-2 cross above rsi2_exit, a new native ExitReason::Indicator the engine computes from bar history, plus the RiskGate stops and the ATR target. Momentum gained an optional dual-MA trend filter (price above a medium and a long MA, plus a positive lookback return) for trending regimes. All parameters are config.
- Regime drives the blend, active factor persisted. The regime detector still selects the leading factor (trending->momentum, range-bound->reversion, neutral->blend). The engine now persists the regime AND the selected active factor per symbol (regime_state.active_factor, additive column + tolerant migration) for the GUI.
- Profiles. strategy.profile defaults swing (the current EMA/Bollinger stack, unchanged, so nothing changes silently). active_quant overlays the evidence-backed set: RSI-2 reversion, dual-MA momentum, a wider liquid whitelist (BTC/ETH/SOL, SPY/QQQ/AAPL/MSFT/NVDA), tighter tiers, budget, cooldown, and spend ceiling. Timeframe stays 5min so the 5Min Alpaca backfill still warms the 200-period filter; cadence rises from RSI-2 frequency and the wider whitelist, noted as the safe choice.
- Two-tier, cost-bounded. Small, low-conviction entries take the FAST tier (native + RiskGate, no council); larger or higher-conviction entries take the COUNCIL tier. A hard spend ceiling (daily/monthly USD) forces the fast tier when reached, logged council_skip reason spend_ceiling. active_quant sizes the budget (40/day) and ceilings ($5/day, $100/month at ~$0.04/call) so a month projects to about $20 to $48, well under $100. Both are pure cost controls that can only skip spend.
- Crypto stop. Crypto uses a wider volatility stop (crypto_atr_stop_mult, default 2x); equities keep the existing ATR stop. All stops stay inside the RiskGate authority. No Level-1 limit changed.
- Startup + Python surface. The startup block now states the profile, timeframe, active factors, regime thresholds, RSI-2 parameters, tier thresholds, whitelist, budget, and spend ceiling. config_access gained a profile getter, the active_quant overlay, and spend-ceiling helpers that mirror the C++.
- Tests + verification. Python pytest 250 passed (up from 243, 7 new active_quant/spend-ceiling tests). C++ ctest 16/16 with ~31 new strategy/tier/spend checks (RSI-2 entry/exit/cross-back/vol/volume/trend filters, momentum dual-MA, regime factor selection, evaluate() blend selecting RSI-2, decide_tier, spend_ceiling_reached, config profile switch + validation). Bounded offline verification: the engine runs the active_quant profile end to end over 24000 synthetic bars without a crash and persists regime + active factor. The RiskGate, the live-trading gate, and the adaptive limit-weakening invariant were untouched. Live stays OFF, RL gated 0/500.
- Next: the queued core-satellite hybrid (quant core + LLM-research satellite sleeve).

Newest entries at top. One entry per session. Format: date, model used, what changed, what is stable, what is next.

### 2026-07-14 (Opus 4.8) — fix council verdict parsing for real provider response shapes, force structured output, resolve the unparseable-output stall

- **Raw shapes captured (not guessed).** One real call per provider showed: gpt-5.5 clean JSON, claude-opus-4-8 clean JSON, gemini-3.1-pro-preview a valid object followed by a stray extra "}" (double brace). A follow-up capture showed Gemini also DROPS the closing "}" sometimes (finishReason STOP, thoughts ~575 to 741 tokens, total under 1100, well under the 2048 cap, so NOT truncation). The old parser sliced the first "{" to the last "}", so both Gemini quirks produced invalid JSON and an unparseable verdict.
- **Parser fix.** llm_consensus/http_json.extract_json_object now decodes the FIRST complete JSON object at each "{" with raw_decode (ignoring a stray trailing brace or prose), prefers the verdict-shaped object (so a thinking model's incidental object in its reasoning is skipped), and repairs an unterminated object (closes an open string, drops a dangling comma, appends missing braces) when nothing decodes cleanly. A genuinely broken body still returns None and the provider flat-falls-back with source error.
- **Structured output.** OpenAI already uses response_format json_object, Gemini already uses response_mime_type application/json. Anthropic: claude-opus-4-8 does NOT support an assistant-message prefill (HTTP 400), so the "{" prefill trick is unavailable, the request keeps the strict JSON instruction and relies on the robust parser. The token cap was NOT raised (truncation was ruled out).
- **Before and after.** Before: the Gemini stray/missing-brace shapes were unparseable (source error), Opus intermittently failed too. After: gpt-5.5 4/4, claude-opus-4-8 4/4, gemini-3.1-pro-preview 4/4, so 12/12 real across a 4-calls-each run, and a full council through the bridge returns verdict strong_buy, agreement 3, all three per-model sources real, with zero broken-pipe/unparseable lines.
- **Diagnostic gated.** A masked raw-text log (env MAL_COUNCIL_DEBUG, off by default) prints on a parse failure, never a key value. The capture scripts were throwaway (scratchpad), not committed.
- **Stable:** Python pytest 243 passed (6 new parser/shape tests with recorded real fixtures), C++ and frontend untouched this session. RiskGate, the live-trading gate, and the adaptive limit-weakening invariant untouched. RL gated (0/500), live OFF.
- **Next:** run the warm real loop and watch a native crossover reach a RiskGate decision now that the council returns real verdicts reliably.

### 2026-07-14 (Opus 4.8) — fix GUI supervisor start so the stack survives past warming, gate engine start on bridge readiness, surface start failures

- **Shutdown cause found (reproduced).** From a cold GUI, Start reached warming then the whole stack shut off. Root cause: the supervisor spawned the bridge WITHOUT the whale env flags the script exports (SEC_EDGAR_ENABLED / WHALE_LIVE_ENABLED, which default OFF in the whale library). So the bridge reported whale_real=false, the engine's strict on-real whale check refused, the engine exited, and the supervisor tore down the bridge too. Confirmed by reproduction: bridge /status whale_real=False without the flags, True with them.
- **Env parity fix.** The supervisor now spawns the bridge with stack.bridge_env() (port + the whale flags from config), the SAME helper the start script now uses via the new stack bridge-env-export CLI, so the two cannot drift.
- **Readiness gating.** The supervisor waits for the bridge /health to pass (60s window, a slow health check is not a failure) before starting the engine, then confirms via stack.bridge_missing_real_layers that the required on-real layers are actually served real, failing HERE with a specific reason instead of letting the engine exit cryptically. A teardown fires only on a real unrecoverable error, never on a normal warming transition.
- **Failure surfaced in the GUI.** A failed start sets engine state.error (shown in the Ops panel and the top status strip as start failed) and writes an engine_supervisor event to the log, so the GUI shows the cause instead of going dark.
- **Cold-GUI clarity.** run_gui.sh documents that the backend hosts the supervisor and must run first, prints a GUI backend is ready line, and the Start button drives the supervisor (it never launches the backend it runs inside).
- **Verified live from a cold GUI.** Started the backend, triggered the supervisor start: starting to warming to running in ~18s, still running after 25s (no shutoff), bridge council_real=dnn_real=whale_real=sec_edgar=True, graceful stop returned not_running, engine log Shutdown complete. Kill independence held (tests: the kill path writes the control file with no supervisor involvement, and teardown never touches the kill-request file).
- **Stable:** Python pytest 237 passed (5 new supervisor tests), frontend typecheck + 11 render tests + build. C++ untouched this session. RiskGate, the live-trading gate, and the adaptive limit-weakening invariant untouched. RL gated (0/500), live OFF.
- **Next:** fix council verdict parsing so real provider responses (gemini-3.1-pro-preview, claude-opus-4-8) become verdicts reliably.

### 2026-07-14 (Opus 4.8) — raise bridge-call timeouts above real council latency, degrade on a slow provider, handle client disconnect, fix the no-trade stall

- **Root cause confirmed by measurement.** A full REAL council round trip is ~16.1s sequential (gate 1.3s + GPT-5.5 4.6s + Opus 2.9s + Gemini 3.1 Pro 7.3s, the slow one doing extended thinking at council_max_tokens 2048). The engine's bridge-call timeout was the default 1500 ms, so the engine hung up mid-response, the bridge raised BrokenPipeError, the council returned no verdict, the RiskGate never got agreement, and nothing traded.
- **Timeouts raised and made config values (no literals).** New `council` config: `engine_council_call_timeout_ms` 60000 (engine wait for /score/llm), `engine_bridge_call_timeout_ms` 8000 (fast dnn/whale/rl/status), `provider_timeout_seconds` 30 (bridge per-provider), `gate_timeout_seconds` 15 (Haiku gate). Validation rejects an engine council timeout below a provider timeout. The engine reads the two engine-side values (core/engine.cpp gather_factors + verify, core/main.cpp status probe); the Python bridge reads the two provider-side values (llm_consensus/config_access.py, consensus.py).
- **Council degrades on a slow provider and runs concurrently.** Providers are now scored concurrently (llm_consensus/consensus.py _score_all), so a slow provider only delays the council by its own time, not the sum. A provider that times out fails ALONE (flat/error verdict, logged), the other two plus the gate still produce a verdict, and the per-model source shows which failed. Verified through the bridge: the full council now returns in ~8.9s (concurrent), verdict strong_buy, agreement 3, all three sources real, no broken pipe.
- **Client disconnect handled in the bridge.** python_bridge/server.py _send and do_POST catch BrokenPipeError / ConnectionResetError, log one line, and never write a second time (the 500) over a broken socket, so a timeout can never cause the double traceback again.
- **Stable:** C++ ctest 16/16 (config timeout parse + validation), Python pytest 232 passed (5 new: config timeouts, slow-provider proceeds-on-rest, concurrent-not-serialized, _send disconnect, do_POST no-second-write). Live re-run confirmed council calls complete in ~8.9s with real per-model sources and zero broken pipes. RiskGate, the live-trading gate, and the adaptive limit-weakening invariant untouched. RL gated (0/500), live OFF.
- **Next:** run the warm real loop and watch native crossovers reach a RiskGate decision now that the council no longer stalls.

### 2026-07-14 (Opus 4.8) — self-clean stale processes and ports on start, PID tracking for clean teardown, block duplicate starts

- **Port-collision class of start failure resolved.** A prior run that crashed while holding a port no longer blocks the next start. Both the start script and the GUI supervisor self-clean pre-flight through one shared implementation (api_server/stack.py).
- **Pre-flight port cleanup.** Frees the exact ports this stack owns (bridge, GUI backend, Vite) of stale holders, graceful signal then force kill, one line per port. Only those ports, never a blanket kill. The supervisor frees ONLY the bridge port (never the api port it is served on), and free_port always protects our own pid so the supervisor can never kill the backend it runs in.
- **PID tracking + clean teardown.** The script records every started pid (bridge, engine, backend, frontend) in .run/pids, and a trap stops them and removes the file on exit and Ctrl-C. A crashed prior run self-heals on the next start: stale pids stopped, file cleared, a stale engine lock cleared.
- **Single-instance guard.** A healthy full stack already running (engine pid alive + a live health check, stack.stack_running) refuses a second start rather than fighting for ports. A stale pid file with dead pids is not a running instance, it is cleared and the start proceeds.
- **Kill switch stays independent.** Pre-flight, pid tracking, and self-heal never read or write the kill-request control file (asserted in tests), so cleanup never disturbs the safety halt, which still halts the engine with the supervisor down.
- **Stable:** Python pytest 227 passed (8 new pre-flight/pid/self-heal/guard tests, all process and port control mocked, no real network). Frontend unchanged this session (typecheck + 10 render + build green from the supervisor session). RiskGate, the live-trading gate, and the adaptive limit-weakening invariant untouched. RL gated (0/500), live OFF.
- **Next:** raise the engine-to-bridge and bridge-to-provider timeouts above real council latency and degrade gracefully on a slow provider, to fix the broken-pipe no-trade stall.

### 2026-07-14 (Opus 4.8) — GUI start/stop for paper trading through an independent supervisor, kill switch stays independent

- **Start and stop paper trading from the GUI.** New backend supervisor (api_server/supervisor.py) owns the bridge + engine lifecycle and exposes GET /engine/state, POST /engine/start, POST /engine/stop on the existing loopback backend. Start runs the same warmed sequence as scripts/start_paper_trading.sh (backfill, warm-verify, bridge, engine feed_mode alpaca_paper clock real, health checked between steps) through one shared callable, api_server/stack.py, so the script and the GUI never drift. The script was refactored to reuse stack (warm-report CLI, seed_feed_clock, the lock). State: not_running, starting, warming (per-symbol warm progress), running, stopping. Strict no-silent-mock: an unreachable on-real layer makes Start fail loudly with what is missing.
- **GUI controls.** Ops page carries a Start paper trading / Stop panel (Start has a confirm step, disabled while running or starting, shows lifecycle and warm progress), mirrored in the top status strip. No key value is ever shown.
- **Single-instance guard.** A shared .control/engine.lock records the engine and bridge pids, written by BOTH the script and the supervisor. A start is refused when a live lock exists (script or a prior GUI start). A stale lock from a crashed run (dead pid) is detected and cleared on the next start.
- **Kill switch stays independent.** GUI Stop is a graceful shutdown of what the supervisor started, a different mechanism from the safety halt. The C++ engine reads the kill-request control file itself each iteration, so a kill halts the engine even with the GUI, backend, and supervisor all down. The supervisor and its stack module never read or write the kill-request file (asserted in tests).
- **Stable:** Python pytest 219 passed (10 new supervisor + warm + kill-independence tests, mocked process control, no real network), frontend typecheck + 10 render tests + production build. C++ untouched this session (ctest 16/16 from the prior session). RiskGate, the live-trading gate, and the adaptive limit-weakening invariant untouched. RL gated (0/500), live OFF.
- **Next:** self-clean stale processes and ports on start so a prior run holding a port never blocks a start, then raise bridge-call timeouts above real council latency to fix the no-trade stall.

### 2026-07-13 (Opus 4.8) — tuner floor to sustain native entries, consume remaining GUI controls, fix simulated-clock skip and replay cooldown

- **Tuner floor sustains native entries.** New `adaptive.rule_based_weight_floor` (default 0.35). The adaptive tuner never nudges the raw rule_based weight below it (learning/adaptive.cpp, still under the 0.6 cap, still recorded to param_history), AND compose_gate_verdict guarantees rule_based at least that NORMALIZED share (signal_engine, new rule_based_min_share). The share floor is the part that matters: a raw floor alone failed because the five advisory factors saturate at the cap and dilute rule_based's normalized share toward zero, dropping the gate confidence below the RiskGate minimum and stalling entries near 30. Verified: a 5000-step synthetic run keeps generating native entries past 100 closed trades (entries at #31, #61, #101, #131 in a 6000-step run). No risk limit touched.
- **Every remaining controls.json override is consumed.** The engine now consumes model toggles, regime pins, and budget each iteration (core/operator_controls.hpp, consume_operator_controls). Model toggles drop a disabled provider from the council (>=1 required or a logged skip), regime pins override the detector per symbol (test-only), budget adjusts council_daily_budget + per-symbol cooldown at runtime within bounds. Each logs old/new. Promote and rollback of the dnn champion execute through the Python registry path at the audited endpoint (gated by meets_promotion_criteria, a runtime promote cannot bypass it), a documented safe choice since the registry is Python-owned.
- **Two mode-consistency fixes.** The equities market-hours council skip keys off the simulated timestamp under clock_mode simulated (real wall-clock under real). Replay council-cooldown spacing keys off the true historical bar ts (util::iso8601_to_epoch), matching the per-day trade cap.
- **native_conviction_feeds_gate** documented (kept default true, recommended for the paper week, operator-controlled, unchanged this session).
- **Stable:** C++ ctest 16/16 (new test_tuner_floor, test_operator_controls, test_time_modes), Python pytest 209 passed (new promote/rollback-execute + flat-key tests, backfill), frontend typecheck + 9 render tests + build. RiskGate, the live-trading gate, and the adaptive limit-weakening invariant untouched. RL gated (0/500), live OFF.
- **Next:** run the warmed paper week and watch fills accumulate past the old plateau; the mock advisory factors still cap synthetic fills via the model-agreement gate (a mock artifact that a real council would not have).

### 2026-07-13 (Opus 4.8) — warm native strategy from real-bar backfill, runtime feed/clock toggle with open-position safety, warmed real-time paper start

- **Warm-start from a real-bar backfill.** The engine already seeds its bar history from the `bars` table on construction; this session makes that a first-class warm-start. New pure helpers `strategy::min_bars_to_warm` / `indicator_warm_state` / `indicators_warm` (signal_engine/strategy.cpp) compute the longest indicator lookback (100-period EMA + 2 = 102, dominating ADX, ATR, Bollinger-20, RSI-14, volume, realized vol). The startup block prints a per-symbol per-indicator warm/cold line (`Engine::warm_states`). Verified: a real Alpaca backfill wrote thousands of 5-min bars per symbol and the engine reported all four WARM at open.
- **Warm-state gate on the real path.** On feed_mode alpaca_paper the engine does not evaluate a symbol for a native entry until its indicators are warm (`symbol_is_warm`); a cold symbol waits and never fires on partial data. Cold/warm transitions log as `warm_state`. Offline feed modes are unchanged (not gated).
- **Runtime feed + clock toggle with an open-position safety rule.** New core/feed_clock.hpp (pure): `read_feed_clock` resolves feed_mode/clock_mode from controls.json each iteration (fallback = launch feed/clock, so a missing file never forces an offline run onto the live feed), and `feed_switch_orphans_position` encodes the rule. `run_forever` now re-dispatches on the resulting feed each iteration (tick modes poll, bar modes step). A clock switch applies immediately. A feed switch away from alpaca_paper with an open position is BLOCKED (never orphans it), logged `feed_mode_blocked`; a switch into alpaca_paper re-arms the warm gate; every applied switch logs `feed_mode`/`clock_mode` old->new. Surfaced on the Controls page via a validated `/controls/feed_clock` endpoint (refuses an unsafe switch, audited), mirrored in the run-state banner and the top status strip.
- **Start script warms first.** scripts/start_paper_trading.sh now backfills the whitelist and verifies every symbol warm before opening the loop, and seeds controls.json feed=alpaca_paper clock=real so the GUI and engine agree from the first tick.
- **Live verification (headless, warmed).** The one command backfilled real bars, reported all four symbols WARM at open, brought up the full stack (bridge council_real=dnn_real=whale_real=sec_edgar=True), and ran the alpaca_paper loop against warm indicators. See RETURN.md for the full run table (bars, regimes, warm-state, the feed/clock toggle exercised live, and whether a native signal fired).
- **Stable:** C++ ctest 13/13 (new test_feed_clock, test_warm_start; warm asserts added to test_strategy), Python pytest 207 passed (new test_backfill, feed/clock API test), frontend typecheck + 9 render tests + build. RiskGate, the live-trading gate, and the adaptive limit-weakening invariant untouched. RL gated (0/500), live OFF.
- **Next:** with warm indicators seeded, watch for the first live native entry -> Haiku gate -> real council verdict -> RiskGate -> Alpaca paper fill; and consume the remaining controls.json overrides (model toggles, regime pins, budget, promote/rollback).

### 2026-07-13 (Opus 4.8) — per-level mock/real toggle, full four-level real-time paper activation, strict no-silent-mock, SEC EDGAR live verified

- **Per-level source axis (mock/real), distinct from enable.** Each advisory layer now has two axes: enable (off drops the factor) and source (mock/real, only when on), so each reads off / on-mock / on-real. The engine reads both from controls.json each iteration (core/layer_toggles.hpp: council_source / dnn_advisory_source / whale_source, plus factor_source_real and layer_state); on-mock keeps the C++ mock even with the bridge up. Source changes log to the event log as layer_source. Adaptive has enable only; safety has neither axis (always on, always real). Surfaced on Controls and Ops via a validated /controls/source endpoint (refuses safety/adaptive), mirrored in the run-state banner.
- **Full four-level activation default + one command.** config: llm.use_real_council true, whale.sec_edgar_enabled / whale_live_enabled true. scripts/start_paper_trading.sh brings up bridge (real council + dnn + whale) -> engine (alpaca_paper, clock real, full whitelist) -> GUI, health-checked, clean teardown. Live trading stays OFF.
- **Strict no-silent-mock on the real path.** On alpaca_paper, a layer set on-real with no reachable real service makes the engine refuse to start (Engine::verify_real_layers_reachable), printing what is missing; on-mock starts silently; offline modes keep mock. Reachability from a new bridge /status (cheap, no paid call).
- **Bug fixed:** run_forever routed alpaca_paper into the bar-step branch (synthetic/replay only), so continuous alpaca_paper exited immediately. It now falls through to the tick path (run_iteration), so the primary online loop actually runs.
- **SEC EDGAR verified live, two forms.** New SecForm4Adapter (insider Form 4, ~2 business day lag) joins Sec13FAdapter (~45 day lag). Real fetches of both returned HTTP 200 and are recorded as fixtures. WhaleActivity gained delay_label so the specific lag surfaces. FTS exposes no value, so the whale signal stays weak neutral context under the 0.35 cap.
- **Startup proof block** shows per level: enabled + source (off/on-mock/on-real), council models + Haiku gate, dnn champion provenance, whale + SEC EDGAR on, RL fill count vs the 500 gate, kill switch ARMED, live DISABLED, L1 headline.
- **Live verification (headless, ~16 min).** The one command brought up the full stack (bridge/engine/GUI all healthy), the engine ran the alpaca_paper tick loop continuously and ingested + persisted real Alpaca bars for all four whitelisted symbols, and the council mock/real flip worked LIVE (POST /controls/source -> /runstate reflected it, engine logged layer_source real->mock->real). No native entry fired in the window (indicators need far more than a 15-min cold start to warm up), so the council and skip feed had no entries this run; the real-council verdict path itself is proven separately (real_providers().score() returns source=real for all three providers). No threshold was lowered to force a trade.
- **Stable:** C++ ctest 11/11 (new test_strict_mode), Python pytest 204 passed (Form 4 fixture, whale cap, /controls/source), frontend typecheck + 9 render tests + build. RiskGate, the live-trading gate, and the adaptive limit-weakening invariant untouched. RL stays gated (0/500), live OFF.
- **Next:** warm the native strategy with a real-bar backfill so a live run can exercise a real council verdict end to end; consume the remaining controls.json overrides (model toggles, regime pins, budget, promote/rollback).

### 2026-07-12 (Opus 4.8) — fix OpenAI and Gemini to models the keys can reach, add startup model validation

- **Diagnosed against live APIs, not assumptions.** New scripts/list_provider_models.sh resolves keys keystore-first and lists each provider's reachable models (redacting any key). The earlier OpenAI 400 and Gemini 404 were never auth failures. OpenAI `gpt-5.5` IS reachable; the request shape was wrong. Gemini `gemini-3.1-pro` does NOT exist for the key; `gemini-3.1-pro-preview` does.
- **OpenAI request-shape fix.** GPT-5 family needs `max_completion_tokens` (not the deprecated `max_tokens`, which it rejects) and only the default temperature. Fixed in llm_consensus/providers.py (OpenAIProvider) and the health check. Model string unchanged.
- **Gemini model-string fix.** llm_tertiary is now `gemini-3.1-pro-preview` in config, and the string was corrected everywhere it appeared (provider defaults, consensus defaults, provider_prices, health check, controls model-toggle keys now derive from config so they cannot drift, frontend label, docs, CLAUDE.md approved list).
- **Thinking-model headroom.** Gemini 3.1 Pro is thinking-only and spent the whole 400-token cap on reasoning (finishReason MAX_TOKENS, 2 chars out). council_max_tokens raised to 2048 (a ceiling, not spend: a real verdict used ~324 tokens) so the council gets a parseable verdict. Cost cap only, not a risk limit.
- **Non-fatal startup model check.** New llm_consensus/model_check.py lists each provider's models at bridge startup (real council only) and warns if a configured model is unreachable, turning a silent mid-trade 404 into a visible warning. It never raises, so a provider outage never blocks startup. A `-preview` word suffix is not treated as a date alias, so `gemini-3.1-pro` would not falsely pass; a date suffix like `-20251001` counts as the `claude-haiku-4-5` alias.
- **Re-verified:** all four LLM paths (OpenAI, Anthropic Opus, Haiku gate, Gemini) plus Alpaca paper data and order-auth return working (scripts/verify_live_integrations.sh, table in RETURN.md). End-to-end the real council now returns real verdicts from OpenAI, Anthropic, and Gemini.
- **Stable:** C++ ctest 10/10, Python pytest 198 passed (11 new model_check tests; suite made hermetic against the host keystore so offline tests never issue real calls), frontend typecheck + 9 render tests + production build. RiskGate, live-trading gate, and the adaptive limit-weakening invariant untouched. Live trading stays OFF.
- **Next:** consume the remaining controls.json overrides (model toggles, regime pins, budget, RL enable, promote/rollback); prove paper-loop stability.

### 2026-07-11 (Opus 4.8) — engine consumes per-layer toggles, surfaced in Ops, safety always on

- **Engine consumes per-layer toggles from controls.json each iteration.** New core/layer_toggles.hpp (read_layer_toggles + factor_enabled). consume_layer_toggles runs at the top of run_iteration and step_bar_mode, the same control-file pattern as the kill request. gather_factors filters the ensemble by factor_enabled, so a layer toggled off (adaptive, council, dnn_advisory, whale) drops its factor and contributes nothing to direction, sizing, confidence, or edge. Adaptive off skips the weight nudge. Missing/malformed controls.json means all layers on. Each change logs to the event log as layer_toggle.
- **Advisory only, never a safety bypass.** rule_based (native) and rl_advisory are never gated, the RiskGate still evaluates every order with all four toggleable layers off, and the static-safety layer has no toggle. Asserted in ctest.
- **Surfaced in Ops.** The Ops section shows the same four toggles on the same validated /controls/layer endpoint (no new write path), plus a fixed ALWAYS ON safety indicator. A flip takes effect on the engine's next iteration.
- **Startup and status reflect toggles.** The run-state banner and the engine startup block show which layers are enabled, so an off-by-choice layer is distinct from a mock or unreachable one. store.runstate returns the layers.
- **Stable:** C++ ctest 10/10 (new test_layer_toggles), Python pytest 187 passed, frontend typecheck + 9 render tests + production build. RiskGate, live-trading gate, and the adaptive limit-weakening invariant untouched. Live trading stays OFF.
- **Next:** consume the remaining controls.json overrides (model toggles, regime pins, budget, RL enable, promote/rollback); confirm OpenAI/Gemini model access; prove paper-loop stability.

### 2026-07-11 (Opus 4.8) — unify credential resolution keystore-first, add live integration verification

- **All live-key paths resolve through the one keystore-first resolver.** account_manager.credentials.resolve_env / get_credential (keystore, then env, masked, never logged). Confirmed for the LLM providers, the Haiku gate, the Alpaca clients, and GET /health/integrations; routed the two stragglers through it (whale_signal/adapters SEC_EDGAR_CONTACT_EMAIL, market_data/alpaca_source._data_keys). A keystore key now counts as configured exactly as the engine sees it.
- **Health check + test script use the resolver.** scripts/test_full_system.sh sec_council_live checks presence via resolve_env, sec_alpaca_paper resolves via get_credential inside python (key never touches the shell), so both run when the keystore holds keys and SKIP only when a key is absent everywhere.
- **scripts/verify_live_integrations.sh** runs one real minimal round trip per integration (reusing the resolver-backed health checks), prints a labeled table with latency, and appends it to RETURN.md. Never a resting order (Alpaca order-auth is a GET), never live, never a key value.
- **Verified live (2026-07-11):** Anthropic Opus 4.8 working, Anthropic Haiku 4.5 gate working, Alpaca paper market data working, Alpaca paper order-auth working. OpenAI GPT-5.5 failing HTTP 400 and Gemini 3.1 Pro failing HTTP 404, request/model errors not auth, so the keys resolve but the operator should confirm model access or request shape. Approved model strings unchanged per CLAUDE.md.
- **Stable:** Python pytest 186 passed (49 in test_api_server, +4 resolver tests). Frontend unchanged (Health view already reflects keystore-resolved state). RiskGate, live-trading gate, and the adaptive limit-weakening invariant untouched. Live trading stays OFF.
- **Next:** engine consumption of the remaining controls.json operator overrides (layer/model toggles, regime pins, budget, RL enable); confirm the OpenAI and Gemini model access; prove paper-loop stability.

### 2026-07-10 (Opus 4.8) — operational GUI upgrades and live provider cost panel

- **Always-visible kill switch** in the top strip on every page. One click, one confirm, writes the same kill-request control file the engine consumes. Shows armed or tripped, manual resume unchanged, no second resume path.
- **Run-state banner** under the strip (RunStateBanner + GET /runstate): loop mode, clock, data source, bridge up/down, real vs mock council.
- **Council skip-reason feed** (SkipFeed + GET /skips) reading the event log for council_skip, risk_precheck, market_hours, on Paper Overview and the new Ops page.
- **Staleness badges** (StalenessBadge) on feed-dependent panels (equity/market data, positions, signals, council) with a warning color past a configurable threshold.
- **Clickable trade rows** open a TradeDetailModal (GET /trade/{id}): order and sizing, regime, factors that fired, council verdict at entry, entry/exit events.
- **Day summary card** (DaySummary + GET /day_summary): trades today, win rate today, council calls today vs budget, estimated spend today.
- **Drawdown shading** overlaid on the equity curve.
- **Provider cost panel** (ProviderCostPanel + GET /providers/cost): balance where exposed, else provider spend, else a local estimate always computed and labeled estimated. No provider exposes a stable prepaid-balance endpoint for a plain key, so the reported signal is the local estimate from recorded council calls times per-model prices in the new config/provider_prices.yaml (Python-only). Concurrent reads with a timeout, absent key reports unavailable, no key value returned or logged.
- **Stable:** C++ ctest 9/9, Python pytest 182 passed (45 in test_api_server), frontend typecheck + 9 render tests + production build. All new endpoints read-only, bound 127.0.0.1, no Level 1 or operational write. RiskGate, live-trading gate, and the adaptive limit-weakening invariant untouched. Live trading stays OFF.
- **Next:** wire engine consumption of controls.json; prove paper-loop stability.

### 2026-07-10 (Opus 4.8) — remove ClankApp, Alpaca-only paper credentials, gate native conviction, full-system test script, live API health check

- **ClankApp removed for a dead host.** api.clankapp.com is DNS-unreachable. Deleted the adapter + mock, the __init__ import, the clankapp_key credential + required-fields entry, the synthetic fixture, the ClankApp parser test, the CLANKAPP_API_KEY env line, the Dash SOURCE_GROUPS entry, the demo + schema.md mentions, and the React Settings whale group. Removed-for-dead-host comments left in place. SEC EDGAR is the sole active whale adapter (default_adapters returns [Sec13FAdapter]); the 0.35 advisory cap is unchanged; Whale Alert and Unusual Whales stay reserved.
- **Alpaca is the only paper venue with credentials.** Paper keys are APCA_API_KEY_ID and APCA_API_SECRET_KEY (older ALPACA_* kept as fallbacks). Removed IBKR paper and Coinbase paper credential specs from the registry, the env example, and the Settings page. Kept IBKR gateway host/port/enabled (config.ibkr) and the reserved live and data env vars.
- **native_conviction_feeds_gate flag (default true).** New signal_engine::compose_gate_verdict composes the gate confidence and edge: true feeds the native rule_based conviction (current behavior), false takes confidence and edge from the advisory factors alone while direction and sizing still use the full ensemble. New ctest test_native_conviction_gate covers both states. RiskGate untouched. The Open-Flags double-count note is now marked GATED.
- **scripts/test_full_system.sh** runs 17 sections (build zero-warnings, ctest, pytest, config validation, RiskGate + kill switch, strategy + regime, real-fill feedback, council offline, council live optional, cost controls, dnn advisory, RL gating, whale, Alpaca paper optional, API backend, frontend, live exclusion), PASS/FAIL/SKIPPED per section, continues past failures, nonzero exit on any failure, summary table, self-cleanup.
- **Live API health check.** GET /health/integrations (api_server/health.py) does one real minimal round trip per integration concurrently with timeouts, reporting working/failing/not_configured + latency, never placing an order or touching live or writing an op/Level-1 value or logging a key. Frontend Health view + top-strip aggregate added.
- **Stable:** C++ ctest 9/9, Python pytest 176 passed, frontend typecheck + 8 render tests + production build, and scripts/test_full_system.sh all sections PASS (the two live sections SKIPPED because keys resolve from the keystore, not the process env). RiskGate, live-trading gate, and the adaptive limit-weakening invariant untouched. Live trading stays OFF and is excluded from testing.
- **Next:** wire engine consumption of controls.json; prove paper-loop stability.

### 2026-07-10 (Opus 4.8) — confirm SEC EDGAR as the sole active whale feed, reserve Unusual Whales as a paid upgrade

- **SEC EDGAR is now the sole ACTIVE whale source.** `whale_signal.adapters.default_adapters()` returns `[Sec13FAdapter()]` (was `[ClankApp, Sec13F]`). SEC EDGAR is free, keyless, and delayed (13F, ~45-day lag, equities context), gated by SEC_EDGAR_ENABLED and defaulting to the offline mock.
- **Crypto feeds reclassified as reserved, not active.** ClankApp and Whale Alert stay wired and importable as optional crypto adapters, off the default chain, following the reserved-integration pattern.
- **Unusual Whales Pro reserved as the real-time paid upgrade.** UNUSUAL_WHALES_API_KEY stays reserved (unset); a code comment in whale_signal/adapters.py documents it as the paid upgrade for richer equities smart-money data (options flow, dark pool, congressional, insider, 13F) at roughly 48 dollars per month, pending an operator decision. No adapter wired.
- **Docs + env:** CONTEXT.md Whale Tracking Decisions rewritten (SEC EDGAR sole active, crypto reserved, Unusual Whales reserved paid). .env.example whale comments updated. whale_signal/__init__.py docstring updated.
- **Tests:** test_whale_signal.py updated (default_adapters is sole sec_13f, new test_sec_edgar_is_sole_active_source, offline-never-raise now also exercises the reserved crypto adapters). Full Python suite green.
- **NOT touched:** RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant. Live trading stays OFF.
- **Next:** wire engine consumption of controls.json; prove paper-loop stability.

### 2026-07-09 (Opus 4.8) — rebuild the React GUI Alpaca-style with Paper/Live subpages, a Controls surface, and validated backend control endpoints

- **Rebuilt web/ Alpaca-style.** Dark neutral theme, one gold accent (green gains, red losses), clean cards, dense tables. Left sidebar: Paper, Live, Controls, Settings. Top strip on every page: engine state, active mode, portfolio value, daily PnL, kill-switch status.
- **Paper + Live each hold Overview / Stocks / Crypto subpages.** Stocks and Crypto filter SERVER-SIDE via a new `category` query param on /positions, /orders, /trades, /signals (stocks = SPY,QQQ; crypto = BTC/USD,ETH/USD; "/" and "-" treated as equal so a legacy BTC-USD is crypto). Live stays LOCKED and zeroed; no Live control enables live.
- **New Controls page = the CONTEXT.md GUI Plan surface.** Weight sliders by layer, per-layer toggles (safety always on, no toggle), per-model council toggles + the Haiku base-check gate, champion/challenger auto-promote + promote + rollback (gated + confirm), an RL enable toggle gated on the real-fill count, per-symbol regime override (test only), a council budget dial. Level 1 renders read-only.
- **Validated backend control endpoints (api_server/controls.py + app.py).** Every control clamps + validates server-side and audits to the events log with old/new. Weights reuse the Dash channel (ui.db.save_weight_overrides -> weight_overrides.json + weight_changes); the rest persist to a controls.json control file. RL enable refused below rl_min_real_fills; safety toggle refused; budget clamped to bounds; regime pins accept only whitelist symbols. STRUCTURAL rule (asserted): no control writes a Level 1 value, touches the gate, loosens a limit, or enables live. Bind stays 127.0.0.1.
- **Verified against the real market_ai_lab.db (working copy):** category=crypto returned BTC-USD, category=stocks empty (the DB holds only legacy bootstrap symbols); a weight change normalized to sum 1.0 and landed in weight_changes + an audited event; RL enable refused (238 real fills < 500 gate) with the count shown; kill state reflected the engine. A real uvicorn server on 127.0.0.1:8011 served /health (engine running, bridge reachable), /controls, and the category filter over loopback.
- **Stable:** backend 36 pytest (full Python suite 173 passed), frontend 7 render tests + typecheck clean + production build (84 KB gzip). Dash UI untouched (fallback). RiskGate, live-trading gate, and the adaptive limit-weakening invariant untouched. Live trading stays OFF.
- **Next:** wire the engine/trainer to consume the non-weight controls.json overrides; prove paper-loop stability.

### 2026-07-09 (Opus 4.8) — wire the GUI kill-request control file into the engine kill switch

- **The GUI kill button now actually halts the engine.** New `Engine::consume_operator_kill_request()` (`core/engine.cpp`) reads the API backend's control file (`.control/kill_request.json`, env `MAL_CONTROL_DIR` overrides, matching config `system.control_dir`) at the top of BOTH per-iteration entry points — `run_iteration` (tick / alpaca_paper online loop) and `step_bar_mode` (bar-driven synthetic_regimes / replay) — BEFORE any signal is evaluated.
- **Same latching mechanism, not a separate path.** When `requested` is true it trips the existing kill switch exactly as a loss breach does: `kill_switch_.trip(reason)` + per-venue `accounts_->trip_kill_switch` + a `kill_switch` critical event. It reads a control file only; it never touches the RiskGate.
- **No re-trip on restart.** The processed request is archived to `kill_request.processed.json` (atomic rename, delete fallback), so a stale file never re-trips on a later run.
- **Manual resume unchanged.** The switch stays latching; an operator halt requires the same manual resume as a loss-triggered halt. No new resume path.
- **Supporting bits:** `SystemConfig::control_dir` (config.hpp/.cpp/default_config.yaml), a `json_get_bool` JSON helper (`core/bridge_client.*`) mirroring the existing tiny readers, and read-only `Engine::kill_switch_tripped()` / `manual_resume_pending()` accessors.
- **Verified end to end (synthetic_regimes + the real POST /kill endpoint):** baseline 800-step synthetic run opened 52 paper trades; with a pending request the same run tripped on iteration 1 (event order startup -> kill_switch -> summary), opened 0 trades, and archived the request (wall-clock 0.17s). On the tick path, the operator halt persisted `venue_state.kill_switch_tripped=1` for alpaca/coinbase/ibkr via the existing `snapshot_balances`, so the GUI reflects it.
- **Stable:** C++ ctest 8/8 (new `test_kill_switch`: 14/14 checks). Python pytest 160 passed (added the control-file shape-contract test). RiskGate logic, the live-trading gate, and the adaptive limit-weakening invariant untouched. Live trading stays OFF.
- **Next:** the CONTEXT.md GUI Plan controls; prove paper-loop stability.

### 2026-07-08 (Opus 4.8) — replace the Gemini 3 Flash base-check gate with Claude Haiku 4.5

- **Base-check gate now runs on Claude Haiku 4.5** (`llm_consensus/gate.py`: `GeminiFlashGate` -> `HaikuGate`). It reuses the council's Anthropic Messages client and the same `ANTHROPIC_API_KEY`, so no new credential is needed. Same screening prompt and same yes/no-plus-reason JSON contract; the gate still skips the council on "no".
- **Shared Anthropic transport extracted** (`llm_consensus/providers.py`: new `anthropic_request` + `anthropic_text`, mirroring `gemini_request`/`gemini_text`). `AnthropicProvider` was refactored onto them so the council secondary and the gate share one transport (DRY). Gate response capped at 128 tokens.
- **Config**: `config/default_config.yaml` `llm_gate: gemini-3-flash` -> `claude-haiku-4-5`, commented that the gate uses Haiku through the Anthropic client and reuses `ANTHROPIC_API_KEY`. `gate_enabled` unchanged.
- **Env**: `.env.example` keeps `GEMINI_API_KEY` (still used by the tertiary council slot `gemini-3.1-pro`); only the gate comment moved to `ANTHROPIC_API_KEY`. No new credential.
- **Startup line**: `python_bridge/server.py` prints the gate model via `council_status_line`, now showing `claude-haiku-4-5`.
- **Fail-safe posture preserved**: gate disabled -> AlwaysProceedGate; no key -> permissive mock (proceed); call error / unparseable -> fail-open (proceed).
- **Tests**: `tests/test_llm_consensus.py` gate tests moved to Haiku mocks (Anthropic envelope, `ANTHROPIC_API_KEY`); `tests/test_council_cost_controls.py` stub gate model string updated. Full Python suite green (159 pytest passed).
- **Docs**: CONTEXT.md (API Notes + Cost Notes + Key Decisions); CLAUDE.md approved-model-strings rule; AUDIT.md; "Flash gate" -> "base-check gate" comment sweep across consensus.py/config_access.py/core/engine.cpp/config/config.hpp.
- **NOT touched**: RiskGate logic, the live-trading gate, the adaptive limit-weakening invariant. Live trading stays OFF.
- **Next**: consume the kill-request file in the engine, then the CONTEXT.md GUI Plan controls; prove paper-loop stability.

### 2026-07-06 (Opus 4.8) — React + TypeScript trading GUI (Settings, Paper, Live) on a read-only FastAPI backend

- **Built a React + TypeScript GUI in web/**, Coinbase Pro dark theme, additive to the Dash UI (which stays as the fallback, untouched). Left sidebar + top status bar (engine state, active view, kill switch, bridge). Three routed pages: Settings and APIs, Paper, Live. Dependency-free SVG equity chart, typed API client, REST for initial load, WebSocket for live updates, loading and error states throughout.
- **Thin read-only FastAPI backend in api_server/.** Endpoints: /health, /account, /positions, /orders, /trades, /pnl, /signals, /council, /whale, /risk, /venues, /approval, /credentials (GET masked + POST encrypted), /credentials/test, /kill (GET + POST), and the /stream WebSocket on a 2s tick. Binds 127.0.0.1 only. Read-only on the operational tables. The only write paths are credential entry through the existing encrypted keystore and a kill-switch halt request written to a control file (.control/), never an operational table and never the RiskGate.
- **Kill switch, honestly.** The engine trips its own kill switch and reflects it in venue_state; the GUI shows that state and records a durable operator halt request through the control-file channel. Wiring the engine to consume the request file is a flagged follow-up.
- **LLM council keys** (OpenAI, Anthropic, Google) added to the credential registry so the Settings page manages them; they resolve through the existing resolve_env path (in-app first, then env), so a saved key flows to the council with no provider change.
- **Verified live** against the real market_ai_lab.db: /health shows engine running + bridge reachable, /account paper equity resolves, /venues lists alpaca/coinbase/ibkr, /approval reports all four mechanisms with live locked.
- **Stable:** backend 22 pytest (full Python suite 159 passed), frontend 3 render tests + typecheck clean + production build. No real network in any test. RiskGate, live-trading gate, and the adaptive limit-weakening invariant untouched. Live trading stays OFF.
- **Next:** consume the kill-request file in the engine, then the CONTEXT.md GUI Plan controls.

### 2026-07-06 (Opus 4.8) — remove Polymarket, Alpaca paper as primary online loop, IBKR wired live behind the gate

- **Removed Polymarket fully.** Deleted PolymarketPaperAdapter and all Polymarket routing (execution + engine, both the native and legacy bootstrap-sim paths), the Apify Polymarket whale adapter, and every Polymarket/Apify reference in config, credentials, the dashboard groups, schema comments, and the demo. Added tests/test_no_polymarket.py as a regression guard.
- **Alpaca paper is the primary ONLINE loop.** `feed_mode: alpaca_paper` forces the online AlpacaFeed (real 5-minute bars over the bridge, paper orders to Alpaca paper) through the same closed-bar path. Startup now prints an alpaca line and an online-mode note. Alpaca still has NO live path (live_adapter none).
- **IBKR wired as the live-only venue behind the gate.** IbkrLiveAdapter replaced the sim placeholder and POSTs to the bridge /execute/ibkr_live only through the gated Live branch. New execution/ibkr_adapter.py maps orders to IBKR contracts/orders and places/cancels/reports status via ib_insync (imported lazily, pinned optional). A missing IB Gateway or dropped socket returns unavailable and never simulates. Startup probes IB Gateway reachability when ibkr.connection_enabled is true. Live stays DISABLED behind the approval gate.
- **Verified (synthetic_regimes + simulated clock, 4000 iterations):** 16000 bars closed, 31 native entries (reversion 28, momentum 3), 31 exits (target 30, stop 1), tuner 623 weight changes within clamps, train_real challenger_recorded (15900 samples, sharpe 0.9392).
- **Stable:** C++ ctest 7/7 (added test_ibkr_routing), Python pytest 137 passed (added test_ibkr_adapter, test_no_polymarket). No network or socket in any test.
- **Next:** wire the live-approval workflow end to end, then the GUI overhaul (CONTEXT.md GUI Plan).

### 2026-07-05 (Opus 4.8) — offline loop becomes a real training environment

- **Made the offline paper loop generate real native fills** so it can train the tuner / `dnn_advisory` / (eventually) RL. RiskGate logic, the live-trading gate, and the adaptive limit-weakening invariant untouched; live trading stays off; Alpaca remains paper + market-data only.
- **Config-tunable entry thresholds:** the last hardcoded strategy literal (reversion volume multiple) moved to `strategy.vol_multiple`; the ADX/rvol/ATR/EMA/Bollinger/RSI thresholds were already config-backed. Added load-time validation (periods ≥1, bb_std/vol_multiple >0).
- **Volatility-aware synthetic feed** (`market_data/synthetic_feed.*`, `feed_mode: synthetic_regimes`): a deterministic warmup→uptrend→range→downtrend generator that crosses the ADX + realized-vol thresholds so BOTH momentum and mean-reversion enter. Verified standalone: 3 momentum + 17 reversion signals over 1200 bars under seed 42.
- **Historical replay** (`feed_mode: replay`): drives the loop from real `bars`-table rows in chronological order through the same closed-bar path; configurable date range + `replay_speed`; refuses loudly (run the Alpaca backfill first) when the range is empty — never silently zero. Added `Storage::bars_in_range`.
- **Simulated clock** (`clock_mode: simulated`): bar time advances internally (`util::epoch_to_iso8601`) so finite/synthetic runs actually close bars; real-clock stays default for the continuous loop.
- **Wired the native signal into the ensemble as `rule_based`** so a genuine technical setup's conviction reaches the RiskGate (previously confidence/edge came only from mock advisory factors, which vetoed every native entry on the 0.02 edge floor). No gate logic or threshold changed; no risk value loosened.
- **End-to-end verification (Task 5):** synthetic run closed 48,000 bars, fired 31 native entries (momentum 3 / reversion 28), closed 31 trades (30 win / 1 loss; ATR target 30, stop 1) all under the native whitelist, tuner active past its ≥30 gate, and `python -m ml_factor.train_real` trained a real-data challenger (`challenger_recorded`, 47,900 samples, Sharpe 0.98). Residual: entries plateau after ~30 fills as the tuner de-weights `rule_based` (see Known Issues).
- **Startup transparency:** prints active feed mode, clock mode, and resolved strategy thresholds.
- **Tests:** C++ `feed_modes` (synthetic crosses thresholds + ≥1 momentum & ≥1 reversion under fixed seed; simulated clock closes the expected bar count; replay reads in order + stops + refuses when empty) → **ctest 6/6**; pytest `test_replay_refusal.py` (binary refuses on empty bars) → **pytest 125 passed**.
- **Stable:** clean warning-free build, ctest 6/6, pytest 125. **Next:** address the tuner-throttle residual (min-weight floor for the native factor) so sustained offline training keeps generating fills, then the GUI overhaul.

### 2026-07-05 (Opus 4.8)

- **Closed every open follow-up flag from the 12-task prompt, built the RL advisory module (shipped off), and added two council cost cuts.** RiskGate / live-trading gate / adaptive limit-weakening invariant untouched; live trading stays off.
- **Flags cleared:** created a Python 3.14.4 venv, installed both pinned requirements files, ran the full suite — **124 pytest passed** (pandas pin reconciled to 2.2.3 so it builds against numpy 1.26.4). Ran `python -m ml_factor.train_real` against the demo DB: refuses cleanly (`insufficient_real_data`, 0 real samples < 200, synthetic champion retained). Recorded a **real SEC EDGAR 13F fixture** (5 hits, delayed-disclosure) and updated the parser test; ClankApp left SYNTHETIC (host DNS-unreachable, blocker logged). Residual doc sweep done: Binance→Coinbase and DNN/RL→`dnn_advisory` across `docs/ARCHITECTURE.md`, `docs/BUILD_SPEC.md`, `docs/FOLLOWUP_CREDENTIALS.md`; `git mv docs/DNN_RL_DESIGN.md docs/DNN_ADVISORY_DESIGN.md` + updated every code/README reference; **AUDIT.md refreshed** to honest current state.
- **RL advisory (`rl_advisory/`, Stable-Baselines3 PPO), shipped OFF:** gym `TradingEnv` (rolling-window obs; discrete flat/long/short; equities long-only; reward = realized PnL − mandatory txn cost − drawdown penalty), a hard `rl_min_real_fills` gate (default 500) that refuses **before importing any backend** with **no synthetic-data path**, walk-forward eval + champion/challenger via the shared promotion gate, `/score/rl` bridge endpoint with labelled mock fallback, artifacts registered with provenance. `rl_enabled` defaults false → engine never calls it and the factor (`rl_advisory_factor_weight = 0.0`) stays out of the ensemble. Advisory only, hard-capped at 0.5, never a sole controller.
- **Council cost cuts (in `llm_consensus/consensus.py`, before the Flash gate + providers):** (1) risk pre-check — the engine evaluates cheap RiskGate preconditions read-only and, when already blocked, skips the whole council (logged `risk_precheck`); (2) equities market-hours skip — SPY/QQQ skip the gate+council outside US RTH while crypto stays 24/7 (logged `market_hours`, config `engine.equities_market_hours_only` default true). C++ engine short-circuits before the bridge call; config adds `rl_enabled`/`rl_min_real_fills`/`equities_market_hours_only`; startup block prints RL + market-hours state.
- **Tests added:** `tests/test_rl_advisory.py` (env contract, txn-cost reward, long-only clamp, trainer refuses below gate, `/score/rl` disabled/mock, factor stays out when disabled, walk-forward + challenger gate) and `tests/test_council_cost_cuts.py` (both skips fire before any provider/gate; never skips crypto; disabled-by-config no-op). No network in any test.
- **Stable:** C++ safety spine builds clean, `ctest` **5/5**; full Python suite **124 passed**; RL package stays import-light (torch/SB3 lazy) so the bridge and suite run without them.
- **Next:** prove paper-loop stability over time, then the GUI overhaul (CONTEXT.md GUI Plan).

### 2026-07-04 (Opus 4.8)

- **Completed the 12-task master prompt** on `feat/native-strategy-council-cost-controls`; fast-forwarded onto `origin/main`.
- Task 1 bars OHLCV storage + Alpaca historical backfill; Task 2 native strategy layer (momentum + reversion + regime detector, closed-bar eval, native ATR exits); Task 3 real-fill learning (dropped `simulate_outcome` from the default path, tuner gated at ≥30 closed trades); Task 4 council cost controls (entries-only, Flash gate, daily budget, per-symbol cooldown, token cap, neutral skip, skip logging); Task 5 `dnn_advisory` rename + real-data walk-forward training pipeline + provenance + gated promotion; Task 6 `CoinbaseSimAdapter` replaces Binance; Task 7 free-first whale feeds (ClankApp + SEC EDGAR) live-OFF by default; Task 8 Level-1 config defaults; Task 9 security hardening (loopback bridge, credential masking, pre-commit secrets hook, pinned deps); Task 10 startup transparency block.
- Task 11 (this turn): extracted the tuner ≥30-sample gate into `learning/adapt_gate.hpp` (pure predicate) so it is unit-testable; added `tests/test_tuner_minsample.cpp` (registered in CTest — **5/5 C++ suites green**) and `tests/test_council_cost_controls.py`. Native-exit + council-gate decision logic already covered by `tests/test_strategy.cpp`. Council cost-control test logic verified live (yaml present); pytest itself not runnable in-session (no pytest).
- Task 12 (this turn): CLAUDE.md build-order step 3 + hard-rule aligned to native strategies / `dnn_advisory`; README.md + AUDIT.md corrected Binance→Coinbase and `dnn_advisory`; RETURN.md entry finalized; CONTEXT.md decisions logged.
- **Stable:** C++ safety spine + native strategy + council gate build clean, `ctest` 5/5. Engine refactor (adapt-gate extraction) is behaviour-preserving. RiskGate / live-gate / limit-weakening invariant untouched; live trading OFF by default.
- **Next / flags:** run full `pytest` + real numpy training in a venv; record real ClankApp/SEC-EDGAR fixtures; finish the residual doc-consistency sweep (`docs/ARCHITECTURE.md`, `docs/BUILD_SPEC.md`, `docs/FOLLOWUP_CREDENTIALS.md`, `docs/DNN_RL_DESIGN.md`) + AUDIT honest-state refresh. See RETURN.md + "Open Flags / Follow-ups".

### 2026-07-02 (Opus)

- Implemented real LLM council: OpenAI, Anthropic, Gemini providers plus Gemini Flash gate
- Split consensus.py into focused modules, added prompt caching
- Config flags use_real_council (default false) and gate_enabled (default true)
- 29 council tests, full suite 73 passing, RiskGate untouched
- Stable: council with offline mock fallback, real-vs-mock startup transparency
- Next: second master prompt for strategy layer and supporting infrastructure
