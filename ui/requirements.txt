# Plotly Dash dashboard / control board.
#
# EXACT version pins (Task 9 security hardening): reproducible installs, no
# silent supply-chain drift. Bump deliberately after review.
dash==2.18.2
plotly==5.24.1
pandas==2.2.3
PyYAML==6.0.3
# Encrypted-at-rest credential store for the Accounts/Connections page.
cryptography==46.0.5
# Live whale/SEC data-source fetches (lazy-imported; mock fallback if absent).
requests==2.32.5
